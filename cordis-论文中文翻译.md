# 时空可组合性的一种编程范式

**原文标题：** A Programming Paradigm for Spatiotemporal Composability

**作者：** Yifan Shi¹²（石一帆）⁽¹⁾, Wei Zhang¹（张伟）, Tianyi Cui²（崔天翼）

**单位：** ¹北京大学 ²DeepSeek-AI

> 译者注：作者中文姓名系根据拼音推断，如有出入以原文为准。本译文为学术翻译，数学符号、公式、代码与参考文献均保留原文形式。

---

## 摘要

现代软件——从插件系统到自我进化的智能体执行框架（agent harness）——日益要求动态组合（dynamic composition），但其形式化基础仍然欠发达。我们识别出该问题的两个正交维度：**时间可组合性**（temporal composability），即在移除组件时能够完全逆转该组件副作用的能力；以及**空间可组合性**（spatial composability），即能够声明并响应式地管理组件间依赖的能力。我们分别通过将经典效应（effect）与余效应（coeffect）概念提升（lift）为运行时机制来应对这两个维度。我们形式化了**可逆效应**（revertible effects），其中每个上下文变换都配备一个显式的逆变换，并证明效应追踪保持组合运算，从而保证组件移除时状态的完全恢复。我们形式化了**响应式余效应**（reactive coeffects），其中依赖被登记在一个带类型的上下文中，满足状态的变化会自动触发组件的激活与停用。一个**组件生命周期模型**将可逆效应与响应式余效应同各种控制流的交互操作化，产生一个统一上下文类型，将效应上下文与余效应上下文整合为一个连贯的编程范式。我们在 **Cordis** 中实现了这些思想——一个时空可组合性的元框架（meta-framework），它提供了一个带有效应追踪与余效应解析的核心库，以及一个带有配置调和（configuration reconciliation）与热模块替换（hot module replacement）的声明式组件加载器。

---

## 目录

1. 引言
   - 1.1 可组合性的维度
   - 1.2 动机示例（插件系统；自我进化的智能体执行框架；粗粒度变通方案）
   - 1.3 贡献
2. 预备知识
   - 2.1 效应
   - 2.2 余效应
   - 2.3 与动态可组合性的关系
3. 可逆效应与响应式余效应
   - 3.1 可逆效应（效应上下文；可逆效应函数）
   - 3.2 响应式余效应（余效应上下文；规格与通知；隔离与拦截）
   - 3.3 组件生命周期（幂等恢复；迭代；一致性；异步性）
   - 3.4 上下文范式（统一上下文；上下文范式的定位）
4. 实现与案例研究
   - 4.1 核心库（效应追踪；余效应操作；组件生命周期；上下文访问）
   - 4.2 组件加载器（声明式配置层；热模块替换）
   - 4.3 案例研究：Koishi
5. 讨论
   - 5.1 服务复用（service multiplexing）
   - 5.2 访问控制与沙箱化
   - 5.3 语言无关性与语言选择
   - 5.4 相互依赖与组件粒度
   - 5.5 依赖类型化与版本化
6. 相关工作
   - 6.1 效应与余效应系统
   - 6.2 编程范式
   - 6.3 时间可组合性
   - 6.4 空间可组合性
7. 结论
参考文献

---

## 1. 引言

组合（composition）——由较简单的部分组装出复杂系统——是软件工程的一项基本原则 [1]。传统上，组合是静态的：函数调用、模块导入与类继承在编译期解析，并在整个执行期间保持不变。然而，现代软件日益要求**动态组合**，即组件在运行时被加载、卸载与重新配置。插件架构 [2] 与自我进化的智能体执行框架都需要能够安全地在运行中添加和移除功能的系统，然而当前的实践退而求助于粗粒度机制 [3]，只能通过重启来重新配置，从而丢弃运行时状态。尽管动态组合的实际重要性日益增长，其理论基础仍欠发达——与静态组合所拥有的丰富形式框架相比尤其如此。

### 1.1 可组合性的维度

为了刻画动态组合的需求，我们在已得到充分研究的组合代数方面之外，识别出两个正交维度：

- **时间可组合性**针对时间维度：在移除一个组件时，该组件对共享环境所做的修改必须被完全而安全地逆转。这要求追踪组件执行的每一次资源分配、事件注册与状态变更，并保证在移除时有序地回收它们。
- **空间可组合性**针对空间维度：组件必须能够以结构化且可验证的方式声明、发现与解析彼此之间的依赖。这要求管理依赖拓扑，并响应依赖变化来协调组件生命周期。

在静态设定下，时间可组合性退化为词法作用域（例如 RAII [4]、bracket 模式 [5]），空间可组合性退化为模块导入解析。在动态设定下，组件在运行时到达与离开，两个维度都变得困难得多：时间可组合性必须处理长寿的、有状态的效应，其作用域不受词法边界约束；空间可组合性必须处理在执行过程中出现、消失或改变身份的依赖。

### 1.2 动机示例

#### 1.2.1 插件系统

插件系统是动态组合的典型实例。我们以 Visual Studio Code（VSCode）——使用最广泛的可扩展 IDE 之一——作为代表性示例。

**时间局限。** VSCode 在一个称为扩展宿主（extension host）的共享进程中运行所有扩展。尽管扩展可以被动态安装，该宿主却不提供在运行时卸载单个扩展代码的机制。一旦某个扩展的 `activate` 函数已经执行，禁用或卸载它就需要重启整个宿主，从而影响所有已加载的扩展。纯粹声明式的扩展（如主题、按键绑定和代码片段）不携带代码，可以自由移除。然而，在按安装量排名的前 100 个扩展中，有 87 个包含可执行代码 ¹，因此在移除时需要此类重启。尽管 VSCode 提供了 `deactivate` 钩子，它只是宿主进程即将终止时的优雅关闭回调，因而并不能实现实时移除。此外，该钩子将效应清理与效应创建（位于 `activate`）分离，违反了关注局部性（locality of concern），使完整清理难以验证。

**空间局限。** VSCode 确实提供了 `extensionDependencies` 用于声明扩展之间的依赖，但它很少被使用：在按安装量排名的前 100 个扩展中，只有 7 个对非内置扩展声明了 `extensionDependencies`。¹ 这种稀缺反映了扩展 API 的形态：它暴露的是固定的、浅层的扩展点，如命令、视图和语言功能。扩展通过这些扩展点向宿主贡献功能，而不是相互依赖，因此扩展间依赖很少出现。此外，VSCode 的扩展间交互机制不提供任何结构性契约：它通过 `vscode.extensions.getExtension(...).exports` 向其他扩展暴露功能，但返回的值是无类型的（默认是 `any`），因此依赖方无法依赖一个经过类型检查的接口。简而言之，VSCode 引导扩展走向一组固定的、由宿主提供的扩展点，且不提供让它们安全地、结构化地相互依赖的方式。

这两种局限并非 VSCode 独有；它们在插件系统中普遍存在 [2, 6]，只是程度不同。

> ¹ 数据取自 2026 年 6 月 9 日的 Visual Studio Code 市场（Marketplace）。

#### 1.2.2 自我进化的智能体执行框架

现代 AI 智能体依赖运行时智能体执行框架（harness）[7–9]。这些系统可能组合多样化的工具集 [10] 与执行环境、管理权限与沙箱、维护会话状态与持久化、提供上下文管理与记忆系统 [11]、编排子智能体与多智能体工作流 [12]，并向用户和自动化系统暴露接口。未来的执行框架可能会在持续服务请求的同时，生成并部署对自身组件的修改。模型合成的可复用工具为组件级自我修改提供了一个较窄的先驱 [13]。每一次此类修改本身都是动态组合的一个实例。

由于这些修改持续发生且很少或没有人工监督，动态可组合性变得不可或缺。没有时间可组合性，每一次自我修改都会强制完全重启，丢弃进程内累积的全部状态；在如此高的频率下，累积的不可用时间变得可观，进行中的任务会被反复打断；更糟的是，一个有缺陷的自我修改可能禁用掉恢复过程本身所需要的那个进程。没有空间可组合性，每个模块都必须自行检测并适应它所依赖的模块的出现、消失或身份变化，而且只能通过临时（ad hoc）手段进行；更糟的是，天真的代码替换策略可能静默地破坏依赖方，或引入只在重载时才显现的循环依赖。

#### 1.2.3 粗粒度变通方案

动态可组合性之所以受到的正式关注有限，一个原因是操作系统与容器编排器已经提供了一种粗粒度的替代品。进程终止与重启在整块地址空间的粒度上实现了时间可组合性；容器编排 [3] 在整项服务的粒度上实现了空间可组合性。在实践中，大多数软件通过退而使用这些粗粒度机制来容忍细粒度可组合性的缺失：行为异常的模块通过重启进程来处理，服务依赖则由编排器管理。

然而，这种变通方案代价高昂。在时间上，每次重启都会丢弃进程内累积的全部状态（例如缓存、连接、部分计算），重建这些状态需要数秒到数分钟 [14]；在此期间保持可用性需要冗余副本，为弥补无法恢复单个组件的不足而付出资源开销。在空间上，容器级编排无法表达共享同一地址空间的组件之间的依赖，并为本可以是本地函数调用的交互引入了网络开销。这两种机制都作用于进程与容器的边界，而现代系统越来越在更细的粒度上进行组合。这种粒度错配要求一种组合抽象，在与组件自身相同的层次上管理效应与依赖。

### 1.3 贡献

动态可组合性的两个维度分别关注计算如何**修改**其环境以及如何**依赖**其环境。这两个方向恰恰是效应系统 [15, 16] 与余效应系统 [17, 18] 所形式化的内容：效应为关于环境修改的推理提供了形式化词汇，余效应为关于环境需求的推理提供了形式化词汇。然而，现有的表述将推理限制在词法固定作用域上的编译期分析，没有扩展到组件在运行时到达与离开的动态场景。通过把效应提升为可逆的运行时模型、把余效应提升为响应式的依赖解析机制，我们为动态可组合性获得了一个统一的形式化基础——它语言无关，可应用于任何需要动态组合的软件架构。我们做出以下贡献：

1. **我们形式化了可逆效应**（第 3.1 节）：在该模型中，每个上下文变换都与一个显式的逆函数配对，产生效应函数，其追踪与恢复是保持组合性的运算。这为动态的时间可组合性提供了代数基础。
2. **我们形式化了响应式余效应**（第 3.2 节）：一个带类型的依赖上下文，其中组件将其需求声明为依赖集合，一个基于满足状态的通知机制自动将状态转移分类为激活、停用或中性。这为动态的空间可组合性提供了代数基础。
3. **我们建立了一个组件生命周期模型**（第 3.3 节），将可逆效应与响应式余效应同各种控制流的交互操作化，并将效应上下文与余效应上下文整合为一个统一上下文类型（第 3.4 节），构成一种面向动态可组合系统的编程范式。
4. **我们在 Cordis 中实现了这些思想**（第 4 节）：一个时空可组合性的元框架，提供了实现该形式模型的核心库（含效应追踪与余效应解析），以及一个带配置调和与热模块替换的声明式组件加载器。我们通过 Koishi 聊天机器人平台上的案例研究验证了该设计——一个在生产环境中使用、拥有超过 4000 个社区插件的生态系统。

---

## 2. 预备知识

本节简要概述效应系统与余效应系统——支撑我们工作的两根理论支柱。我们假定读者熟悉基本的类型论与范畴论；本节的目标是固定记号并介绍第 3 节将操作化为运行时机制的关键抽象。

### 2.1 效应

在简单类型 λ 演算（STLC）[19, 20] 中，类型判断 Γ ⊢ 𝑡 : 𝑇 表示项 𝑡 在上下文 Γ 下具有类型 𝑇。效应系统对类型进行精化，记录计算可能产生哪些副作用，得到如下形式的判断：

Γ ⊢ 𝑡 : 𝑇^effect　　(1)

这里，结果类型被标注上效应代数（effect algebra）中的一个元素，记录该计算可能产生哪些副作用，从而能够对带状态的计算进行组合式推理。这一方法源于 Lucassen 与 Gifford [21]，他们引入了一种区分类型、效应与区域（region）的种化类型系统，以发现并行程序中的调度约束。

**单子效应。** Moggi [15] 首先通过单子（monad）对计算效应进行范畴化建模；Wadler [22] 在 Haskell 中推广了这一方法。范畴 𝒞 上的单子 (𝑇, 𝜂, 𝜇) 将有效应的计算封装为类型 𝑇(𝐴) 的值，其中 𝜂 : 𝐴 → 𝑇(𝐴) 提升纯值，𝜇 : 𝑇(𝑇(𝐴)) → 𝑇(𝐴) 对嵌套计算进行排序。经典实例包括 Maybe 单子（偏序性）、State 单子（可变状态）与 IO 单子（外部交互）。

**代数效应。** Plotkin 与 Power [16, 23] 证明代数运算决定单子，建立了效应接口与其实现解耦的框架。一个效应签名 Σ 声明一组运算（例如，对状态而言的 get : () → 𝑆、put : 𝑆 → ()）；程序自由地调用运算，而无需承诺特定的解释。Plotkin 与 Pretnar [24] 随后引入了效应处理器（effect handler），通过提供续延语义来解释运算：

handle 𝑒 with { op(𝑣, 𝜅) ↦ … }　　(2)

处理器接收运算参数 𝑣 与受限制续延（delimited continuation）𝜅，它可以调用该续延零次、一次或多次，从而在统一框架内支持异常、协程与非确定性 [25]。Koka [26, 27]、Eff [28] 与 OCaml 5 [29] 等语言采用了代数效应，并作出了不同的设计取舍。

### 2.2 余效应

与效应相对偶，余效应系统 [17, 30] 丰富的是上下文而非类型，得到如下形式的判断：

Γ^coeffect ⊢ 𝑡 : 𝑇　　(3)

这里，上下文被标注上余效应代数中的一个元素，描述计算对环境的要求，例如要访问的资源、要持有的能力，或要依赖的服务。效应建模程序对世界的影响，而余效应建模世界对程序的约束。

**余单子余效应。** 使用余单子（comonad）来构造依赖上下文的计算，这一思想最早由 Uustalu 与 Vene [31] 提出，他们提出了对称（半）幺半余单子，作为 Moggi 效应单子框架的对偶，以刻画数据流与属性求值等概念。Petricek 等人 [17] 在这一基础上提出将余效应作为上下文依赖性的统一静态分析。一个余单子 (𝐷, 𝜀, 𝛿) 刻画依赖上下文（context-dependent）的计算：𝜀 : 𝐷(𝐴) → 𝐴 从上下文中提取当前值，𝛿 : 𝐷(𝐴) → 𝐷(𝐷(𝐴)) 为嵌套访问复制上下文。环境余单子 𝐷(𝑋) = 𝐸 × 𝑋 建模对固定环境 𝐸 的依赖；流余单子 𝐷(𝑋) = ℕ → 𝑋 建模对时序数据的依赖。

**分级余效应。** 为了更细粒度地追踪，分级余效应系统使用带预序的半环 𝒮 = (𝑆, ≤, +, ×, 0, 1) 作为余效应代数 [32]，这一体系后来由 Gaboardi 等人 [18] 与分级效应统一。𝑆 的元素标注每个变量绑定以量化其使用方式：0 表示未使用，1 表示线性使用，𝑛 表示有界使用，∞ 表示无限制使用。半环运算顺序地（×）与并行地（+）组合余效应，从而能够在统一的代数框架 [36] 内实现精确的资源追踪、敏感性分析 [33] 与信息流控制 [34, 35]。

### 2.3 与动态可组合性的关系

效应与余效应系统沿着两个互补方向组织关于计算的推理：效应描述计算如何修改其环境，而余效应描述计算如何依赖其环境。这两个方向恰对应第 1 节识别的动态可组合性的两个维度：

- **时间可组合性**要求组件对共享环境的修改在卸载时是可逆转的。相关的效应是有状态的效应，它们持久地变换该环境；逆转这样的变换要求它承认一个逆变换。
- **空间可组合性**要求组件间依赖被声明并响应式地管理。这样的依赖正是余效应所捕获的东西，管理它们相当于把每一个依赖对环境所供应的内容进行解析。

然而，经典效应与余效应系统是静态的工具：效应在词法固定的作用域内被追踪，并由编译期处理器释放（discharge）；余效应标注是针对执行前确定的上下文进行验证的。相比之下，动态组合要求这些保证对在运行时到达与离开的组件成立，并且针对的是持续演化的上下文。没有任何固定词法作用域能划定一个在部署之后才加载的插件；没有任何编译期上下文能预见到由运行时配置涌现出的依赖。

这促使视角的转变：与其给静态类型系统添加更多标注，不如将效应与余效应的概念结构**实体化**（reify），使运行时可以直接对它们操作，从而动态地确立这些系统静态地提供的保证。第 3 节将发展这一构造。

---

## 3. 可逆效应与响应式余效应

本节将第 2 节引入的效应与余效应概念提升为运行时机制，构建一种动态组合理论。核心思想是把承载效应与余效应的类型上下文转变成**上下文类型**——将上下文实体化为头等实体的、可被运行时操作的类型。对于效应类型，我们将其建模为配备逆变换的上下文变换，从而实现动态的时间可组合性。对于余效应上下文，我们将其建模为携带依赖信息的类型，从而实现动态的空间可组合性。这两种机制与不同控制流的相互作用产生一个组件生命周期模型，而同时承载效应与余效应的统一上下文本身就构成一种编程范式。

### 3.1 可逆效应

时间可组合性是在运行时加载与卸载组件、使得卸载时共享环境恢复到组合前状态的能力。这要求组件对环境的每一次修改既可追踪又可逆转。因此我们将效应建模为类型 Γ → Γ×(Γ → Γ) 的函数：作用于当前上下文时，它产生修改后的上下文连同显式的逆。逆变换使效应可逆，而将逆变换返回给运行时使效应可追踪。我们称这样的效应为**可逆效应**：通过在执行过程中记录并组合这些逆变换，环境的完全恢复成为结构性保证。

#### 3.1.1 效应上下文

给定任意非纯函数 𝑓_impure : 𝑋 → 𝑌，我们将其变换为纯形式 𝑓 : Γ×𝑋 → Γ×𝑌，其中 Γ 是上下文，所有可能的副作用都可以表示为对 Γ 的变换。对于任意固定的输入 𝑥 : 𝑋，导出的映射 𝛾 ↦ pr1(𝑓(𝛾, 𝑥)) 与返回值无关地捕获了 𝑓 的副作用。作为第一近似，假设每个效应的逆变换先验可得；那么 Γ 上可容许的效应在复合运算 ∘ 下构成对称群 Sym(Γ) 的一个子群 𝔉Γ ≤ Sym(Γ)，其公理对应系统的结构性要求：

- **封闭性**保证效应的顺序复合仍然是可容许的；
- **单位元**（idΓ，即 Γ 上的恒等函数）作为复合的单位；
- **逆元**要求每个效应 𝑓 ∈ 𝔉Γ 都承认一个还原 𝑓⁻¹ ∈ 𝔉Γ，保证任何效应复合都可以被撤销。

逆元要求由作用域限制来证明其合理性：Γ 只建模系统的内部状态，即系统对其保有完全控制的状态。系统对其自身上下文所做的每一处修改都是可逆的，因为先前的值总是可以恢复。针对系统边界之外实体（网络请求、文件 I/O）的操作并不修改 Γ；从 𝔉Γ 的视角看，它们相当于 idΓ。外部交互由领域特定策略（例如补偿事务 [37]）处理，不在本模型范围内。

为了在上下文内部记录效应，我们引入以下核心定义：

**定义 1.** 给定上下文 Γ，定义其效应上下文为：

𝜕Γ ≔ Γ×𝔉Γ　　(4)

它可以理解为二元组 (𝛾, 𝜑)，其中：

- 𝛾 ∈ Γ 表示当前上下文状态；
- 𝜑 ∈ 𝔉Γ 是将上下文恢复到其初始状态的变换。

特别地，初始状态可以表示为 (𝛾₀, idΓ)。

由于上述 𝜑 的存在，对 𝜕Γ 执行的所有效应都可以被自动追踪并完全恢复。现在我们给出具体构造。

**定义 2.** 定义从 𝔉Γ 到 𝔉𝜕Γ 的变换 trackΓ：

trackΓ : (Γ → Γ) → 𝜕Γ → 𝜕Γ
trackΓ = 𝑓 ↦ (𝛾, 𝜑) ↦ (𝑓(𝛾), 𝜑∘𝑓⁻¹)

该变换将上下文 Γ 上的任意函数 𝑓 ∈ 𝔉Γ 转换为效应上下文 𝜕Γ 上的函数。在状态 (𝛾, 𝜑) 下执行 trackΓ(𝑓) 时，𝑓 的逆被复合到 𝜑 上，从而在上下文中有效地追踪了 𝑓 的效应。每个 trackΓ(𝑓) 本身都是 𝜕Γ 上的双射，其逆为 (𝛾′, 𝜑′) ↦ (𝑓⁻¹(𝛾′), 𝜑′∘𝑓)；相应地，我们将 𝜕Γ 上的可容许效应定义为像集 𝔉𝜕Γ ≔ trackΓ(𝔉Γ)，由定理 4 可知它是 Sym(𝜕Γ) 的一个与 𝔉Γ 同构的子群。

**定理 3.** 下图可交换。即 ∀𝑓 ∈ 𝔉Γ，pr1∘trackΓ(𝑓) = 𝑓∘pr1。

（交换图：Γ —𝑓→ Γ；𝜕Γ —track(𝑓)→ 𝜕Γ；纵向箭头 pr1、pr1）

**定理 4.** track 保持 ∘ 运算。即 ∀𝑓, 𝑔 ∈ 𝔉Γ：

trackΓ(𝑓∘𝑔) = trackΓ(𝑓)∘trackΓ(𝑔)　　(5)

该定理保证 𝔉Γ 中的每个函数都可以构造出一个等价的、其效应在 𝜕Γ 上被追踪的追踪版本。

**定义 5.** 定义 𝜕Γ 上的变换 recoverΓ：

recoverΓ : 𝜕Γ → 𝜕Γ
recoverΓ = (𝛾, 𝜑) ↦ (𝜑(𝛾), idΓ)

该变换将恢复函数 𝜑 应用于当前状态 𝛾，并将 𝜑 重置为单位元。这有效地恢复了 𝛾 上的所有效应。下图说明在一系列效应 track(𝑓₁), ⋯, track(𝑓ₙ) 施加到 𝜕Γ 之后，recover 如何将上下文恢复到其初始状态：

（示意图：Γ —𝑓₁→ Γ —𝑓ₙ→ Γ —恢复→ Γ；𝜕Γ —track→ 𝜕Γ —track→ 𝜕Γ —recover→ 𝜕Γ）

#### 3.1.2 可逆效应函数

上一节的 track/recover 模型假定逆变换是给定的，并原子性地恢复所有累积效应。然而在实践中，每个效应的逆变换并非先验已知：它必须由调用者在效应施加点提供。此外，recover 是全有或全无的：它不能选择性地撤销一个效应而保留其他效应。为解决这两个问题，我们在输入端与输出端同时增强该模型：

1. 在输入端，我们不仅变换 Γ，还返回相应的逆函数，从而获得手动追踪能力：Γ → Γ×(Γ → Γ)，即 Γ → 𝜕Γ；
2. 在输出端，我们不仅变换 𝜕Γ，还返回相应的逆函数，从而获得部分恢复能力：𝜕Γ → 𝜕Γ×(𝜕Γ → 𝜕Γ)，即 𝜕Γ → 𝜕²Γ。

这一增强保持了输入与输出之间的结构一致性，因此我们仍然可以定义保持 track 数学性质的相应理论。我们将 𝔉Γ 增强为 𝔈Γ 与 𝔈*Γ：

**定义 6.** 定义效应函数 𝔈Γ 与严格效应函数 𝔈*Γ 为：

𝔈Γ ≔ Γ → Γ×(Γ → Γ)
𝔈*Γ ≔ (𝑒 : Γ → Γ×(Γ → Γ)) × ((𝛾 : Γ) → (let (𝛿, 𝑔) = 𝑒(𝛾) in 𝑔(𝛿) = 𝛾))　　(6)

其中 𝑒(𝛾) 产生二元组 (𝛿, 𝑔)，表示：

- 𝛿 : Γ 是新上下文；
- 𝑔 : Γ → Γ 是当前效应的逆函数。

上述定义中的约束 𝑔(𝛿) = 𝛾 可以用下面的交换图来直观表示，保证返回值的第二个分量确实是变换本身的逆：

（交换图：Γ —𝑓→ Γ；Γ —𝑓⁻¹→ Γ；𝜕Γ 上的 pr1、pr2 投影；𝑒 : Γ → 𝜕Γ）

正如 track 将 𝔉Γ 提升到 𝔉𝜕Γ，我们定义 effect 将 𝔈Γ 提升到 𝔈𝜕Γ：

**定义 7.** 定义效应函数变换 effectΓ 为：

effectΓ : 𝔈Γ → 𝜕Γ → 𝜕²Γ
effectΓ = 𝑒 ↦ (𝛾, 𝜑) ↦ let (𝛿, 𝑔) = 𝑒(𝛾) in ((𝛿, 𝜑∘𝑔), trackΓ(𝑔))

下图说明 effect 如何将 𝔈Γ 上的运算"翻译"到 𝔈𝜕Γ。我们将证明这一翻译确实保持图的交换性。

（交换图：Γ —𝑓→ Γ、Γ —𝑓⁻¹→ Γ；𝜕Γ —𝑒→ 𝜕Γ 与 pr1、pr2 投影；𝜕²Γ；effect 映射）

**定理 8.** effect 在 𝔈* 上封闭。即 ∀𝑒 ∈ 𝔈*Γ，effectΓ(𝑒) ∈ 𝔈*𝜕Γ。

由于效应函数 𝔈Γ 不再是上下文上的自同态，它们不能直接复合。因此我们定义一个新运算来表达效应复合：

**定义 9.** 给定函数 𝑓, 𝑔 ∈ 𝔈Γ，定义其效应复合 𝑓⋄𝑔 为：

𝑓⋄𝑔 : Γ → 𝜕Γ
𝑓⋄𝑔 = 𝛾 ↦ let (𝛿, 𝑠) = 𝑔(𝛾) in
　　　　　　let (𝜀, 𝑡) = 𝑓(𝛿) in
　　　　　　(𝜀, 𝑠∘𝑡)

**定理 10.** ⋄ 运算在 𝔈* 上封闭。即 ∀𝑓, 𝑔 ∈ 𝔈*Γ，𝑓⋄𝑔 ∈ 𝔈*Γ。

现在我们可以证明 effect 与 track 相类似的性质。

**定理 11.** effect 保持 ⋄ 运算。即 ∀𝑓, 𝑔 ∈ 𝔈Γ：

effectΓ(𝑓)⋄effectΓ(𝑔) = effectΓ(𝑓⋄𝑔)　　(7)

综合起来，这些构造构成**可逆效应**：𝔈*Γ 中的每个效应函数都显式地提供自己的逆；effect 在效应上下文 𝜕Γ 上追踪这些逆；⋄ 运算在保持可逆性的前提下组合它们。时间可组合性由此构造实现：加载一个组件相当于施加一序列效应函数（将逆累积进 𝜑），卸载它相当于施加 𝜑 以将上下文恢复到组合前的状态。

卸载一个组件是否会扰动另一个组件，取决于 ⋄ 下的交换性：若 𝑓⋄𝑔 = 𝑔⋄𝑓，则恢复 𝑓 后 𝑔 的贡献保持完好，两个效应可以安全地居于相互独立的组件中。当效应不可交换时，系统必须强制执行排序约束——要么在单个组件内部（通过效应迭代器的 LIFO 纪律，第 3.3 节），要么跨组件（通过施加依赖驱动激活顺序的响应式余效应，第 3.2 节）。

### 3.2 响应式余效应

空间可组合性是组件之间声明依赖、系统在运行时解析、提供与撤销这些依赖的能力。这要求每当共享上下文变化时都重新评估依赖满足状态，使得组件恰在其依赖变得可用时激活、在依赖被撤回时停用。因此我们将组件的依赖建模为一种**规格**（specification），并将上下文的每一次变化针对该规格分类为**激活**（activating）、**停用**（deactivating）或**中性**（neutral）。针对规格进行分类是检测满足状态变化的方式；响应该分类是驱动激活与恢复的方式。我们称这样的余效应为**响应式余效应**：通过分类上下文变化并由此驱动激活与恢复，正确的依赖排序成为结构性保证。

#### 3.2.1 余效应上下文

传统的控制反转（IoC）容器 [38, 39] 通常将依赖建模为简单的键值映射。本节将 IoC 形式化为余效应上下文，与可逆效应协同，为动态组合提供数学基础。

**定义 12.** 给定类型族 𝒱 : 𝐾 → Type，定义余效应上下文为依赖偏函数类型：

Σ ≔ (𝑘 : 𝐾) ⇀ 𝒱𝑘　　(8)

其中 𝜎 : Σ 是一个有限偏函数，为每个 𝑘 ∈ dom(𝜎) ⊆ 𝐾 赋一个类型为 𝒱𝑘 的值。我们记：

- 𝜎(𝑘) 为应用（当 𝑘 ∈ dom(𝜎) 时有定义）；
- 𝜎[𝑘 ↦ 𝑣] 为扩展（当 𝑘 ∉ dom(𝜎) 时有定义）；
- 𝜎∖𝑘 为限制（当 𝑘 ∈ dom(𝜎) 时有定义）；
- 𝑘 ∈ dom(𝜎) 为成员关系。

类型族 𝒱 的使用保证了每个依赖键 𝑘 都关联一个特定的值类型 𝒱𝑘，为依赖访问提供静态类型安全。扩展与限制的前置条件对应可逆效应中的幂等性要求：依赖不能被提供两次（扩展要求 𝑘 ∉ dom(𝜎)），也不能在缺失时被撤销（限制要求 𝑘 ∈ dom(𝜎)）。基于这一上下文结构，我们定义两个核心运算：

**定义 13.** Σ 上的 get 与 set 运算定义为：

get : (𝑘 : 𝐾) → Σ → 𝒱𝑘
get = 𝑘 ↦ 𝜎 ↦ 𝜎(𝑘)
set : (𝑘 : 𝐾)×𝒱𝑘 → Σ → Σ×(Σ → Σ)
set = (𝑘, 𝑣) ↦ 𝜎 ↦ (𝜎[𝑘 ↦ 𝑣], 𝜆𝜎′. 𝜎′∖𝑘)

其中 get(𝑘) 以前置条件 𝑘 ∈ dom(𝜎) 为前提，set(𝑘, 𝑣) 以前置条件 𝑘 ∉ dom(𝜎) 为前提。

值得注意的是，set(𝑘, 𝑣) 的类型是 𝔈Σ——恰恰是余效应上下文上的效应函数。因此我们可以直接应用第 3.1 节的效应机制：effectΣ 提供依赖登记的自动追踪与恢复。这正是响应式余效应与可逆效应之间的协同：余效应运算就是效应，而效应是可逆的。

#### 3.2.2 规格与通知

前述定义描述了单个依赖如何被登记与访问。然而，访问一个不存在的依赖是运行时失败。因此组件应当仅在它声明的所有依赖都存在时才激活，而不是乐观地访问并在缺失时失败。这引出两个问题：组件声明的依赖是否被联合满足，以及当该状态变化时系统应如何响应。余效应上下文 Σ 携带一种自然的可观察结构，使这两个问题都可处理：对于任意余效应规格 𝑑 ⊆ 𝐾，定义满足谓词：

𝜎 ⊧ 𝑑 ≔ ∀𝑘 ∈ 𝑑. 𝑘 ∈ dom(𝜎)　　(9)

该谓词是可判定的（因为 dom(𝜎) 是有限的）。由于对 𝜎 的所有变更都经过效应函数（其逆恢复先前的定义域），满足状态的变化在每个效应边界上都是可检测的。这就是响应性的代数基础：效应系统保证每次余效应变化都会被观察到。

**定义 14.** 余效应规格为：

𝔇Σ ≔ Set(𝐾)　　(10)

表示组件向环境声明的依赖集合。

使该规格具有响应性的是它如何分类状态转移。任何将 𝜎 变换为 𝜎′ 的效应都可以由规格 𝑑 ∈ 𝔇Σ 根据 𝑑 的满足状态是否被改变来分类：

**定义 15.** 给定余效应规格 𝑑 ⊆ 𝐾 与状态 𝜎, 𝜎′ ∈ Σ，定义：

notify𝑑(𝜎, 𝜎′) ≔
　激活（activating），若 𝜎 ⊭ 𝑑 且 𝜎′ ⊧ 𝑑
　停用（deactivating），若 𝜎 ⊧ 𝑑 且 𝜎′ ⊭ 𝑑
　中性（neutral），其他情况　　(11)

这是良定义的，因为 𝜎 ⊧ 𝑑 可判定，且所有状态转移都由效应函数中介。响应式不变式为：激活转移触发组件效应的执行（带完全效应追踪），而停用转移触发累积效应的恢复。这些转移的精确操作语义取决于它们与控制流的交互，将在第 3.3 节发展。

空间可组合性由 set 与 notify 的相互作用得出。若组件 𝐴 提供键 𝑘（通过 set(𝑘, 𝑣)），组件 𝐵 声明 𝑘 ∈ 𝑑𝐵，那么 𝐵 只能在 𝐴 完全激活并提供 𝑘 之后激活（因为 𝜎 ⊧ 𝑑𝐵 要求 𝑘 ∈ dom(𝜎)）。反过来，卸载 𝐴 将 𝑘 从 dom(𝜎) 中移除，这会破坏 𝐵 的满足状态；因此系统保证在 𝐴 的恢复开始之前，𝐵 已完全停用。于是依赖排序（依赖方在其依赖之后激活；依赖在其依赖方之前卸载）从通知机制中涌现出来，而无需单独指定。

#### 3.2.3 隔离与拦截

基础余效应上下文 Σ 建模一张扁平的依赖表。然而在实践中，系统可能需要为不同组件将不同的值绑定到同一逻辑依赖。本节用两种机制扩展余效应上下文：**余效应隔离**（同一键在不同上下文中解析出不同的值）与**余效应拦截**（对依赖访问的横切行为）。

**余效应隔离。** 通过引入隔离域（isolation realm），余效应隔离允许同一依赖在不同上下文中绑定到不同的值。这在多租户系统、测试环境与组件沙箱中具有广泛应用。

**定义 16.** 定义带隔离的余效应上下文为：

Σiso ≔ Map(𝐾, 𝑅)×((𝑟 : 𝑅) ⇀ 𝒱𝑟)　　(12)

它可以表示为二元组 (𝜌, 𝜎)，其中：

- 𝜌 : Map(𝐾, 𝑅) 是隔离域表，将逻辑键映射到域标识符；
- 𝜎 : (𝑟 : 𝑅) ⇀ 𝒱𝑟 是依赖表，是从域标识符到带类型值的部分依赖函数。

两层映射结构将逻辑层与存储层解耦，使依赖访问具有上下文感知能力。访问键 𝑘 时，系统首先解析 𝜌(𝑘) 获得域标识符 𝑟，然后访问 𝜎(𝑟) 获取实际值。

**定义 17.** Σiso 上的 get、set 与 isolate 运算为：

get : (𝑘 : 𝐾) → Σiso → 𝒱𝜌(𝑘)
get = 𝑘 ↦ (𝜌, 𝜎) ↦ 𝜎(𝜌(𝑘))
set : (𝑘 : 𝐾)×𝒱𝜌(𝑘) → Σiso → Σiso×(Σiso → Σiso)
set = (𝑘, 𝑣) ↦ (𝜌, 𝜎) ↦ ((𝜌, 𝜎[𝜌(𝑘) ↦ 𝑣]), 𝜆(𝜌′, 𝜎′). (𝜌′, 𝜎′∖𝜌′(𝑘)))
isolate : 𝐾×𝑅 → Σiso → Σiso×(Σiso → Σiso)
isolate = (𝑘, 𝑟) ↦ (𝜌, 𝜎) ↦ ((𝜌[𝑘 ↦ 𝑟], 𝜎), 𝜆(𝜌′, 𝜎′). (𝜌′∖𝑘, 𝜎′))

余效应隔离机制本质上实现了一个运行时临时多态（ad-hoc polymorphism）系统。通过隔离域标识符，同一依赖键可以在不同上下文中解析出完全不同的值，且这种多态可以在运行时被动态调整。与传统依赖注入相比，余效应隔离提供更细粒度的控制，能够为特定组件定制隔离；所有运算仍然是效应函数（𝔈Σiso），因此继承了可逆性。

**余效应拦截。** 第二种机制，余效应拦截，将横切元数据附加到依赖访问上，在不修改依赖值的情况下增加行为。该元数据既可以由上下文携带，也可以由组件声明，因此我们同时扩展余效应上下文与余效应规格：

**定义 18.** 定义带拦截的余效应上下文与规格为：

Σinter ≔ ((𝑘 : 𝐾) → ℳ𝑘)×((𝑘 : 𝐾) ⇀ (ℳ𝑘 → 𝒱𝑘))
𝔇inter ≔ (𝑘 : 𝐾) ⇀ ℳ𝑘　　(13)

上下文 Σinter 是二元组 (𝜄, 𝜎)：𝜄 是安装在上下文本身上的上下文携带元数据，默认为空（𝜖𝑘）；𝜎 将每个键 𝑘 映射到一个从元数据 ℳ𝑘 到值 𝒱𝑘 的提供者函数。规格 𝑑 ∈ 𝔇inter 携带组件声明的元数据，为每个键赋予其元数据 𝑑(𝑘)，其中 dom(𝑑) 充当依赖集合。每个键的元数据配备一个幺半群 (ℳ𝑘, ⊕𝑘, 𝜖𝑘)：合并运算 ⊕𝑘 是结合的，以 𝜖𝑘（空元数据）为单位元。

**定义 19.** Σinter 上的 get、set 与 intercept 运算为：

get : (𝑘 : 𝐾)×ℳ𝑘 → Σinter → 𝒱𝑘
get = (𝑘, 𝜇) ↦ (𝜄, 𝜎) ↦ 𝜎(𝑘)(𝜇⊕𝑘 𝜄(𝑘))
set : (𝑘 : 𝐾)×(ℳ𝑘 → 𝒱𝑘) → Σinter → Σinter×(Σinter → Σinter)
set = (𝑘, 𝜓) ↦ (𝜄, 𝜎) ↦ ((𝜄, 𝜎[𝑘 ↦ 𝜓]), 𝜆(𝜄′, 𝜎′). (𝜄′, 𝜎′∖𝑘))
intercept : (𝑘 : 𝐾)×ℳ𝑘 → Σinter → Σinter×(Σinter → Σinter)
intercept = (𝑘, 𝜈) ↦ (𝜄, 𝜎) ↦ ((𝜄[𝑘 ↦ 𝜄(𝑘)⊕𝑘 𝜈], 𝜎), 𝜆(𝜄′, 𝜎′). (𝜄′[𝑘 ↦ 𝜄(𝑘)], 𝜎′))

当具有规格 𝑑 的组件访问键 𝑘 时，系统求值 𝜎(𝑘)(𝑑(𝑘)⊕𝑘 𝜄(𝑘))：组件声明的元数据与上下文携带的元数据 𝜄 合并，提供者函数作用于合并结果。这一合并遵循每个键自身的语义（例如标量字段被覆盖、集合值字段被取并集），并且是右偏的，因此 𝜄(𝑘) 优先并可以覆盖组件的声明，让外层上下文能够约束组件如何使用余效应而无需修改该组件（例如第 5.2 节）。

### 3.3 组件生命周期

前几节发展的效应与余效应模型独立地定义了代数结构与规格。为赋予它们操作意义，我们将它们组合成**组件**（component）这一概念——两者共同支配其行为的运行时实体。这里 Γ 同时充当效应上下文与余效应上下文；具体统一见第 3.4 节。

**定义 20.** 上下文 Γ 上的组件定义为：

ℭΓ ≔ 𝔇Γ×𝔈Γ　　(14)

表示二元组 (𝑑, 𝑒)，配备一个可变生命周期状态，其中：

- 𝑑 ∈ 𝔇Γ：余效应规格，声明组件对环境要求的依赖；
- 𝑒 ∈ 𝔈Γ：效应函数，定义组件激活时贡献的效应。

组件的两侧共同决定其目标状态：从效应侧看，组件必须存活（其效应已施加到上下文且相应的逆尚未被调用）；从余效应侧看，所有声明的依赖必须被满足（𝜎 ⊧ 𝑑）。当两个条件都成立时目标状态为**激活**（ACTIVE）；否则为**未激活**（INACTIVE）。每当目标状态改变——无论由哪一侧引起——都会发起一次转移：**重载**（RELOAD）执行组件的效应函数 𝑒，在上下文上累积副作用；**卸载**（UNLOAD）施加累积的逆以恢复上下文。在最简单的形式中，生命周期就是图 1 所示的两状态模型。在实践中，转移与各种控制流结构交互，需要针对幂等恢复、迭代、一致性与异步性进行扩展。

```
            RELOAD
    ┌───────────────────┐
    ▼                   │
 INACTIVE            ACTIVE
    │                   ▲
    └───────────────────┘
            UNLOAD
```

**图 1 | 组件基础生命周期**

#### 3.3.1 幂等恢复

组件的 UNLOAD 施加累积的逆以恢复上下文；为了保证安全，每个逆至多运行一次。在 recover 层面，这一点由构造保证：定义 5 将 𝜑 重置为 idΓ，因此第二次 recover 是空操作。这一义务只出现在 effect 交还给调用者的部分释放器（disposer）上（定义 7 的 𝜕Γ → 𝜕²Γ 分量），它从累积的 𝜑 中切除单个效应的逆：调用它两次会施加该逆两次并破坏上下文。因此我们使每个返回的释放器是幂等的。

我们将每个释放器的"待发"（armed）状态作为其自身的私有状态，使受保护的逆保持为它所作用的、状态 𝛾 的纯函数，而不是依赖隐藏的可变状态。构建保护器（guard）是生成性的 [40]：每次应用都为新的释放器生成一个全新的句柄，因此没有两个释放器共享句柄，且它保持在保护器内部。实现将其实现为每个 dispose 闭包中新捕获的 armed 变量。

**定义 21.** 幂等保护器 idem : (Γ → Γ) → (Γ → Γ)（在 𝜕Γ 上同理）使逆至多在其第一次调用时触发。包裹是生成性的：每次应用都生成一个全新句柄 ℎ，它对所产生的释放器私有，且不出现在 idem 的类型中。状态记录已经耗尽的句柄；全新的 ℎ 不在其中，因而是活跃的，触发将其标记为耗尽：

idem(𝑔) = let ℎ fresh in 𝛾 ↦ { 𝑔(𝛾)[ℎ ↦ ()]　若 ℎ ∉ dom(𝛾)
　　　　　　　　　　　　　　　 𝛾　　　　　 若 ℎ ∈ dom(𝛾)　　(15)

由于 ℎ 在构建保护器时选定而非由调用者提供，且耗尽集合只增不减，释放器至多触发一次，却仍然是带前置条件的纯效应函数；𝜕Γ、𝜕²Γ 与 𝔉Γ 保持完好，耗尽集合不是可逆状态的一部分。

effectΓ 的幂等变体（定义 7）随后仅有一处替换不同：返回的逆 trackΓ(𝑔) 被 idem 包裹（它生成自己的句柄），而前向累积 𝜑∘𝑔（由已经幂等的 recover 恢复）保持不变。

effect_idemΓ : 𝔈Γ → 𝜕Γ → 𝜕²Γ
effect_idemΓ = 𝑒 ↦ (𝛾, 𝜑) ↦ let (𝛿, 𝑔) = 𝑒(𝛾) in ((𝛿, 𝜑∘𝑔), idem(trackΓ(𝑔)))

由于只有返回的逆被包裹，累积 𝜑 与第 3.1.2 节的同态结果不受影响。

#### 3.3.2 迭代

组件的 RELOAD 转移可能顺序执行多个效应，UNLOAD 必须恢复它们。我们使用**效应迭代器**（effect iterator）建模多步执行，其中每一步返回修改后的上下文、一个逆与一个续延：

**定义 22.** 定义效应迭代器 𝔈_iterΓ 与严格效应迭代器 𝔈_iter*Γ 为如下递归类型：

𝔈_iterΓ ≔ 𝜇ℑ. Γ → Γ×(Γ → Γ)×Maybe(ℑ)
𝔈_iter*Γ ≔ 𝜇ℑ. (𝑒 : Γ → Γ×(Γ → Γ)×Maybe(ℑ)) × ((𝛾 : Γ) → (let (𝛿, 𝑔, 𝑜) = 𝑒(𝛾) in 𝑔(𝛿) = 𝛾))　　(16)

其中 𝑒(𝛾) 产生三元组 (𝛿, 𝑔, 𝑜)，表示：

- 𝛿 是新上下文；
- 𝑔 是当前效应的逆函数；
- 𝑜 表示续延：
  - Nothing 表示迭代终止；
  - Just(𝑖) 提供下一步迭代。

效应迭代器变换 effect_iterΓ 通过递归调用将 effectΓ 扩展到迭代器结构：

**定义 23.** 定义效应迭代器变换 effect_iterΓ 为：

effect_iterΓ : 𝔈_iterΓ → 𝜕Γ → 𝜕²Γ
effect_iterΓ = 𝑖 ↦ (𝛾, 𝜑) ↦
　let (𝛿, 𝑔, 𝑜) = 𝑖(𝛾) in
　match 𝑜
　| Nothing ⇒ ((𝛿, 𝜑∘𝑔), trackΓ(𝑔))
　| Just(𝑖′) ⇒ let (𝑠, 𝑟) = effect_iterΓ(𝑖′)(𝛿, 𝜑∘𝑔) in
　　　　　　　(𝑠, trackΓ(𝑔)∘𝑟)

在每一步递归中，逆 𝑔 按应用顺序复合到 𝜑 上，因此累积的恢复函数 𝜑∘𝑔₁∘⋯∘𝑔ₖ 在施加时自然以 LIFO 顺序恢复效应。在任意两个连续步骤之间，系统可以中断转移——若目标状态已改变，则施加迄今累积的逆以恢复上下文。这一性质是迭代器模型内在的：每一步的 Maybe(𝔈_iter) 续延构成自然的可中断边界，无需额外机制，如图 2 所示。在这个意义上，效应迭代器是被实体化的受限制续延——主流语言通过 yield 运算符 [41] 暴露的结构，因此该模型可以直接映射到它们已经提供的生成器（generator）上。

```
            RELOAD ──(步骤边界可中断)──►
    ┌───────────────────────────────────┐
    ▼                                   │
 INACTIVE                            ACTIVE
    │                                   ▲
    └───────────────────────────────────┘
            UNLOAD
```

**图 2 | 带步骤边界中断的迭代式转移**

单步效应（𝔈Γ）是迭代器立即返回 Nothing 的退化情形，因此转移是原子的、没有中断边界。对于多步转移，步骤的粒度决定响应性：更细的步骤允许对目标状态变化作出更快的反应，代价是更频繁的条件检查。

作为进一步的扩展，将迭代器返回类型中的逆分量 Γ → Γ 替换为另一个迭代器，得到双向效应迭代器 𝔈_bidiΓ = 𝜇ℑ. Γ → Γ×ℑ×Maybe(ℑ)，其三元组 (𝛿, 𝑝, 𝑛) 同时提供前向续延 𝑛 与反向步骤 𝑝。这允许 UNLOAD 也增量地进行，并可被中断回 RELOAD。然而，很少有编程语言提供原生的双向迭代语义，限制了这一扩展的实际适用性。

#### 3.3.3 一致性

当依赖在运行时被替换时，目标状态可能在极短时间内经历 ACTIVE → INACTIVE → ACTIVE 的转移。如果为第一个 ACTIVE 发起的 RELOAD 仍在进行（或尚未被中断），第二个 ACTIVE 就到达了，系统将面临依赖值已经改变的 ACTIVE → ACTIVE 场景。简单地继续原来的 RELOAD 会使组件绑定到过时的依赖，破坏一致性。

为检测此类过时，我们引入**纪元**（epoch）机制对目标状态进行版本化。为余效应规格 𝑑 定义纪元函数 𝜀𝑑 : Σ → 𝐸：

𝜀𝑑(𝜎) ≔ ⟨𝜎(𝑘) | 𝑘 ∈ 𝑑⟩　　(17)

两个纪元相等当且仅当所有依赖值一致：𝜀𝑑(𝜎) = 𝜀𝑑(𝜎′) ⇔ ∀𝑘 ∈ 𝑑. 𝜎(𝑘) = 𝜎′(𝑘)。INACTIVE 的纪元是一个特殊的常量 ⊥𝐸，与任何激活纪元都不相等。

每当转移开始时，目标状态当前的纪元 𝜀target 被记录为 𝜀inert。在每个迭代器步骤边界，系统比较 𝜀target 与 𝜀inert：仅当它们匹配时下一步才继续；否则转移被中止并施加累积的逆。

纪元机制有效地将生命周期从一维的二元 INACTIVE/ACTIVE 模型扩展为多维的星形结构。ACTIVE 的不同依赖配置形成相互独立的分支，全部连接到 INACTIVE 状态，如图 3 所示。

```
                    INACTIVE (⊥)
                     ▲   ▲   ▲   ▲   ▲   ▲
                     │   │   │   │   │   │
          ┌──────────┘   │   │   │   │   └──────────┐
          │              │   │   │   │              │
 ACTIVE   ACTIVE    ACTIVE   ACTIVE   ACTIVE    ACTIVE
⟨a:1,b:1⟩ ⟨a:1,b:2⟩ ⟨a:1,b:3⟩ ⟨a:2,b:1⟩ ⟨a:2,b:2⟩ ⟨a:2,b:3⟩
```

**图 3 | 基于纪元（epoch）的生命周期：每种依赖配置构成独立分支**

#### 3.3.4 异步性

同步模型假定每个转移相对于外部状态变化是瞬时完成的。当转移涉及异步计算时，这一假定被打破。我们抽象地建模非即时性：转移产生一个类型为 Future(𝐴) 的值，其中 Future 是一个不透明类型构造子，其定义性属性是：在提交与解析之间，外部状态可能改变。

在该模型下，RELOAD 与 UNLOAD 占据真实时间，期间目标状态可能改变。这从两方面威胁资源安全：立即回滚进行中的 RELOAD 违反尚未执行完毕效应的 LIFO 顺序；快速振荡可能两次调用同一逆。为恢复安全，我们将 RELOAD 与 UNLOAD 从瞬时转移提升为**惯性状态**（inertial states）：一旦进入，转移运行至完成，之后系统才对目标状态的任何变化采取行动。由此产生的状态机如图 4 所示。

```
              RELOAD
    ┌───────────────────────────┐
    │        (惯性，运行至完成)   │
    ▼                           │
 INACTIVE                    ACTIVE
    │        (惯性，运行至完成)   │
    └───────────────────────────┘
              UNLOAD
```

**图 4 | 异步转移的惯性状态机**

惯性状态的语义是：

- 若组件处于 RELOAD 且目标变为 INACTIVE，则 RELOAD 运行至完成，然后 UNLOAD 开始。若目标在 RELOAD 完成前恢复为 ACTIVE，则组件正常进入 ACTIVE。
- 对称地，若组件处于 UNLOAD 且目标变为 ACTIVE，则 UNLOAD 先完成，然后 RELOAD 开始。若目标恢复为 INACTIVE，则组件进入 INACTIVE。
- 待决的反向转移被推迟到当前惯性状态解析，届时根据当时的目标决定执行或丢弃。

迭代器步骤边界与惯性语义组合：在每一步，系统检查纪元是否仍然匹配。若不匹配，转移中止（施加累积的逆），待决转移开始；这就是在每个惯性转移内部运作的同步中断机制。

### 3.4 上下文范式

第 3.3 节的组件定义使用单一上下文 Γ 同时承载效应与余效应。本节为该统一给出具体构造，并论证由此产生的上下文类型本身构成一种编程范式。

#### 3.4.1 统一上下文

对于上下文 Γ，效应上下文 𝜕Γ（第 3.1 节）提供了一个更高层的抽象，记录上一层上下文与该层累积的副作用。使该结构递归并组合余效应上下文 Σ，得到如下类型：

**定义 24.** 上下文类型 Γ∞ 定义为：

Γ∞ ≔ 𝜇Γ. Γ×(Γ → Γ)×Σ　　(18)

其中三个投影分别为：

- Γ：当前上下文状态（递归）；
- Γ → Γ：用于效应恢复的累积逆；
- Σ：携带依赖信息的余效应上下文。

在该定义下，effect 成为 Γ∞ 上的自同态，将 𝜕-塔统一为单一自相似类型。余效应上下文 Σ 被结构性地整合：依赖运算（set、get）作用于 Σ 分量，而第二分量的逆函数追踪它们的撤销。由于 Σ 之下的类型族 𝒱 不受约束，系统需要在组件间共享的任何全局状态都可以编码为带适当值类型的依赖——Σ 涵盖所有共享可变状态，而不仅仅是组件间依赖。组件与环境之间的每一次交互都经过这一单一实体。

**层次组合。** Γ∞ 的递归结构支持层次化控制：父上下文聚合多个子层级的效应，形成树形控制结构，在保持模块化的同时实现跨层级统一管理。效应变换实现了字面意义上的"即插即用"隐喻：

- 加载组件对应于执行其效应（插入）；
- 卸载组件对应于恢复其效应（拔出，不影响其他运行中的组件）；
- 层次中不同层级的组件可独立加载与卸载；父上下文聚合并管理其所有子级的效应，支持任意嵌套的组合。

**指称与实现。** 将运算类型化为 Γ∞ 上的效应，固定了它的**指称**（denotation）——后继状态与逆的配对——但未固定它的**实现**（realization），后者决定该逆如何执行。

**定义 25.** 效应函数 𝑓 : 𝔈Γ∞ 承认两种实现：

- **原地实现**（in-place realization）改变上下文并返回非平凡逆；后继状态与输入共享别名，恢复运行逆以逆转该变更。
- **派生实现**（derived realization）保持输入不变，在 Γ∞ 的递归结构中返回一个以恒等为逆的全新上下文；恢复丢弃派生的上下文。

在纯函数设定下两者重合；命令式宿主可以对每个运算任选其一。第 4.1.2 节将给出两种实现的代表性实现。

#### 3.4.2 上下文范式的定位

编程范式在如何处理副作用上有着根本差异。两个既有的极点界定了该谱系：

**显式状态穿线（函数式）。** 为保持引用透明性，纯函数式语言将副作用建模为对状态的显式变换。State 单子 𝑆 → (𝐴, 𝑆) [22] 将环境穿线（thread）过每一次计算。这一方法产生强组合保证：效应在类型中可见，适合等式推理。然而，它施加了显著的可用性代价：调用链中的每个函数都必须接受并返回状态参数，即使它只是原样传递状态。随着效应维度增多（日志、配置、I/O），单子堆叠或效应处理器样板代码激增。

**隐式变更（命令式/面向对象）。** 主流命令式语言允许组件在调用点无需显式声明即可修改共享状态与访问依赖。在效应侧，代表性示例是 React 的 `useEffect` 钩子：它在组件内部 fiber 上登记一个持久副作用，但效应目标与登记机制都不作为显式参数出现——识别依赖调用次序在隐藏运行时状态中的位置。在余效应侧，Java 的服务定位器模式（例如 Spring 的 `ApplicationContext.getBean(...)`）在运行时从全局注册表中检索依赖，在每个调用点都需要空值检查与类型转换；依赖关系是隐式的，散布于整个代码库。更一般地，理解 f() 如何修改或依赖系统需要传递性地阅读其实现。重构变得脆弱，因为移动或删除一个调用可能静默破坏遥远的不变式。

**上下文范式**结合了函数式方法的可追踪性与命令式方法的可用性。效应与余效应都通过显式上下文参数来中介。因此每个运算都可以归因于它所调用的具体上下文，进而归因于该上下文所属的组件。

除了结合两个极点的优点外，上下文范式让开发者能够单独处理每个效应与依赖，并自动将它们组合进系统行为。对于可逆效应，开发者只需提供每个原子运算的逆，任何复合的逆都由组合推导得出（第 3.1 节），因此加载与拆除天然可逆。对于响应式余效应，组件只声明它需要的依赖，运行时自动解析与重新接线（第 3.2 节），在提供者被添加、移除或替换时保持依赖接线一致。在这两个方向上，原本依赖开发者纪律的正确性成为范式的结构性质。

---

## 4. 实现与案例研究

本节介绍 **Cordis**，它将第 3 节的形式模型实现为一种实用的编程抽象。Cordis 是一个时空可组合性的**元框架**：与针对特定领域（如 Web 路由、ORM、UI 渲染）的应用框架不同，它不预设任何具体场景；它唯一的职责是提供通用的动态组合语义。实现分为三层：(1) **核心库**（第 4.1 节）直接实现效应与余效应系统；(2) **组件加载器**（第 4.2 节）在核心之上扩展配置调和与热模块替换；(3) **应用框架**（如 Koishi，第 4.3 节）在前两层之上构建领域特定功能。

### 4.1 核心库

表 1 总结了理论构造与其运行时对应物的对应关系。特别地，我们在本节通篇使用下文引入的运行时名称，保留理论符号用于形式对应。我们还用 @@name 表示框架内部的符号键（symbol key），因此 ctx[@@store] 中的方括号表示对上下文上不透明槽位的符号键访问，而不是对字符串键映射的索引。

**fiber** 是组件的运行时实例：携带组件走过第 3.3 节生命周期的有状态对象。它将组件的静态规格与生命周期算法读取的活动转移状态捆绑在一起。静态规格包括余效应规格 fiber.inject（即 𝑑）与效应函数 fiber.apply（即 𝑒），以及两个相关上下文：fiber.parent——fiber 实例化所在的上下文，与 fiber.ctx——由它派生的全新子上下文（定义 25），fiber 在其中运行。转移状态包括累积逆 fiber.dispose（即 recover）、目标状态版本 fiber.epoch（即 𝜀𝑑(𝜎)，其中 ⊥ 表示 INACTIVE），以及进行中转移的句柄 fiber.inertia。表 1 将上述各项与它们的理论对应物并列列出。

| 理论（第 3 节） | 实现 |
|---|---|
| Γ∞ | ctx，头等上下文 |
| 𝔈Γ、𝔈_iterΓ | 返回/产出逆的效应回调（effect callback） |
| effectΓ(𝑒) | ctx.effect(callback) |
| Σ、Σiso、Σinter | ctx[@@store]、ctx[@@isolate]、ctx[@@intercept] |
| get(𝑘)、set(𝑘, 𝑣) | ctx.get(key)、ctx.set(key, value) |
| isolate(𝑘, 𝑟) | ctx.isolate(key, realm) |
| intercept(𝑘, 𝜈) | ctx.intercept(key, metadata) |
| ℭΓ | fiber，组件的运行时实例 |
| 𝑑 : 𝔇Γ | fiber.inject |
| 𝑒 : 𝔈Γ | fiber.apply |
| 𝜀𝑑(𝜎) | fiber.epoch |
| recover | fiber.dispose，累积逆 |

**表 1 | 理论到实现的对应关系**

本节其余部分自底向上构建核心库。第 4.1.1 节实现可逆效应——唯一允许改变上下文的原语；第 4.1.2 节在其上实现响应式余效应；第 4.1.3 节将两者组合为组件生命周期；第 4.1.4 节暴露基于它们构建的上下文级运算。

#### 4.1.1 效应追踪

本节实现可逆效应（第 3.1 节）。Cordis 中的每一次上下文变更都流经唯一原语 ctx.effect：余效应提供、组件实例化以及所有其他改变上下文的运算都归结为一次 ctx.effect 调用，因此任何通过上下文执行的运算都会被自动追踪，并在组件卸载时被恢复。操作上，ctx.effect 是 effect_iterΓ（定义 23）的实现：它接受类型为 𝔈_iterΓ 的回调并将其提升为 𝔈_iter𝜕Γ，产生一个 dispose 闭包，调用时恢复效应。Cordis 通过这一个运算同时接受 𝔈Γ 与 𝔈_iterΓ（临时多态）；我们以迭代器形式为代表，因为普通效应函数就是产出单个逆的退化迭代器。

算法 1 展示了 ctx.effect 的构造。我们记 𝑓∘𝑔 为先运行 𝑔 再运行 𝑓 的释放器，记 id 为空操作；因此前置（prepend）每个新逆可得到 LIFO 恢复。

**算法 1 效应追踪**

```
1  async function execute(callback, guard)
2    iter ← callback()
3    inverse ← id
4    while guard()
5      (value, done) ← await iter.next()
6      if value then inverse ← value ∘ inverse
7      if done then break
8    return inverse
9  function effect(ctx, callback)
10   armed ← true
11   task ← execute(callback, () ↦ armed)
12   async function dispose()
13     if not armed then return
14     armed ← false
15     recover ← await task
16     recover()
17   ctx.dispose ← dispose ∘ ctx.dispose
18   return dispose
```

引擎 execute 将回调作为效应迭代器（𝔈_iterΓ，定义 22）驱动，并将每一步产出的逆折叠为单一复合。在每一步之前它咨询调用者提供的 guard；一旦 guard 失效，迭代停止，只保留迄今累积的逆。这就是第 3.3.2 节的步骤边界中断：Maybe(𝔈_iter) 续延由迭代器的 done 标志与 guard 共同实现。

ctx.effect 是 execute 的一个薄包装，额外添加两件事。其一，幂等自释放（实现 idem，定义 21）：guard 报告 armed 标志，返回的 dispose 将 armed 翻转为 false，这同时停止任何进行中的迭代并使恢复至多触发一次。其二，父级组合：dispose 被前置到外层上下文的累积逆 ctx.dispose 上，因此子效应的逆本身是父上下文上的效应，实现 𝜕²Γ 的递归结构。组件层级（第 4.1.3 节）复用同一 execute，但用测试纪元稳定性的 guard 取代 armed。

#### 4.1.2 余效应操作

本节实现响应式余效应（第 3.2 节）。所有余效应操作作用于每个上下文携带的三个符号键槽位：

- @@store：值存储 𝜎 : (𝑟 : 𝑅) ⇀ 𝒱𝑟，从域符号到带类型值的映射；
- @@isolate：域表 𝜌 : Map(𝐾, 𝑅)，从余效应键到域符号的映射；
- @@intercept：拦截表 𝜄 : (𝑘 : 𝐾) → ℳ𝑘，为每个键分配元数据。

前两者组合成两层解析 𝑘 → 𝜌(𝑘) → 𝜎(𝜌(𝑘))：ctx.get(key)（算法 2）先从 @@isolate 读取域符号 𝜌(𝑘)，再从 @@store 读取绑定值 𝜎(𝜌(𝑘))。𝜌 间接层让隔离将键重定向到独立绑定，而 @@intercept 仅在绑定被访问时被查询，调整的是它的使用方式而非解析目标。我们分两部分实现这些运算：(1) **提供与通知**——安装或撤回绑定并向依赖方传播变化；(2) **隔离与拦截**——重塑键的解析方式。

**提供与通知。** 由于 set(𝑘, 𝑣) 的类型是 𝔈Σ（第 3.1 节），余效应提供是一次 ctx.effect 调用，并继承其自动追踪与恢复。算法 2 实现了 ctx.set(key, value)，即具体的 set(𝑘, 𝑣)：回调在域符号 𝜌(𝑘) 下向存储绑定一个值，返回的 dispose 函数移除它。安装与移除都调用 notify 将变化传播给依赖组件。

**算法 2 余效应操作**

```
1  function get(ctx, key)
2    realm ← ctx[@@isolate][key]                 ▷ 𝜌(𝑘)
3    return ctx[@@store][realm]                   ▷ 𝜎(𝜌(𝑘))
4  function set(ctx, key, value)
5    function callback()
6      realm ← ctx[@@isolate][key]                ▷ 𝜌(𝑘)
7      ctx[@@store][realm] ← value                ▷ 𝜎[𝜌(𝑘) ↦ 𝑣]
8      notify(ctx, [key])
9      return function()
10       delete ctx[@@store][realm]                ▷ 𝜎∖𝜌(𝑘)
11       notify(ctx, [key])
12   return ctx.effect(callback)
```

算法 3 通过针对每个活动 fiber 测试：某个变化的键是否出现在其 fiber.inject 中，并解析到同一域；若是，则调用 refresh（第 4.1.3 节）基于新状态重新评估该 fiber。这就是定义 15 的响应式分类：使 𝜎 ⊧ 𝑑 翻转的变化会激活或停用该 fiber，而 refresh 的幂等性使中性变化无害。这种重新评估与各种控制流的交互将在第 4.1.3 节发展。

**算法 3 响应式通知**

```
1  function notify(ctx, keys)
2    for fiber in all_fibers do
3      for key in keys do
4        if key ∈ fiber.inject and fiber.ctx[@@isolate][key] = ctx[@@isolate][key] then
5          refresh(fiber)
6          break
```

**隔离与拦截。** 这两个运算在结构上做同样的事：各自派生一个子上下文，为 key 调整一张继承的表，父上下文保持不变，因此恢复是隐式的：丢弃子上下文即可，无需运行显式逆。ctx.isolate(key, realm) 用 realm（默认新生成的符号）覆盖域映射 𝜌（实现 isolate，定义 17），因此将不同符号赋给同一键的两个上下文解析到独立的绑定。ctx.intercept(key, metadata) 将元数据合并进拦截表 𝜄（实现 intercept，定义 19）：按该定义，新元数据与上下文已为 key 携带的元数据组合，并对其优先。

#### 4.1.3 组件生命周期

组件通过 ctx.use 实例化为 fiber。本节为 fiber（第 4.1 节引入）赋予第 3.3.4 节惯性状态机的操作意义。两个字段驱动下述算法：fiber.parent——fiber.ctx 的父上下文，构成组件层次（Γ∞ 的递归结构，第 3.4.1 节）；fiber.inertia——进行中异步转移的句柄（空闲时为 null）。

算法 4 展示组件实例化。组件将余效应规格 component.inject（即 𝑑）与效应函数 component.apply 配对；实例化将组件的配置绑定到 fiber.apply（第 9 行）——配置应用后的效应函数（即 𝑒），生命周期随后运行它。回调函数（第 2 行）是在父 fiber 中被追踪的效应：执行时它通过调用 refresh（算法 5）发起子级的生命周期；被恢复时它强制子级的纪元为 ⊥ 并触发卸载。这保证了卸载父级会级联到所有子级，这是父级效应上下文上 ⋄ 组合的直接结果。

**算法 4 组件实例化**

```
1  function use(ctx, component, config)
2    function callback()
3      refresh(fiber)
4      return function()
5        fiber.epoch ← ⊥
6        unload(fiber)
7    fiber ← Fiber(parent: ctx, inject: component.inject)
8    fiber.ctx ← ctx[fiber ↦ fiber]
9    fiber.apply ← () ↦ component.apply(fiber.ctx, config)
10   ctx.effect(callback)
11   return fiber
```

算法 5 实现第 3.3.4 节的惯性状态机，其中 RELOAD 与 UNLOAD 是惯性的：一旦进入，转移运行至完成，之后系统才对目标状态变化作出响应。refresh 函数从余效应存储重新计算 fiber 的纪元，若 fiber 尚未处于转移中，则发起 reload 或 unload 任务 ²。reload 函数记录当前纪元并执行组件的效应函数 apply。完成后它检查纪元是否仍然匹配：若匹配，fiber 稳定进入 ACTIVE；若不匹配（无论新纪元是 ⊥ 还是不同的激活配置），则链接到 unload。对称地，unload 以 LIFO 顺序恢复所有被追踪的效应，然后要么稳定进入 INACTIVE，要么链接到 reload。这种相互递归实现了惯性性质：一旦转移开始，它必然在任一新转移开始前完成。

**算法 5 组件生命周期**

```
1  function refresh(fiber)
2    epoch ← 𝜀𝑑(𝜎)
3    if epoch = fiber.epoch then return
4    fiber.epoch ← epoch
5    if fiber.inertia then return
6    if epoch ≠ ⊥ then
7      fiber.inertia ← create_task(reload(fiber))
8    else
9      fiber.inertia ← create_task(unload(fiber))
10 async function reload(fiber)
11   epoch0 ← fiber.epoch
12   recover ← await execute(fiber.apply, () ↦ fiber.epoch = epoch0)
13   fiber.dispose ← recover ∘ fiber.dispose
14   if fiber.epoch = epoch0 then
15     fiber.inertia ← null
16   else
17     fiber.inertia ← create_task(unload(fiber))
18 async function unload(fiber)
19   await fiber.dispose()
20   fiber.dispose ← id
21   if fiber.epoch = ⊥ then
22     fiber.inertia ← null
23   else
24     fiber.inertia ← create_task(reload(fiber))
```

纪元通过针对当前余效应存储解析每个声明键并取解析值的元组来计算（第 3.3.3 节）。由于 notify（第 4.1.2 节）在每次余效应变化时重新计算纪元，fiber 恰在解析值变化时重载。

该算法在两个互补层级上运作。在转移层级，reload 与 unload 在完成时检查纪元，支持跨转移的惯性链接。在每个转移内部的步骤层级，效应执行（算法 1）在每个迭代器步骤边界检查纪元，支持单个转移内的部分回滚。这两种机制对应第 3.3.3 节的转移间与转移内纪元检查。

#### 4.1.4 上下文访问

第 4.1.2 节的余效应操作构成一个反射式（reflective）API：余效应用 ctx.set(key, value) 写入、用 ctx.get(key) 读取，两者都按键名。Cordis 在此反射式 API 之上叠加第二种更原生的扩展与消费上下文的方式：**属性访问**。组件可以将余效应作为属性 ctx[key] 访问，仿佛它是上下文的原生结构，而不是通过方法调用。在 TypeScript 中，Cordis 通过 Proxy 实现这一点，其 get 陷阱中介每一次属性访问。算法 6 展示上下文如何将此类访问解析为余效应，基于第 4.1.2 节的原语 get。

**算法 6 Proxy 中介的上下文访问**

```
1  function resolve(ctx, key)
2    fiber ← ctx.fiber
3    repeat
4      if key ∈ fiber.inject then return get(ctx, key)
5      if fiber = root then throw UNDECLARED_ACCESS
6      fiber ← fiber.parent.fiber
```

算法 6 从访问上下文出发向上沿 fiber 链行走：在第一个于其 inject 中声明 key 的 fiber 处，访问被授权，值由 get（算法 2）取得；若行走到达根而仍无此类声明，访问被拒绝。这就是 proxy 与裸 ctx.get 的不同之处：ctx.get(key) 是全函数查找，返回绑定值或什么都不返回、永不失败；而 proxy 拒绝任何对未声明余效应的访问，在使用点强制余效应规格 𝑑。声明但缺失的值在此不会出现，因为 fiber 只有在所有声明的余效应被满足后才成为 ACTIVE（第 4.1.3 节）。

这种拒绝是在访问点执行的运行时检查。由于组件的余效应规格 𝑑 是静态声明的，同样的违规原则上可以在编译期检测到：在执行前针对声明的 𝑑 解析每个 ctx[key]；第 5.3 节讨论宿主语言的类型级依赖声明与编译期元编程如何恰好在进行这种中介。

### 4.2 组件加载器

核心库为组件开发者提供了动态组合的命令式原语，如 ctx.effect、ctx.use 与 ctx.set。应用编排者有一个不同的关注点：将既有组件组装成运行系统，并在其生命周期内调整组合。组件加载器通过引入**声明式配置层**来解决这一关注点：编排者将期望的组合指定为持久数据结构，加载器将对该规格的变更翻译为相应的命令式 fiber 操作。

#### 4.2.1 声明式配置层

Cordis 系统的运行时是 fiber 的组合，它们按照所栖身的上下文层次形成树（第 3.4.1 节）。通过将每个 fiber 序列化为条目（entry），并将条目组织进同一树结构，声明式层将应用的结构捕获为持久规格。

**定义 26.** 一个条目声明单个 fiber，记录：

- id —— 稳定标识符，在其组（group）的子列表变化时用作调和键（reconciliation key）；
- url —— 要实例化的组件模块的 URL；
- isolate —— 应用到条目上下文上的隔离注解；
- intercept —— 应用到条目上下文上的拦截注解；
- config —— 绑定进组件以形成其效应函数 apply 的配置；
- disabled —— 条目是否被管理性地关闭。

在运行时，条目管理它声明的 fiber，并响应这些字段的变化，将声明式规格绑定到其命令式实现。

这些条目构成配置树，它是系统加载内容的权威记录。条目可以是映射到单一 fiber 的叶子，或其组件可能转而加载更多组件，使条目成为分支节点。Cordis 为这类分组与嵌套加载提供组件：@cordisjs/group 以子条目列表为配置并将它们作为子组加载，而 @cordisjs/include 加载外部配置文件（YAML 或 JSON）并将其条目作为嵌套子树嫁接进来。

当条目的记录变化时，加载器增量调和：它不拆毁 fiber 并整体重建，而是检查哪些字段变化了，并为每个字段应用扰动最小的操作。

- id、url —— 重建条目，因为其身份或其组件已变化；
- isolate —— 重写条目的域映射，迁移条目提供的任何余效应，并通知解析发生偏移的依赖方；
- intercept —— 就地更新，因为拦截元数据在读取时被查询，无需重载；
- config —— 交给组件，由组件决定如何应用新载荷，通常通过与先前配置做差异比较、仅在实质性变化时重载。特别地，@cordisjs/group 条目的配置是其子条目列表，因此它以子 id 上的键控差异（keyed diff）应用更新，创建、移除或更新每个子项；由于更新幸存的子项会重新进入同样的逐字段分派，组调和与条目更新沿树向下共同递归；
- disabled —— 设置时卸载 fiber，清除时重载它。

Cordis 还允许组件在运行时更新自身配置或禁用自身。无论哪种情况，加载器都将变化写回配置层，使持久化的规格保持对实时系统的忠实。

#### 4.2.2 热模块替换

热模块替换（HMR）在模块层级应用可逆效应模式：当源文件变化时（通常是在开发期间），系统就地替换受影响的模块而不重启进程。由于 fiber 已经界定了其组件全部效应与余效应，一个本身是组件的模块可以通过 fiber 运算单独替换：释放旧 fiber 恢复组件安装的一切，从重载模块实例化的新 fiber 重新安装它。因此，与 Webpack [42] 或 Vite [43] 的 HMR 相比，HMR 不需要开发者标注的接收边界（acceptance boundaries）。

@cordisjs/hmr 组件提供 HMR 引擎，分三个阶段运作。

**阶段 1：模块分类。** 引擎接受两个输入：stashed 集合（自上次重载以来内容发生变化的文件 URL）与 externals 集合（不能被热替换、转而触发完全重启的模块）。记 get_imports(url) 为 url 直接导入的模块，它将变化的依赖子图分类，将每个模块标记为 accepted（接收）或 declined（拒绝）：

**算法 7 模块分类**

```
1  function classify(stashed, externals)
2    accepted ← stashed
3    declined ← externals
4    pending ← ∅
5    for url in stashed do
6      pending ← pending ∪ (get_imports(url) ∖ (accepted ∪ declined))
7    repeat
8      progress ← false
9      for url in pending do
10       if get_imports(url) ∩ accepted ≠ ∅ then
11         accepted ← accepted ∪ {url}
12         pending ← pending ∖ {url}
13         progress ← true
14       else if get_imports(url) ⊆ declined then
15         declined ← declined ∪ {url}
16         pending ← pending ∖ {url}
17         progress ← true
18       else
19         pending ← pending ∪ (get_imports(url) ∖ (accepted ∪ declined))
20   until not progress
21   declined ← declined ∪ pending
22   return (accepted, declined)
```

以 stashed 文件的导入作为种子，这一不动点（fixed point）在模块的某个导入被接收时接收该模块，在其所有导入都被拒绝时拒绝该模块；任何悬而未决的模块（如陷入导入环）默认被拒绝。

**阶段 2：过时条目检测。** 使用 accepted 与 declined，引擎随后将组件条目过滤为过时的条目，即依赖树到达已变化模块的条目。它用 get_dependencies 遍历每个条目的树，后者以 declined 为边界收集模块的传递导入：

**算法 8 过时条目检测**

```
1  function get_dependencies(root, declined)
2    deps ← ∅
3    function traverse(url)
4      if url ∈ deps or url ∈ declined then return
5      deps ← deps ∪ {url}
6      for child in get_imports(url) do traverse(child)
7    traverse(root)
8    return deps
9  function detect(entries, accepted, declined)
10   stale_entries ← ∅
11   for entry in entries do
12     tree ← get_dependencies(entry.url, declined)
13     if tree ∩ accepted ≠ ∅ then
14       accepted ← accepted ∪ tree
15       stale_entries ← stale_entries ∪ {entry}
16   return stale_entries
```

当且仅当条目的树与 accepted 相交时该条目过时；该树随后被折入 accepted，因此其上的每个过时模块都会在下一阶段失效。

**阶段 3：事务性重载。** 最后，引擎重载过时条目。它使 accepted 模块的缓存失效 ³，备份每个被移除的模块以支持回滚，然后按 url 重新导入每个过时条目的组件模块并换入全新 fiber：

**算法 9 事务性模块重载**

```
1  function reload(ctx, accepted, stale_entries)
2    backup ← invalidate_caches(accepted)
3    try
4      for entry in stale_entries do
5        entry.fiber.dispose()
6        entry.fiber ← ctx.use(import(entry.url), entry.config)
7    catch error
8      restore_caches(backup)
9      for entry in stale_entries do
10       entry.fiber.dispose()
11       entry.fiber ← ctx.use(backup[entry.url], entry.config)
12     throw error
```

事务性保证确保系统永不进入半重载状态：若任何模块导入失败（例如由于语法错误），缓存被恢复，每个过时条目从 backup[entry.url] 重建——即缓存刚被恢复的先前组件——撤销已经进行的换入。

> ³ 在 Node.js 上，这意味着同时清除 ES 模块与 CommonJS 模块系统的缓存，因为通过 ES 加载器导入的模块可能同时出现在两者中。

### 4.3 案例研究：Koishi

Koishi 是一个基于 Cordis 构建的开源聊天机器人应用框架 ⁴。经过四年多的开发，它积累了超过 4000 个社区贡献插件 ⁵，涵盖从即时通讯（IM）适配器、数据库驱动到管理控制台与终端用户功能。其规模与多样性使它成为 Cordis 动态可组合性在生产环境中的代表性验证。

**元框架的表现力与通用性。** Koishi 作为服务端机器人运行，其每项能力都作为第 4.1 节上下文原语之上的插件实现；Koishi 本身只贡献聊天机器人领域的词汇。同样的模型在一个完全不同的运行时中重现：Koishi 的 Web 控制台是第二个独立的 Cordis 应用，其插件组合的是浏览器及其用户界面的原语，而非服务端的原语。上述迥异的场景确立了第 3 节模型的两个性质。(1) 它**有表现力**：其原语足以承载一个完整的生产系统，宿主框架只提供领域词汇。(2) 它**通用**：它固定了效应与余效应如何组合，而把它们的意义留给每个应用，因此既不预设特定领域，也不预设特定运行时。

**无认知开销的时间可组合性。** 第 1.2.1 节考察的插件系统无法在不重启扩展宿主的情况下卸载单个扩展的效应。Koishi 常规性地执行此操作：编排者从控制台禁用一个插件，其效应就地被撤回；在开发期间，HMR 引擎在保存时重新应用被编辑的插件，同时保留系统其他位置的缓存状态与活动连接。Cordis 使这种移除不仅可能，而且对插件作者来说毫不费力。由于通过上下文执行的效应被追踪、其逆被自动组合（第 3.1 节），即使是没有经验的作者也能为插件的上下文中介效应获得有序清理，而无需编写卸载路径。这实现了第 1.2.1 节指出的缺失的关注局部性：原本依赖每位作者勤勉的正确性，改由抽象一次性承担。

**跨开放生态的空间可组合性。** 与第 1.2.1 节中插件间依赖基本缺位的插件系统相反，Koishi 的生态展现出真正的依赖拓扑：IM 适配器提供对各消息平台的访问，数据库驱动提供持久存储，功能插件将这些声明为余效应并访问它们。在运行时重新配置提供者，例如切换存储后端或重连适配器，只会重新激活解析依赖发生变化的依赖方（第 3.2 节）；依赖不可用的插件保持未激活直到它出现，而不会报错。案例研究所确证的是，这种组合跨越独立编写的代码成立：插件与其依赖通常由不同作者编写，他们除连接彼此的余效应之外不做任何协调，因此响应式余效应在独立贡献者的开放生态中保持了装配的一致性。

**有效性的威胁。** 这里的证据来自单一宿主语言中的单一生态，因此无法将范式本身的优点与它的 TypeScript 实现或 Koishi 特定领域的优点分开，而且它是观察性的而非与替代架构的受控比较。因此案例研究所确立的是一个存在性加采用性结果，而非定量结果；对照基线测量该抽象的开销及其对开发者生产力的影响仍是未来工作。

> ⁴ Koishi 目前使用 Cordis v3。本文介绍的是 Cordis v4，它精化了效应与余效应语义并重新设计了加载器；核心组合模型在两个版本间是共享的。
> ⁵ Koishi 用"插件"一词指代本文形式化为"组件"的概念。

---

## 5. 讨论

前几节呈现的形式模型与实现引入了一种面向动态可组合性的编程范式。本节考察该范式如何扩展到更广泛的工程关切，并讨论设计张力与开放问题。

### 5.1 服务复用（Service Multiplexing）

OSGi [44] 等动态组件平台围绕服务组织组合：服务是提供者在一个接口下发布、消费者绑定的能力单元。Cordis 的余效应模型呼应了这一概念，一个服务对应一个键背后的接口。提供服务（provide）的组件是其提供者，注入（inject）服务的组件是其消费者。单个服务可能由多个提供者实现，这种多重性可以两种形式实现。(1) **独占绑定**（exclusive binding）：多个实现共享一个接口，但同一时刻至多绑定一个；编排者选择哪个实现被绑定，在它们之间切换需要卸载一个提供者并加载另一个，瞬间扰动每个消费者的依赖。(2) **服务代理**（service broker）：一个作为接口入口点的中央服务被底层提供者与消费者共同注入，因此多个提供者共存，代理在它们之间分派每个请求。与独占绑定相比，代理吸收了这种扰动：更新底层提供者时代理保持原位，因此消费者看不到依赖变化，不触发任何重载。

服务代理支撑三项能力：负载均衡、滚动更新与跨进程调用。

**负载均衡。** 当多个提供者共存时，代理按可配置策略（如轮询、最少负载、延迟加权）或消费者点名的显式目标在它们之间分发请求。由于提供者是普通组件，可以添加或移除它们以伸缩容量；每个提供者通过可逆效应向代理登记，因此卸载它会撤销登记并自动将其从代理的路由集合中剔除。

**滚动更新。** 在运行时升级服务实现归结为受控的提供者转移 [45, 46]。为执行该转移，新提供者作为额外 fiber 加载并向代理登记；一旦它变为 ACTIVE，流量从旧提供者逐渐转移到新提供者（例如通过调整选择权重），旧提供者在不再承载进行中请求后被卸载。这种提供者转移把传统上属于基础设施层级的操作（例如容器编排、蓝绿部署）变成了应用层级的组合模式。

**跨进程调用。** 服务代理也可以跨越进程边界应用 [47]。每个进程托管自己的 Cordis 上下文与本地提供者；一个协调组件将它们链接起来，把每个都视为远程提供者。跨进程服务访问由保持接口的 RPC 机制中介，使分布对消费者透明。一个注意事项是跨进程调用会引入延迟，并可能中途失败，因此同步暴露它会阻塞调用者。打算跨进程暴露的接口必须针对异步契约设计。

### 5.2 访问控制与沙箱化

鉴于应用由独立组件组装而成，保护应用需要两种互补机制：(1) 约束组件可以访问哪些依赖，(2) 将不可信代码与宿主环境隔离。Cordis 通过依赖声明与拦截支持第一种；第二种需要外部隔离边界。

**基于能力的访问控制。** 依赖访问机制（第 4.1.4 节）已经构成一种对 Proxy 中介属性的访问控制形式：组件只能访问它声明的依赖；未声明的访问引发错误。这在结构上类似于基于能力的安全 [48–50]，其中权威由对引用的持有授予，而非由环境权威授予。inject 声明充当能力请求，上下文 Proxy 充当能力中介。由于这些请求是静态声明的，组件所需的全部 Proxy 中介能力在它运行之前就已知，让编排者可以在加载时审查与批准，而不是在访问发生时才发现。

这种中介通过拦截机制推广到细粒度策略。访问控制元数据可以由上下文携带或由组件声明（定义 18），提供者在依赖被调用时查询它以决定请求是否被允许。例如，文件系统依赖可以携带声明组件可读写哪些路径的元数据，提供者对每次调用检查元数据。由于这种拦截存在于上下文上而非任何一方的代码中，编排者可以调整它来约束任何组件对依赖的访问而无需修改提供者，例如授予社区组件只读数据库访问，而核心组件保有完全访问。此外，由于拦截只影响依赖如何被调用，而非它是否被满足，它可以在运行时安装、重新配置或移除，而不触发任何重载或扰动依赖图。

**不可信组件的沙箱化。** 当组件的代码不可信时，语言级访问控制是不够的，因为能访问宿主运行时的恶意组件可以直接触及底层对象，使此类检查形同虚设。真正的隔离要求超越语言级手段的**执行边界**，例如软件故障隔离（software fault isolation）[51]、隔离的语言运行时、沙箱进程或虚拟化容器 [52]。无论何种机制，不可信组件都在自己的隔离上下文中运行，并通过桥（bridge）触及宿主提供的依赖，这推广了第 5.1 节的跨进程调用：同样的透明性论证使这种桥接访问与本地注入无法区分。在宿主侧，桥是一个普通 fiber，其能力可以被上述访问控制衰减（attenuate）。

### 5.3 语言无关性与语言选择

尽管 Cordis 用 TypeScript 实现，上下文范式是语言无关的：时空可组合性仅由其两个可组合性维度定义，因此可以在任何沿着两个维度满足特定要求的语言中实现。我们逐一分析每个维度的要求。

**时间可组合性。** 在最基本层面，时间可组合性要求闭包（closure）：可逆效应将动作与逆配对，该逆必须作为值被捕获——连同它恢复的状态——以便在拆除时重放。除此之外，组件的代码与加载它的副作用必须在运行时可以被引入与撤回。

语言如何满足第二个要求取决于其执行模型。在托管运行时中，这采取可编程模块注册表的形式，已加载模块可以从注册表中驱逐，一旦无引用即被垃圾回收；例如 Node.js 就暴露这样的注册表。⁶ 原生代码不暴露模块注册表，因此引入与撤回采取显式动态链接与解链的形式（例如 Unix 上的 dlopen/dlclose，Windows 上的 LoadLibrary/FreeLibrary）[53]，即将目标代码加载进运行中的进程，之后再将其分离。WebAssembly 根据其嵌入者选择两条路径之一：在托管嵌入者（如 JavaScript 宿主）下，模块实例由宿主的收集器回收；在原生嵌入者丢弃它时释放（如 Wasmtime）。跨这些机制，可逆效应模型将加载视为上下文上的效应，其逆撤销模块引入的符号、类型或处理器的登记。

**空间可组合性。** 空间可组合性要求组件声明依赖的机制，以及运行时提供与注入这些依赖的机制。这归结为依赖注入（DI）问题 [39]，它表现在跨语言不同的两个层级：依赖如何被类型化，以及其访问如何被中介。

在类型层级，语言应提供让开发者表达良类型依赖访问的方式。消费者通过从上下文读取键来获得余效应，因此上下文类型（第 3.2.1 节）必须记录每个键的余效应。Typeclass（Haskell）[54] 与 trait（Rust）[55] 通过 instance 或 impl 让提供者从自己的模块扩展上下文类型来实现这一点 [56]。TypeScript 的模块增广（module augmentation）[57] 同样让提供者模块将声明合并进上下文类型。

在运行时层级，依赖访问必须被动态中介：键背后的余效应可能随提供者的加载与卸载而改变，并且可能在不同上下文中被不同地解析。因此语言需要一种透明地介入访问的方式，保持消费者代码不变，例如通过 JavaScript 的 Proxy 对象 [58] 或 Python 的描述符协议（__get__）[59]。缺少此类原语时，运行时反射 [60, 61] 可以动态地中介访问，代价是类型安全与开发者体验。

跨两个层级，元编程设施同时提供类型化与中介。注解 [62] 与装饰器（decorator）将元数据附加到声明，处理器将其展开为中介访问的访问器；编译期元编程（如 Rust 过程宏、Scala 宏 [63]、Zig comptime）为每个依赖发出带类型的声明连同此类访问器，从而免去通用拦截原语。

### 5.4 相互依赖与组件粒度

在响应式余效应模型中，依赖环只会使相关组件永久保持未激活：给定两个组件 𝐴 与 𝐵，若 𝐴 要求由 𝐵 提供的键，而 𝐵 要求由 𝐴 提供的键，则两者的满足谓词都永远不会为真。与并发系统中的死锁不同，这种状态是静态可预测的（仅凭依赖声明即可），且不产生运行时错误；它是静默的不激活，而非失败。

在实践中，大多数表面上的相互依赖都可以分解为消除环的细粒度组件。考虑两个组件：服务器（提供网络接口）与访问控制器（执行授权策略）。两个组件双向交互：访问控制器中介到达服务器的请求，服务器暴露修改访问控制策略的端点。整体式设计会让每个组件依赖另一个。然而，两个交互方向在逻辑上是独立的关切。将它们分解产生四个组件：server-core（服务器核心）、access-control-core（访问控制核心）、request-mediation（请求中介，依赖两个核心以对入站请求应用访问控制）与 policy-management（策略管理，依赖两个核心以通过服务器暴露策略修改）。通过这种方式，环被消除，因为两个核心互不依赖；只有整合组件依赖两者。

这种分解原则上总是可能的，因为每个双向交互都可以分解为独立的单向绑定，但它会增加组件数量：一般情况下，给定 𝑛 个相互交互的组件，整合组件的数量可能随 𝑛 二次增长，因为每对交互组件可能需要为每个交互方向各设一个组件。这不会影响正确性或运行时性能（组件是轻量的），而且更细的粒度可能是有益的：用户获得只加载他们需要的特定整合绑定的能力，实际上提高了系统的可组合性。然而，它可能影响开发者体验：更多组件需要更多配置、更多命名，以及理解依赖图的更多认知开销。

缓解这种粒度代价是工程关切而非理论问题。实际策略包括包打包（即将相关细粒度组件分组为单一可安装单元）、基于约定的接线（即自动连接名称或类型匹配模式的组件）与脚手架工具（即从声明式规格生成样板整合组件）。这些策略在保持无环模型形式保证的同时，将编写负担降低到接近整体式情形。

### 5.5 依赖类型化与版本化

在形式模型中，依赖链纯粹由键身份建立：提供键 𝑘 的组件满足任何在依赖集合中声明 𝑘 的组件。类型族 𝒱𝑘 保证单一编译单元内的类型级一致，但当组件被独立开发与构建时（组件生态中的常见场景），这一保证被打破。这种破坏导致两个不同的问题。

**接口漂移。** 提供者可能在版本之间修改与 𝑘 关联的接口（添加字段、改变方法签名、变更行为契约），而针对较早接口编译的消费者继续声明同一键 𝑘。依赖在余效应层级被满足（𝑘 ∈ dom(𝜎)），但运行时值不再符合消费者的预期，导致类型错误、方法未找到失败或静默的行为分歧 [64]。

**键冲突。** 两个独立开发的提供者可能使用相同的键名 𝑘 表示完全无关的接口。由于键身份单独建立链接，期望一个提供者接口的消费者会接受另一个的值，而没有任何兼容性检查。与接口漂移（提供者与消费者至少共享共同血统）不同，键冲突在预期类型与实际类型之间不存在任何关系，使由此产生的失败不可预测且难以诊断。

两个问题都指向同一个缺口：余效应模型只提供**名义链接**（按键名），而不提供版本化或结构化的链接（按接口兼容性）[65]。我们讨论弥补这一缺口的三种方法，从与基础设施耦合最紧到最语言无关。

**键命名空间化。** 将键空间从 𝐾 扩展到 𝐾×𝑃，其中 𝑃 标识定义接口的包，从构造上消除键冲突：具有相同本地名称的独立开发接口占据不同键。这是最直接的方案，但也是最耦合的：它将包命名空间嵌入形式模型本身，使系统的键身份依赖外部包注册表。

**对等依赖。** 更轻的耦合是通过宿主语言包管理器声明版本约束 [66]。这是 Cordis 目前采用的方法。组件依赖在语义上是对等依赖（peer dependency）：组件不在内部捆绑其依赖，而是期望运行时上下文供应它们。支持对等依赖的包管理器（如 npm）可以强制版本兼容：若提供某键的包版本落在消费者声明的对等范围之外，不兼容性在安装时即被发现，而非作为运行时失败浮现。然而，这种方法有两个局限：(1) 它依赖提供者忠实遵循语义化版本控制，这是一种无法强制执行的约定；(2) 包管理器通常将每个依赖解析为单一版本，这阻止了在一个应用内加载同一包多个版本的组件。

**结构化兼容。** 一种完全语言无关的方法会用兼容性谓词取代成员检查 𝑘 ∈ dom(𝜎)，该谓词验证提供者的实际接口在结构上涵盖消费者的预期。这类似于结构子类型 [67]：若提供的接口是所需接口的子类型，则提供者满足消费者。挑战在于语言无关地定义该谓词：结构化兼容对记录类型（宽度子类型）是直接的，但对行为契约（如前置/后置条件 [68]、效应规格 [21]）变得复杂，一旦参数多态引入有界量化 [69] 便不可判定。

这三种方法处理问题的不同方面。设计一种结合这些方法、同时保持余效应模型动态组合保证的统一依赖模型，仍然是一个开放问题。

---

## 6. 相关工作

动态可组合性与若干既有的研究领域相交。我们综述最相关的几条研究线索，并区分我们的贡献与每一条。

### 6.1 效应与余效应系统

第 2 节回顾了效应与余效应作为支撑我们工作的理论支柱。有三条线索在与 Cordis 相关的方向上扩展它们：将效应重新诠释为上下文必须提供的能力、为效应赋予可逆而非解释性语义，以及在单一分级体系下统一效应与余效应。

**作为能力的代数效应。** 代数效应（第 2.1 节）使效应运算对类型系统可见。与我们的工作最接近的扩展是 Brachthäuser 等人的 Effekt 语言，它将效应类型重新诠释为能力 [70, 71]：效应类型表达的是一段计算从其上下文**要求什么**，而非它可能产生哪些副作用。同样的重新诠释已进入主流实践：Effect-TS 库 [72] 在专用类型参数中记录效应的上下文需求，因此计算的类型指明其周围上下文必须提供哪些服务；与 Effekt 一样，它通过静态类型上的解释来释放效应，而非逆转它们。与我们的观点一样，这种视角将上下文视为能力的中介者。Cordis 与 Effekt 有两方面不同。在**目的**上，代数效应使效应可见是为了支持模块化解释——一个运算具有多种处理器语义；而 Cordis 使效应可见是为了支持追踪与逆转——为每个上下文变换配对逆。在**设定**上，Effekt 在类型层级静态地规范效应，默认基于作用域的推理，其中能力是二等的、被限制在词法作用域内，并通过装箱（boxing）恢复头等使用——装箱通过在类型中追踪捕获的能力来解除该限制；Cordis 则在运行时规范效应，目标是在组件移除时完全恢复资源。

**可逆效应语义。** 一条平行的线索为效应赋予可逆而非解释性的语义。Heunen 等人 [73] 通过将 Hughes 的箭头（arrow）改造为匕首箭头（dagger arrow）与逆箭头（inverse arrow）来在可逆设定下建模副作用，捕获诸如序列化与可变存储等运算承认逆的效应。这是与我们可逆效应最接近的形式化描述：两者都将每个效应与逆配对，而非通过处理器释放它。两者的区别在于可逆性存在于何处。Heunen 等人工作在指称的、范畴的设定中，可逆性是全局性质，由构造保证——因为每个计算都是可逆的，逆从范畴结构中恢复。Cordis 则在运行时追踪逆，不要求整个计算可逆，只要求每个原子效应承认逆，任何复合的逆都由组合推导（第 3.1 节）。

**分级类型作为统一的效应与余效应。** Orchard 等人 [74] 提出分级模态类型（graded modal types）作为涵盖效应推理（通过分级单子）与余效应推理（通过分级余单子）的总括概念，在 Granule 语言中实现，证明单一类型系统可以同时追踪计算做什么与需要什么；更近的工作将余效应扩展到命令式的类 Java 语言 [75, 76] 与按值压栈（call-by-push-value）[77]。所有这些都作用于类型层级：效应与余效应是在词法固定作用域上于编译期检查的静态标注。我们的贡献与此分析正交：我们将同样的两个概念提升为运行时机制，使 Cordis 能够处理动态组合。时间撤回与空间依赖在已加载组件集合演化时被重新解析，而不是在固定程序文本上一劳永逸地确定。

### 6.2 编程范式

第 3.4.2 节确立了上下文范式作为通过显式上下文中介效应与余效应的纪律。两个既有范式值得显式比较：一个共享我们的术语，另一个共享我们对横切关切的处理。

**面向上下文编程（COP）。** COP [78, 79] 为语言配备层（layer）——部分方法与类定义，根据执行上下文在运行时激活与停用，使行为适应而无需基代码指名其上下文依赖 [80]。COP 与 Cordis 在将上下文视为头等的、运行时可变实体，以及动态激活与停用行为方面一致，但这种相似是名义上的。在 COP 中，"上下文"指环境的执行情境（如位置、用户、模式），激活在动态作用域范围内改变方法分派；层既不追踪它引发的副作用也不逆转它们，激活也不受依赖满足的支配。在 Cordis 中，上下文是中介效应与余效应的 Γ∞ 实体：激活运行组件的可逆效应并由响应式余效应满足驱动（第 3.2 节），停用则完全逆转它们。COP 变化的是运行什么行为；Cordis 组合与逆转的是组件安装的效应与依赖。它们的区别在于取舍。COP 将激活折入宿主语言的方法分派，以语言特定性为代价获得动态作用域的层范围；而 Cordis 作为语言无关的叠加层，在共享上下文上响应式地解析激活。因此 Cordis 只能将 COP 的全局、值驱动片段表达为余效应：实现之间的上下文相关选择，而非动态作用域的激活。

**面向切面编程（AOP）。** AOP [81, 82] 将横切关切模块化为切面（aspect）：量化基础程序中所选连接点（join point）的切入点（pointcut），以及在每个连接点编织（weave）的增强（advice）。Cordis 处理同样的、否则将散布在各组件间的上下文行为问题，但其切面的对应物是余效应：一个许多组件声明依赖的共享中介点，横切行为可以在那里被重塑而无需编辑任何一个组件。随后两种范式在两个轴向上不同。(1) **声明 vs 无感知**：AOP 切入点是无感知的、量化的，匹配代码本身不知道被增强的任意连接点；而 Cordis 将横切限制在每个组件声明的余效应上，因此其触及范围恰好是声明的表面。这带来确定性与可追踪性：应用编排者可以在配置层检查与治理什么横切了一个组件，而无需阅读或分析其源码；而 AOP 关切只有通过量化它的切面才可读。(2) **生命周期整合**：Cordis 中的横切变化由组件的效应承载，在组件卸载时被逆转，并响应式地传播给其依赖方，因此它是动态组合模型内的一步操作；动态 AOP 系统 [83, 84] 也可以在运行时编织与解编，但作为独立操作，既不受组件生命周期约束，也不在被增强代码间触发重新解析。

### 6.3 时间可组合性

时间可组合性关注在运行程序中替换或移除组件，同时恢复它安装的效应。先前的方案根据它们如何处理离开组件的状态与效应来划分：将状态前向迁移给后继版本、通过开发者编写的清理恢复效应，或在预先固定的作用域内自动逆转效应。

**有状态前向迁移。** 一个广泛的系统家族通过跨版本前向携带状态来在运行程序中无停机地替换组件。它们都遵守同一时序纪律：组件只有在到达安全的、无交互的点之后才能被交换。Kramer 与 Magee 将该判据确立为静止（quiescence）[45]，Vandewoude 等人后来将其放宽为扰动更小的平静（tranquility）[46]；我们的滚动更新模式（第 5.1 节）通过在卸载提供者之前排空进行中请求来强制执行它。动态软件更新（DSU）随后通过手写变换函数前向迁移状态：Hicks 等人的通用 C 语言 DSU [85]、Stoyle 等人通过无冲突（con-freeness）分析实现的类型安全更新点 [86]，以及 Hayden 等人的 Kitsune [87]，都将旧版本数据映射到新版本表示，就地继承堆对象、打开的文件与连接，同时重新初始化任何未迁移的部分。同样的纪律扩展到持久状态：Overeem 等人 [88] 通过手写升级操作在保持系统可用的同时转换运行中事件存储的 schema 版本。Erlang/OTP [14] 在进程层级采取同样立场，通过 code_change/3 迁移状态，并通过重启受监督进程而非逆转其效应来从故障中恢复；JavaScript 的热模块替换（如 webpack [42]、Vite [43]）在模块层级做同样的事，通过 module.hot 或 import.meta.hot API 在重载间前向传递状态。与 Cordis 的模块替换（第 4.2 节）相比，这些方法更优雅地迁移内存状态：Cordis 逆转旧组件的被追踪效应，并从干净状态重新应用新组件的效应，因此组件自身的内存状态不会在重载后存活，除非被放入更长命的依赖中；在可逆效应之上叠加 DSU 式前向迁移是未来工作。尽管如此，Cordis 的方法在两方面更通用：它不需要 DSU 与 HMR 所需的那类手写迁移函数，并且它支持完全卸载组件并恢复其资源，而不仅仅是在原地更新一个。

**开发者编写的恢复。** 第二个家族通过开发者手工编写的清理或补偿逻辑恢复组件的效应。插件生命周期约定（如 OSGi [44]、Eclipse 扩展点、IntelliJ 与 VSCode）将清理委托给开发者编写的卸载回调；命令模式 [89] 将操作与撤销方法一起封装用于撤销/重做栈；saga 模型 [37] 将长生命周期事务结构化为每个步骤配对一个补偿动作；代数效应处理器可以附加在拆除时运行的终结器 [90]；事件溯源 [91] 通过追加补偿事件而非执行真正的逆来撤回状态。在所有这些中，逆都是未强制执行的义务，与操作解耦，因此遗忘一个逆会静默泄漏资源（如第 1.2.1 节经验记录的那样）。React 的 useEffect 钩子 [92] 最接近在结构上将效应与其逆配对，它返回运行时在每次重新执行前与卸载时调用的清理函数。它的短板是可组合性：钩子只能在组件或其他钩子的顶层调用，绝不能在条件、循环或嵌套函数内调用，且其效应体既不接受异步函数也不接受迭代器。因此效应无法由其他效应组装，也无法与控制流交错，没有可供推导复合逆的原料。Cordis 的效应没有此类限制：它们是自由组合的普通运算，可以异步运行，并且只为每个原子效应要求手写逆，任何复合的逆都由组合推导，因此组装既有效应根本无需编写逆。这种每个效应与其逆的结构性配对使完全恢复成为系统的不变式，而非开发者纪律问题。

**静态作用域的逆转。** 第三个家族自动逆转效应，由构造保证，但将逆转限制在预先固定的作用域内。软件事务内存 [93, 94]（源自硬件事务内存 [95]）记录读/写日志，使一组内存操作要么提交要么中止，将内存回滚到事务前状态。可逆计算从 Landauer 与 Bennett 的热力学分析 [96, 97] 到 Janus 等可逆语言 [98]，更进一步使整个计算的每一步全局可逆。线性类型 [99]、RAII [4] 与 Rust 的所有权系统 [55] 将资源的释放绑定到词法区域。每一种都在静态上固定逆转的作用域与触及范围；相比之下，Cordis 不预先固定任何此类作用域：它在组件生命周期内逆转任意的上下文操作，并将词法资源管理视为互补的，适用于单个组件内的局部资源。

### 6.4 空间可组合性

空间可组合性关注组件对其他组件的依赖如何被声明与绑定。先前的机制根据绑定如何响应变化来划分：在初始化时一次性接线依赖、响应整个组件的可用性，或按单个值的粒度传播变化。

**初始化时依赖接线。** 两种既有机制在初始化时把组件接在一起。依赖注入框架 [39]（如 Spring [100]、Guice、Angular、Inversify）在初始化时将依赖注入组件；UI 框架上下文（如 Vue.js 的 provide/inject 与 React 的 Context API）沿组件树传递它们。有些支持动态作用域（如 Spring 的 prototype/request 作用域、Angular 的分层注入器），但两者都不响应式地重新解析：当提供者在运行时被替换或撤回时，既有依赖方既不被停用也不被重新初始化，且都没有我们组件状态机提供的那类生命周期管理。Cordis 的响应式余效应（第 3.2 节）提供了这一点：每当满足谓词变化时，通知机制触发生命周期转移。

**可用性响应的组件模型。** 与我们响应式余效应最接近的先例响应服务可用性。OSGi 的声明式服务（Declarative Services）与 iPOJO [101, 102] 让组件声明提供与需求的服务，运行时随服务的出现与消失自动激活与停用它们；iPOJO 的 Gravity 项目 [102] 明确针对对变化服务可用性的自主运行时适应，其 provide/require 模型直接预示了 Cordis 的 ctx.provide/ctx.get 模式。R-OSGi [47] 通过 RPC 将同样的抽象透明地扩展到分布式设定，将网络失败映射为服务撤回事件——第 5.1 节将其作为 Cordis 模型的扩展讨论。所有这些系统都通过停用回调恢复，这有两个局限。其一，回调是手写的，因此资源安全依赖开发者纪律，遗忘一个会静默泄漏。其二，回调是同步的：若拆除需要与离开的依赖进行异步交换，这些框架不提供等待它的协议，迫使对可能已经过时的引用进行阻塞等待。Cordis 的响应式余效应弥合了这两个缺口：停用逆转依赖方累积的效应，其惯性 UNLOAD 状态（第 3.3.4 节）在进一步变化之前将异步拆除运行至完成。

**值级响应性。** 函数式响应式编程（FRP）[103] 及其现代化身（如 SolidJS 中的信号 [104, 105]、Vue 的响应式系统、Angular Signals）以值级粒度传播变化：当信号变化时，派生的计算在同步或调度器 [106] 下重新求值。Cordis 的响应式余效应以组件级粒度运作，增加了值级传播不建模的异步生命周期语义。两者是互补而非竞争的：Cordis 余效应本身可以携带响应式值，组件只在其实际消费的部分上更新，将组件级响应性精化为跨越两个层级的细粒度响应式余效应。

---

## 7. 结论

我们通过将效应与余效应这两个类型论概念提升为运行时机制，为动态可组合性呈现了一个形式化基础。**可逆效应**通过为上下文变换配备显式逆，并证明效应追踪保持组合运算，保证组件移除时状态的完全恢复，从而解决时间可组合性。**响应式余效应**通过形式化带类型的依赖上下文（含基于满足状态的通知、余效应隔离与余效应拦截），解决空间可组合性。一个具有针对各种控制流正交扩展的组件生命周期模型，将两种机制的交互操作化。综合起来，这些产生一个统一上下文类型，将效应上下文与余效应上下文整合为一个连贯的编程范式。我们将这一范式实现为 Cordis 元框架：核心库提供效应追踪与余效应解析，以及一个带配置调和与热模块替换的声明式组件加载器。Koishi 案例研究在拥有超过 4000 个社区插件的生产系统中验证了 Cordis 的设计。

在人工策展的插件生态之外，未来验证的一个引人注目的方向是自我进化的智能体执行框架（第 1.2.2 节），其中 AI 智能体在很少人工监督的情况下持续生成并替换自身的执行框架组件。在此类设定中应用 Cordis，将验证快速组件替换下完全恢复的时间保证，以及频繁拓扑变化下依赖协调的空间保证。此类验证将证明该范式作为智能体执行框架及其他自主系统中可恢复、可协调、持续自我进化基础的适用性。

---

## 参考文献

（按学术惯例，参考文献保留原文，不予翻译。）

[1] D. L. Parnas, "On the criteria to be used in decomposing systems into modules," *Communications of the ACM*, vol. 15, no. 12, pp. 1053–1058, 1972, doi: 10.1145/361598.361623.

[2] D. Birsan, "On Plug-ins and Extensible Architectures," *ACM Queue*, vol. 3, no. 2, pp. 40–46, 2005, doi: 10.1145/1053331.1053345.

[3] B. Burns, B. Grant, D. Oppenheimer, E. Brewer, and J. Wilkes, "Borg, Omega, and Kubernetes," *Communications of the ACM*, vol. 59, no. 5, pp. 50–57, 2016, doi: 10.1145/2890784.

[4] B. Stroustrup, *The Design and Evolution of C++*. Addison-Wesley, 1994.

[5] S. Marlow, S. Peyton Jones, A. Moran, and J. Reppy, "Asynchronous Exceptions in Haskell," in *Proceedings of the ACM SIGPLAN 2001 Conference on Programming Language Design and Implementation*, in PLDI '01. New York, NY, USA: Association for Computing Machinery, 2001, pp. 274–285. doi: 10.1145/378795.378858.

[6] C. Szyperski, *Component Software: Beyond Object-Oriented Programming*, 2nd ed. Addison-Wesley, 2002.

[7] R. Lopopolo, "Harness Engineering: Leveraging Codex in an Agent-First World." [Online]. Available: https://openai.com/index/harness-engineering/

[8] Anthropic, "Harness Design for Long-Running Application Development." [Online]. Available: https://www.anthropic.com/engineering/harness-design-long-running-apps

[9] L. Wang et al., "A Survey on Large Language Model Based Autonomous Agents," *Frontiers of Computer Science*, vol. 18, no. 6, p. 186345, 2024, doi: 10.1007/s11704-024-40231-1.

[10] Y. Qin et al., "Tool Learning with Foundation Models," *ACM Computing Surveys*, 2025, doi: 10.1145/3704435.

[11] C. Packer, V. Fang, S. G. Patil, K. Lin, S. Wooders, and J. E. Gonzalez, "MemGPT: Towards LLMs as Operating Systems," *CoRR*, 2023.

[12] T. Guo et al., "Large Language Model Based Multi-Agents: A Survey of Progress and Challenges," in *Proceedings of the Thirty-Third International Joint Conference on Artificial Intelligence*, in IJCAI 2024. 2024, pp. 8048–8057. doi: 10.24963/ijcai.2024/890.

[13] T. Cai, X. Wang, T. Ma, X. Chen, and D. Zhou, "Large Language Models as Tool Makers," in *Proceedings of the Twelfth International Conference on Learning Representations*, in ICLR 2024. 2024.

[14] J. Armstrong, "Making Reliable Distributed Systems in the Presence of Software Errors," Doctoral dissertation, 2003.

[15] E. Moggi, "Notions of computation and monads," *Information and Computation*, vol. 93, no. 1, pp. 55–92, 1991, doi: 10.1016/0890-5401(91)90052-4.

[16] G. Plotkin and J. Power, "Adequacy for Algebraic Effects," in *Foundations of Software Science and Computation Structures*, F. Honsell and M. Miculan, Eds., Berlin, Heidelberg: Springer Berlin Heidelberg, 2001, pp. 1–24.

[17] T. Petricek, D. Orchard, and A. Mycroft, "Coeffects: unified static analysis of context-dependence," in *Proceedings of the 40th International Conference on Automata, Languages, and Programming - Volume Part II*, in ICALP'13. Riga, Latvia: Springer-Verlag, 2013, pp. 385–397. doi: 10.1007/978-3-642-39212-2_35.

[18] M. Gaboardi, S.-y. Katsumata, D. Orchard, F. Breuvart, and T. Uustalu, "Combining effects and coeffects via grading," in *Proceedings of the 21st ACM SIGPLAN International Conference on Functional Programming*, in ICFP 2016. Nara, Japan: Association for Computing Machinery, 2016, pp. 476–489. doi: 10.1145/2951913.2951939.

[19] A. Church, "A Formulation of the Simple Theory of Types," *The Journal of Symbolic Logic*, vol. 5, no. 2, pp. 56–68, 1940, doi: 10.2307/2266170.

[20] B. C. Pierce, *Types and Programming Languages*. MIT Press, 2002.

[21] J. M. Lucassen and D. K. Gifford, "Polymorphic Effect Systems," in *Proceedings of the 15th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages*, in POPL '88. San Diego, California, USA: Association for Computing Machinery, 1988, pp. 47–57. doi: 10.1145/73560.73564.

[22] P. Wadler, "Monads for functional programming," in *Program Design Calculi*, M. Broy, Ed., Berlin, Heidelberg: Springer Berlin Heidelberg, 1993, pp. 233–264.

[23] G. Plotkin and J. Power, "Notions of Computation Determine Monads," in *Foundations of Software Science and Computation Structures*, Berlin, Heidelberg: Springer Berlin Heidelberg, 2002, pp. 342–356. doi: 10.1007/3-540-45931-6_24.

[24] G. Plotkin and M. Pretnar, "Handlers of Algebraic Effects," in *Programming Languages and Systems (ESOP)*, Berlin, Heidelberg: Springer Berlin Heidelberg, 2009, pp. 80–94. doi: 10.1007/978-3-642-00590-9_7.

[25] M. Pretnar, "An Introduction to Algebraic Effects and Handlers. Invited tutorial paper," *Electron. Notes Theor. Comput. Sci.*, vol. 319, no. C, pp. 19–35, Dec. 2015, doi: 10.1016/j.entcs.2015.12.003.

[26] D. Leijen, "Koka: Programming with Row Polymorphic Effect Types," *Electronic Proceedings in Theoretical Computer Science*, vol. 153, pp. 100–126, Jun. 2014, doi: 10.4204/eptcs.153.8.

[27] D. Leijen, "Type directed compilation of row-typed algebraic effects," in *Proceedings of the 44th ACM SIGPLAN Symposium on Principles of Programming Languages*, in POPL '17. Paris, France: Association for Computing Machinery, 2017, pp. 486–499. doi: 10.1145/3009837.3009872.

[28] A. Bauer and M. Pretnar, "Programming with algebraic effects and handlers," *Journal of Logical and Algebraic Methods in Programming*, vol. 84, no. 1, pp. 108–123, Jan. 2015, doi: 10.1016/j.jlamp.2014.02.001.

[29] K. Sivaramakrishnan et al., "Retrofitting parallelism onto OCaml," *Proc. ACM Program. Lang.*, vol. 4, no. ICFP, Aug. 2020, doi: 10.1145/3408995.

[30] T. Petricek, D. Orchard, and A. Mycroft, "Coeffects: a calculus of context-dependent computation," in *Proceedings of the 19th ACM SIGPLAN International Conference on Functional Programming*, in ICFP '14. Gothenburg, Sweden: Association for Computing Machinery, 2014, pp. 123–135. doi: 10.1145/2628136.2628160.

[31] T. Uustalu and V. Vene, "Comonadic Notions of Computation," *Electronic Notes in Theoretical Computer Science*, vol. 203, no. 5, pp. 263–284, 2008, doi: 10.1016/j.entcs.2008.05.029.

[32] A. Brunel, M. Gaboardi, D. Mazza, and S. Zdancewic, "A Core Quantitative Coeffect Calculus," in *Proceedings of the 23rd European Symposium on Programming Languages and Systems - Volume 8410*, Berlin, Heidelberg: Springer-Verlag, 2014, pp. 351–370. doi: 10.1007/978-3-642-54833-8_19.

[33] J. Reed and B. C. Pierce, "Distance makes the types grow stronger: a calculus for differential privacy," *SIGPLAN Not.*, vol. 45, no. 9, pp. 157–168, Sep. 2010, doi: 10.1145/1932681.1863568.

[34] M. Abadi, A. Banerjee, N. Heintze, and J. G. Riecke, "A core calculus of dependency," in *Proceedings of the 26th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages*, in POPL '99. San Antonio, Texas, USA: Association for Computing Machinery, 1999, pp. 147–160. doi: 10.1145/292540.292555.

[35] D. E. Denning, "A lattice model of secure information flow," *Commun. ACM*, vol. 19, no. 5, pp. 236–243, May 1976, doi: 10.1145/360051.360056.

[36] U. Dal Lago and F. Gavazzo, "A relational theory of effects and coeffects," *Proc. ACM Program. Lang.*, vol. 6, no. POPL, Jan. 2022, doi: 10.1145/3498692.

[37] H. Garcia-Molina and K. Salem, "Sagas," in *Proceedings of the 1987 ACM SIGMOD International Conference on Management of Data*, in SIGMOD '87. 1987, pp. 249–259. doi: 10.1145/38713.38742.

[38] R. Johnson and B. Foote, "Designing Reusable Classes," *Journal of Object-Oriented Programming*, vol. 1, pp. 22–35, 1988.

[39] M. Fowler, "Inversion of Control Containers and the Dependency Injection pattern." [Online]. Available: https://martinfowler.com/articles/injection.html

[40] A. M. Pitts and I. D. B. Stark, "Observable Properties of Higher Order Functions that Dynamically Create Local Names, or What's New?," in *Mathematical Foundations of Computer Science 1993 (MFCS 1993)*, in Lecture Notes in Computer Science, vol. 711. Springer, 1993, pp. 122–141. doi: 10.1007/3-540-57182-5_8.

[41] R. P. James and A. Sabry, "Yield: Mainstream Delimited Continuations," in *First International Workshop on the Theory and Practice of Delimited Continuations (TPDC 2011)*, 2011, pp. 20–32.

[42] webpack, "Hot Module Replacement." [Online]. Available: https://webpack.js.org/api/hot-module-replacement/

[43] Vite, "HMR API." [Online]. Available: https://vite.dev/guide/api-hmr

[44] OSGi Alliance, *OSGi Core Release 8*. OSGi Alliance, 2020.

[45] J. Kramer and J. Magee, "The Evolving Philosophers Problem: Dynamic Change Management," *IEEE Transactions on Software Engineering*, vol. 16, no. 11, pp. 1293–1306, 1990, doi: 10.1109/32.60317.

[46] Y. Vandewoude, P. Ebraert, Y. Berbers, and T. D'Hondt, "Tranquility: A Low Disruptive Alternative to Quiescence for Ensuring Safe Dynamic Updates," *IEEE Transactions on Software Engineering*, vol. 33, no. 12, pp. 856–868, 2007, doi: 10.1109/tse.2007.70733.

[47] J. S. Rellermeyer, G. Alonso, and T. Roscoe, "R-OSGi: Distributed Applications Through Software Modularization," in *Proceedings of the ACM/IFIP/USENIX 8th International Middleware Conference*, in Middleware '07. 2007, pp. 1–20. doi: 10.1007/978-3-540-76778-7_1.

[48] J. B. Dennis and E. C. Van Horn, "Programming Semantics for Multiprogrammed Computations," *Communications of the ACM*, vol. 9, no. 3, pp. 143–155, 1966.

[49] M. S. Miller, K.-P. Yee, and J. Shapiro, "Capability Myths Demolished," in *Technical Report SRL2003-02*, 2003.

[50] R. N. M. Watson, J. Anderson, B. Laurie, and K. Kennaway, "Capsicum: Practical Capabilities for UNIX," in *Proceedings of the 19th USENIX Security Symposium*, 2010, pp. 29–46.

[51] R. Wahbe, S. Lucco, T. E. Anderson, and S. L. Graham, "Efficient Software-Based Fault Isolation," in *Proceedings of the 14th ACM Symposium on Operating Systems Principles*, in SOSP '93. 1993, pp. 203–216. doi: 10.1145/168619.168635.

[52] A. Barth, A. P. Felt, P. Saxena, and A. Boodman, "Protecting Browsers from Extension Vulnerabilities," in *Proceedings of the 17th Annual Network and Distributed System Security Symposium*, in NDSS '10. 2010.

[53] W. W. Ho and R. A. Olsson, "An Approach to Genuine Dynamic Linking," *Software: Practice and Experience*, vol. 21, no. 4, pp. 375–390, 1991, doi: 10.1002/SPE.4380210404.

[54] P. Wadler and S. Blott, "How to Make Ad-hoc Polymorphism Less Ad Hoc," in *Proceedings of the 16th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages*, in POPL '89. 1989, pp. 60–76. doi: 10.1145/75277.75283.

[55] N. D. Matsakis and F. S. K. II, "The Rust Language and Type System," in *ACM SIGPLAN ML Family Workshop*, Gothenburg, Sweden, Sep. 2014.

[56] D. Dreyer, R. Harper, M. M. T. Chakravarty, and G. Keller, "Modular Type Classes," in *Proceedings of the 34th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages*, in POPL '07. 2007, pp. 63–70. doi: 10.1145/1190216.1190229.

[57] Microsoft, "Declaration Merging." [Online]. Available: https://www.typescriptlang.org/docs/handbook/declaration-merging.html

[58] T. Van Cutsem and M. S. Miller, "Proxies: Design Principles for Robust Object-oriented Intercession APIs," in *Proceedings of the 6th Symposium on Dynamic Languages*, in DLS '10. 2010, pp. 59–72. doi: 10.1145/1869631.1869638.

[59] R. Hettinger, "Descriptor HowTo Guide." [Online]. Available: https://docs.python.org/3/howto/descriptor.html

[60] P. Maes, "Concepts and Experiments in Computational Reflection," in *Conference on Object-Oriented Programming Systems, Languages, and Applications (OOPSLA)*, 1987, pp. 147–155. doi: 10.1145/38765.38821.

[61] G. Bracha and D. M. Ungar, "Mirrors: design principles for meta-level facilities of object-oriented programming languages," in *Proceedings of the 19th Annual ACM SIGPLAN Conference on Object-Oriented Programming, Systems, Languages, and Applications (OOPSLA)*, 2004, pp. 331–344. doi: 10.1145/1028976.1029004.

[62] R. Rouvoy and P. Merle, "Leveraging component-based software engineering with Fraclet," *Annals of Telecommunications*, vol. 64, no. 1–2, pp. 65–79, 2009, doi: 10.1007/s12243-008-0072-z.

[63] E. Burmako, "Scala Macros: Let Our Powers Combine!," in *Proceedings of the 4th Workshop on Scala*, in SCALA@ECOOP '13. 2013, pp. 3:1–3:10. doi: 10.1145/2489837.2489840.

[64] S. Raemaekers, A. van Deursen, and J. Visser, "Semantic Versioning and Impact of Breaking Changes in the Maven Repository," *Journal of Systems and Software*, vol. 129, pp. 140–158, 2017, doi: 10.1016/j.jss.2016.04.008.

[65] P. Lam, J. Dietrich, and D. J. Pearce, "Putting the Semantics into Semantic Versioning," in *Proceedings of the 2020 ACM SIGPLAN International Symposium on New Ideas, New Paradigms, and Reflections on Programming and Software*, in Onward! '20. 2020, pp. 157–179. doi: 10.1145/3426428.3426922.

[66] P. Abate, R. Di Cosmo, R. Treinen, and S. Zacchiroli, "Dependency Solving: A Separate Concern in Component Evolution Management," *Journal of Systems and Software*, vol. 85, no. 10, pp. 2228–2240, 2012, doi: 10.1016/j.jss.2012.02.018.

[67] L. Cardelli, "Structural Subtyping and the Notion of Power Type," in *Proceedings of the 15th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages*, in POPL '88. 1988, pp. 70–79. doi: 10.1145/73560.73566.

[68] B. Meyer, "Applying 'Design by Contract'," *Computer*, vol. 25, no. 10, pp. 40–51, 1992, doi: 10.1109/2.161279.

[69] B. C. Pierce, "Bounded Quantification is Undecidable," *Information and Computation*, vol. 112, no. 1, pp. 131–165, 1994, doi: 10.1006/inco.1994.1055.

[70] J. I. Brachthäuser, P. Schuster, and K. Ostermann, "Effects as capabilities: effect handlers and lightweight effect polymorphism," *Proc. ACM Program. Lang.*, vol. 4, no. OOPSLA, 2020, doi: 10.1145/3428194.

[71] J. I. Brachthäuser, P. Schuster, E. Lee, and A. Boruch-Gruszecki, "Effects, capabilities, and boxes: from scope-based reasoning to type-based reasoning and back," *Proc. ACM Program. Lang.*, vol. 6, no. OOPSLA1, 2022, doi: 10.1145/3527320.

[72] Effect, "Effect: A TypeScript library for building robust applications." [Online]. Available: https://effect.website/

[73] C. Heunen, R. Kaarsgaard, and M. Karvonen, "Reversible Effects as Inverse Arrows," in *Proceedings of the Thirty-Fourth Conference on the Mathematical Foundations of Programming Semantics (MFPS XXXIV)*, in Electronic Notes in Theoretical Computer Science, vol. 341. 2018, pp. 179–199. doi: 10.1016/j.entcs.2018.11.009.

[74] D. Orchard, V.-B. Liepelt, and H. Eades III, "Quantitative program reasoning with graded modal types," *Proc. ACM Program. Lang.*, vol. 3, no. ICFP, 2019, doi: 10.1145/3341714.

[75] R. Bianchini, F. Dagnino, P. Giannini, E. Zucca, and M. Servetto, "Coeffects for sharing and mutation," *Proc. ACM Program. Lang.*, vol. 6, no. OOPSLA2, Oct. 2022, doi: 10.1145/3563319.

[76] R. Bianchini, F. Dagnino, P. Giannini, and E. Zucca, "A Java-like calculus with heterogeneous coeffects," *Theoretical Computer Science*, vol. 971, p. 114063, 2023, doi: https://doi.org/10.1016/j.tcs.2023.114063.

[77] C. Torczon, E. Suárez Acevedo, S. Agrawal, J. Velez-Ginorio, and S. Weirich, "Effects and Coeffects in Call-by-Push-Value," *Proc. ACM Program. Lang.*, vol. 8, no. OOPSLA2, Oct. 2024, doi: 10.1145/3689750.

[78] R. Hirschfeld, P. Costanza, and O. Nierstrasz, "Context-oriented Programming," *Journal of Object Technology*, vol. 7, no. 3, pp. 125–151, 2008, doi: 10.5381/jot.2008.7.3.a4.

[79] P. Costanza and R. Hirschfeld, "Language constructs for context-oriented programming: an overview of ContextL," in *Proceedings of the 2005 Symposium on Dynamic Languages (DLS '05)*, ACM, 2005, pp. 1–10. doi: 10.1145/1146841.1146842.

[80] G. Salvaneschi, C. Ghezzi, and M. Pradella, "Context-oriented programming: A software engineering perspective," *Journal of Systems and Software*, vol. 85, no. 8, pp. 1801–1817, 2012, doi: 10.1016/j.jss.2012.03.024.

[81] G. Kiczales et al., "Aspect-Oriented Programming," in *ECOOP'97 — Object-Oriented Programming, 11th European Conference*, in Lecture Notes in Computer Science, vol. 1241. Springer, 1997, pp. 220–242. doi: 10.1007/BFb0053381.

[82] G. Kiczales, E. Hilsdale, J. Hugunin, M. Kersten, J. Palm, and W. G. Griswold, "An Overview of AspectJ," in *ECOOP 2001 — Object-Oriented Programming, 15th European Conference*, in Lecture Notes in Computer Science, vol. 2072. Springer, 2001, pp. 327–353. doi: 10.1007/3-540-45337-7_18.

[83] A. Popovici, T. Gross, and G. Alonso, "Dynamic Weaving for Aspect-Oriented Programming," in *Proceedings of the 1st International Conference on Aspect-Oriented Software Development (AOSD 2002)*, ACM, 2002, pp. 141–147. doi: 10.1145/508386.508404.

[84] J. Bonér, "What Are the Key Issues for Commercial AOP Use: How Does AspectWerkz Address Them?," in *Proceedings of the 3rd International Conference on Aspect-Oriented Software Development (AOSD 2004)*, ACM, 2004, pp. 5–6. doi: 10.1145/976270.976273.

[85] M. Hicks, J. T. Moore, and S. Nettles, "Dynamic Software Updating," in *Proceedings of the ACM SIGPLAN 2001 Conference on Programming Language Design and Implementation*, in PLDI '01. 2001, pp. 13–23. doi: 10.1145/378795.378798.

[86] G. Stoyle, M. Hicks, G. Bierman, P. Sewell, and I. Neamtiu, "Mutatis Mutandis: Safe and Predictable Dynamic Software Updating," in *Proceedings of the 32nd ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages*, in POPL '05. 2005, pp. 183–194. doi: 10.1145/1040305.1040321.

[87] C. M. Hayden, K. Saur, E. K. Smith, and M. Hicks, "Kitsune: Efficient, General-Purpose Dynamic Software Updating for C," *ACM Trans. Program. Lang. Syst.*, vol. 36, no. 4, 2014, doi: 10.1145/2629460.

[88] M. Overeem, M. Spoor, and S. Jansen, "The Dark Side of Event Sourcing: Managing Data Conversion," in *IEEE 24th International Conference on Software Analysis, Evolution and Reengineering*, in SANER '17. 2017, pp. 193–204. doi: 10.1109/SANER.2017.7884621.

[89] E. Gamma, R. Helm, R. Johnson, and J. Vlissides, *Design Patterns: Elements of Reusable Object-Oriented Software*. Boston, MA: Addison-Wesley, 1994.

[90] D. Leijen, "Algebraic Effect Handlers with Resources and Deep Finalization," technical report MSR-TR-2018-10, Apr. 2018.

[91] M. Fowler, "Event Sourcing." 2005.

[92] J. Lee, J. Ahn, and K. Yi, "React-tRace: A Semantics for Understanding React Hooks," *Proc. ACM Program. Lang.*, vol. 9, no. OOPSLA2, pp. 471–498, 2025, doi: 10.1145/3763067.

[93] N. Shavit and D. Touitou, "Software Transactional Memory," in *Proceedings of the Fourteenth Annual ACM Symposium on Principles of Distributed Computing*, in PODC '95. 1995, pp. 204–213. doi: 10.1145/224964.224987.

[94] T. Harris, S. Marlow, S. Peyton Jones, and M. Herlihy, "Composable Memory Transactions," in *Proceedings of the Tenth ACM SIGPLAN Symposium on Principles and Practice of Parallel Programming*, in PPoPP '05. 2005, pp. 48–60. doi: 10.1145/1065944.1065952.

[95] M. Herlihy and J. E. B. Moss, "Transactional Memory: Architectural Support for Lock-Free Data Structures," in *Proceedings of the 20th Annual International Symposium on Computer Architecture*, in ISCA '93. 1993, pp. 289–300. doi: 10.1145/165123.165164.

[96] R. Landauer, "Irreversibility and Heat Generation in the Computing Process," *IBM Journal of Research and Development*, vol. 5, no. 3, pp. 183–191, 1961, doi: 10.1147/rd.53.0183.

[97] C. H. Bennett, "Logical Reversibility of Computation," *IBM Journal of Research and Development*, vol. 17, no. 6, pp. 525–532, 1973, doi: 10.1147/rd.176.0525.

[98] T. Yokoyama and R. Glück, "A Reversible Programming Language and its Invertible Self-Interpreter," in *Proceedings of the 2007 ACM SIGPLAN Workshop on Partial Evaluation and Semantics-Based Program Manipulation*, in PEPM '07. 2007, pp. 144–153. doi: 10.1145/1244381.1244404.

[99] P. Wadler, "Linear Types Can Change the World!," 1993.

[100] C. Walls, *Spring in Action*, 6th ed. Manning Publications, 2022.

[101] C. Escoffier, R. S. Hall, and P. Lalanda, "iPOJO: an Extensible Service-Oriented Component Framework," in *IEEE International Conference on Services Computing*, 2007, pp. 474–481. doi: 10.1109/SCC.2007.74.

[102] H. Cervantes and R. S. Hall, "Autonomous Adaptation to Dynamic Availability Using a Service-Oriented Component Model," in *Proceedings of the 26th International Conference on Software Engineering*, in ICSE '04. 2004, pp. 614–623. doi: 10.1109/ICSE.2004.1317483.

[103] C. Elliott and P. Hudak, "Functional Reactive Animation," in *Proceedings of the Second ACM SIGPLAN International Conference on Functional Programming*, in ICFP '97. 1997, pp. 263–273. doi: 10.1145/258948.258973.

[104] G. H. Cooper and S. Krishnamurthi, "Embedding Dynamic Dataflow in a Call-by-Value Language," in *Programming Languages and Systems (ESOP 2006)*, in Lecture Notes in Computer Science, vol. 3924. Springer, 2006, pp. 294–308. doi: 10.1007/11693024_20.

[105] I. Maier and M. Odersky, "Deprecating the Observer Pattern with Scala.React," technical report EPFL-REPORT-176887, 2012. [Online]. Available: https://infoscience.epfl.ch/record/176887

[106] E. Bainomugisha, A. L. Carreton, T. Van Cutsem, W. De Meuter, and others, "A Survey on Reactive Programming," *ACM Comput. Surv.*, vol. 45, no. 4, 2013, doi: 10.1145/2501654.2501666.

---

## 附录：术语对照表（译者注）

| 英文 | 中文 | 说明 |
|---|---|---|
| composability | 可组合性 | 组合（composition）的能力性质 |
| temporal composability | 时间可组合性 | 侧重"时间维度"的卸载可逆性 |
| spatial composability | 空间可组合性 | 侧重"空间维度"的依赖管理 |
| effect / coeffect | 效应 / 余效应 | 沿用类型论惯例译法（对应 monad / comonad 单子/余单子） |
| revertible effects | 可逆效应 | 每个上下文变换配备显式逆 |
| reactive coeffects | 响应式余效应 | 依赖满足状态变化触发激活/停用 |
| agent harness | 智能体执行框架 | 亦可译"智能体运行框架" |
| component / fiber | 组件 / fiber（保留原文） | fiber 为避免与"纤程/协程"混淆，保留原文 |
| lifecycle | 生命周期 | 状态机：INACTIVE / RELOAD / ACTIVE / UNLOAD |
| epoch | 纪元 | 依赖配置版本化机制 |
| inertial state | 惯性状态 | 转移开始后运行至完成 |
| isolation realm | 隔离域 | 键→域标识符的间接层 |
| interception | 拦截 | 对依赖访问的横切元数据 |
| component loader | 组件加载器 | 声明式配置层 + HMR |
| configuration reconciliation | 配置调和 | 对配置变更的最小扰动应用 |
| hot module replacement (HMR) | 热模块替换 | 开发期模块就地替换 |
| quiescence / tranquility | 静止 / 平静 | 动态更新的安全点判据 |
| service broker | 服务代理 | 多提供者共存的中央接口入口 |
| coeffect specification | 余效应规格 | 组件声明的依赖集合 |
| capability | 能力 | 基于能力的安全模型 |
| graded (co)effects | 分级（余）效应 | 以预序半环为代数 |
| context paradigm | 上下文范式 | 通过显式上下文中介效应与余效应 |

---

*译文完。原论文共 48 页（含 4 幅示意图、1 张表、9 个算法与 106 条参考文献）。数学公式与代码保留原文形式；示意图以文字化图示再现；参考文献按学术惯例保留原文。*
