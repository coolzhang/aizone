# 时空可组合性的编程范式

Yifan Shi¹,², Wei Zhang¹, Tianyi Cui²

¹北京大学 ²DeepSeek-AI

## 摘要

现代软件——从插件系统到自我进化的智能体运行框架（agent harness）——越来越需要**动态组合**（dynamic composition），然而其形式化基础仍未得到充分发展。我们识别出该问题的两个正交维度：**时间可组合性**（temporal composability），即在组件被移除时能够完整逆转其副作用的能力；以及**空间可组合性**（spatial composability），即声明并以响应式方式管理组件间依赖的能力。我们分别将经典的效应（effect）与余效应（coeffect）概念提升为运行时机制，从而处理这两个维度。我们形式化了**可逆效应**（revertible effects），其中每次上下文变换都配备一个显式的逆变换，并证明效应追踪保持组合运算，从而保证组件移除时状态的完整恢复。我们形式化了**响应式余效应**（reactive coeffects），其中依赖被注册在一个带类型的上下文中，满足性的变化会触发组件的自动激活与去激活。一个组件生命周期模型将可逆效应与响应式余效应如何与多样化的控制流交互操作化，从而产生一个统一的上下文类型，把效应上下文与余效应上下文整合为一个自洽的编程范式。我们在 **Cordis** 中实现了这些思想——Cordis 是一个时空可组合性的元框架（meta-framework），它提供了一个带有效应追踪与余效应解析的核心库，以及一个带有配置对账（configuration reconciliation）与热模块替换（hot module replacement）的声明式组件加载器。

---

## 目录

1. 引言
   1.1. 可组合性的维度
   1.2. 动机示例
   1.2.1. 插件系统
   1.2.2. 自我进化的智能体运行框架
   1.2.3. 粗粒度的权宜之计
   1.3. 贡献
2. 预备知识
   2.1. 效应
   2.2. 余效应
   2.3. 与动态可组合性的关系
3. 可逆效应与响应式余效应
   3.1. 可逆效应
   3.1.1. 效应上下文
   3.1.2. 可逆效应函数
   3.2. 响应式余效应
   3.2.1. 余效应上下文
   3.2.2. 规范与通知
   3.2.3. 隔离与拦截
   3.3. 组件生命周期
   3.3.1. 幂等恢复
   3.3.2. 迭代
   3.3.3. 一致性
   3.3.4. 异步
   3.4. 上下文范式
   3.4.1. 统一上下文
   3.4.2. 定位上下文范式
4. 实现与案例研究
   4.1. 核心库
   4.1.1. 效应追踪
   4.1.2. 余效应操作
   4.1.3. 组件生命周期
   4.1.4. 上下文访问
   4.2. 组件加载器
   4.2.1. 声明式配置层
   4.2.2. 热模块替换
   4.3. 案例研究：Koishi
5. 讨论
   5.1. 服务多路复用
   5.2. 访问控制与沙箱化
   5.3. 语言无关性与语言选择
   5.4. 相互依赖与组件粒度
   5.5. 依赖的类型化与版本化
6. 相关工作
   6.1. 效应与余效应系统
   6.2. 编程范式
   6.3. 时间可组合性
   6.4. 空间可组合性
7. 结论
参考文献

---

## 1. 引言

组合（composition）——将复杂系统由更简单的部件组装起来——是软件工程的一项基本原则 [1]。传统上，组合是**静态的**：函数调用、模块导入和类继承在编译期被解析，并在整个执行过程中保持不变。然而，现代软件越来越需要**动态组合**，即组件在运行时被加载、卸载和重新配置。插件架构 [2] 与自我进化的智能体运行框架都要求系统能够在运行中安全地增删功能，但当前的实践退而求其次地采用**粗粒度**机制 [3]，只能通过重启来重新配置，从而丢弃运行时状态。尽管动态组合的实践重要性日益增长，与静态组合所拥有的丰富形式化框架相比，其理论基础仍未得到充分发展。

### 1.1. 可组合性的维度

为了刻画动态组合的需求，我们在已被充分研究的组合代数性质之外，识别出两个正交的维度：

- **时间可组合性**处理时间维度：当组件被移除时，它对其享环境所做的修改必须被完整且安全地逆转。这要求追踪组件所执行的每一次资源分配、事件注册与状态变更，并保证在移除时它们被有序地回收。
- **空间可组合性**处理空间维度：组件必须能够以结构化且可验证的方式声明、发现并解析它们彼此之间的依赖。这要求管理依赖拓扑，并协调组件生命周期以响应依赖的变化。

在静态场景下，时间可组合性退化为词法作用域（例如 RAII [4]、bracket 模式 [5]），空间可组合性退化为模块导入解析。而在动态场景下，组件在运行时到来与离去，这两个维度都变得显著更难：时间可组合性必须处理作用域不受词法边界约束的长生命周期、有状态的效应；空间可组合性必须处理在执行过程中出现、消失或改变身份的依赖。

### 1.2. 动机示例

#### 1.2.1. 插件系统

插件系统是动态组合的典型实例。我们以 Visual Studio Code（VSCode）——使用最广泛的可扩展 IDE 之一——作为代表性示例。

**时间维度的局限。** VSCode 在一个被称为"扩展宿主"（extension host）的共享进程中运行所有扩展。尽管扩展可以动态安装，但该宿主并不提供在运行时卸载单个扩展代码的机制。一旦某个扩展的 `activate` 函数执行完毕，禁用它或卸载它就需要重启整个宿主，从而影响到所有已加载的扩展。纯声明式的扩展（如主题、键位绑定、代码片段）不携带任何代码，可以自由移除。然而，在按安装量排名的前 100 个扩展中，有 87 个包含可执行代码¹，因此它们在移除时都需要这样的重启。尽管 VSCode 提供了 `deactivate` 钩子，但它仅在宿主进程已经终止时作为优雅关闭的回调，因而无法实现运行中的移除。此外，该钩子把效应的清理与效应的创建（在 `activate` 中）分离开来，违反了关注的局部性（locality of concern），使得完整的清理难以验证。

**空间维度的局限。** VSCode 确实提供了 `extensionDependencies` 来声明扩展之间的依赖，但它几乎无人使用：在按安装量排名的前 100 个扩展中，只有 7 个对非内置扩展声明了 `extensionDependencies`¹。这种稀缺性反映了扩展 API 的形态：它暴露的是固定的、表层级的扩展点，如命令、视图和语言特性。扩展通过这些扩展点对宿主做贡献，而不是彼此依赖，因此扩展之间的依赖很少出现。此外，VSCode 的扩展间交互机制并不提供结构化的契约：它通过 `vscode.extensions.getExtension(...).exports` 把某个扩展的功能暴露给其他扩展，但返回值是无类型的（默认为 `any`），因此依赖方无法依赖一个经过检查的接口。简言之，VSCode 将扩展导向一组固定的、由宿主提供的扩展点，而没有为它们提供安全、结构化的互相依赖方式。

这两点局限并非 VSCode 独有；它们在各种插件系统中普遍存在 [2, 6]，只是程度不同。

#### 1.2.2. 自我进化的智能体运行框架

现代 AI 智能体依赖于运行时智能体运行框架 [7–9]。这些系统可以组合多样的工具套件 [10] 与执行环境、治理权限与沙箱、维护会话状态与持久化、提供上下文管理与记忆系统 [11]、编排子智能体与多智能体工作流 [12]，并向用户与自动化暴露接口。未来的运行框架可能会在持续服务请求的同时，生成并部署对其自身组件的修改。由模型合成的可复用工具提供了组件级自我修改的一个更窄的前身 [13]。每一次这样的修改本身都是动态组合的一个实例。

由于这些修改持续发生，且只有有限乃至没有人工监督，动态可组合性变得不可或缺。没有时间可组合性，每一次自我修改都会强制一次完整重启，丢弃所有进程本地的累积状态；在这种频率下，累积的不可用时间将十分可观，在途任务会被反复打断；更糟的是，一次错误的自我修改可能恰恰禁用了恢复所需的那个进程。没有空间可组合性，每个模块都必须自行检测并适应它所依赖模块的出现、消失或身份变化，而且只能通过临时手段做到；更糟的是，一种朴素的代码替换策略可能会悄悄破坏依赖方，或引入只在重载时才暴露的循环依赖。

#### 1.2.3. 粗粒度的权宜之计

动态可组合性之所以只获得有限的形式化关注，原因之一是操作系统与容器编排器已经提供了一个粗粒度的替代品。进程终止与重启在"整个地址空间"的粒度上提供了时间可组合性；容器编排 [3] 在"整个服务"的粒度上提供了空间可组合性。实践中，大多数软件通过诉诸这些粗粒度机制来容忍细粒度可组合性的缺失：行为异常的模块通过重启进程来处理，服务依赖由编排器来管理。

然而，这种权宜之计带来了巨大的代价。在时间维度上，每次重启都会丢弃所有进程本地的累积状态（例如缓存、连接、部分计算），重建它们需要数秒到数分钟 [14]；在此期间维持可用性需要冗余副本，从而为"无法恢复单个组件"的能力缺陷付出资源开销。在空间维度上，容器级的编排无法表达共享同一地址空间的组件之间的依赖，并且为那些本可以是本地函数调用的交互引入了网络开销。两种机制都工作在进程与容器的边界上，而现代系统却越来越多地在更细的层次上进行组合。这种粒度错配要求一种组合抽象，在与组件本身相同的层次上管理效应与依赖。

### 1.3. 贡献

动态可组合性的两个维度分别关涉计算**如何修改**其环境以及**如何依赖**其环境。这两个方向正是效应系统 [15, 16] 与余效应系统 [17, 18] 所形式化的内容：效应提供了对"环境修改"进行推理的形式化词汇，余效应提供了对"环境需求"进行推理的形式化词汇。然而，现有的表述把推理局限于对词法上固定的作用域进行编译期分析，并未扩展到组件在运行时到来与离去的动态场景。通过把效应提升为可逆的运行时模型、把余效应提升为响应式的依赖解析机制，我们获得了动态可组合性的统一形式化基础——它语言无关，可适用于任何需要动态组合的软件架构。我们做出如下贡献：

1. 我们形式化了**可逆效应**（第 3.1 节）：一个模型，其中每次上下文变换都与一个显式的逆函数配对，从而产生其追踪与恢复均为保持组合的运算的效应函数。这为动态时间可组合性提供了代数基础。
2. 我们形式化了**响应式余效应**（第 3.2 节）：一个带类型的依赖上下文，其中组件将其需求声明为一个依赖集合，而一个基于满足性的通知机制自动把状态转移分类为"激活""去激活"或"中性"。这为动态空间可组合性提供了代数基础。
3. 我们建立了一个**组件生命周期模型**（第 3.3 节），将可逆效应与响应式余效应如何与多样化的控制流交互操作化，并把效应上下文与余效应上下文整合为一个**统一的上下文类型**（第 3.4 节），从而构成一个面向动态可组合系统的编程范式。
4. 我们在 **Cordis**（第 4 节）中实现了这些思想——Cordis 是一个时空可组合性的元框架，它提供了一个实现该形式化模型的核心库（含效应追踪与余效应解析），以及一个带有配置对账与热模块替换的声明式组件加载器。我们通过对 Koishi 聊天机器人平台（一个拥有 4000 多个社区插件、处于生产使用中的生态系统）的案例研究验证了该设计。

---

## 2. 预备知识

本节简要概述效应与余效应系统——支撑我们工作的两大理论支柱。我们假设读者熟悉基本的类型论与范畴论；这里的目的是固定记号并引入第 3 节将要操作化为运行时机制的关键抽象。

### 2.1. 效应

在简单类型 lambda 演算（STLC）[19, 20] 中，类型判定 Γ ⊢ t : T 表示项 t 在上下文 Γ 下具有类型 T。效应系统细化该类型，以记录某计算可能产生的副作用，从而产生如下形式的判定：

    Γ ⊢ t : T|effect        (1)

这里，结果类型被附注上效应代数（effect algebra）中的一个元素，记录该计算可能产生的副作用，从而能够对带状态的计算进行组合式推理。这一方法源于 Lucassen 与 Gifford [21]，他们引入了一种带种类的类型系统，区分类型、效应与区域，以发现并行程序中的调度约束。

**单子效应。** Moggi [15] 首次通过单子（monad）范畴化地建模了计算效应；Wadler [22] 在 Haskell 中推广了这一方法。范畴 𝒞 上的单子 (T, η, μ) 把一个有效应的计算封装为类型 T(A) 的值，其中 η : A → T(A) 提升纯值，μ : T(T(A)) → T(A) 对嵌套计算进行序列化。经典实例包括 Maybe 单子（偏性）、State 单子（可变状态）和 IO 单子（外部交互）。

**代数效应。** Plotkin 与 Power [16, 23] 证明代数操作（algebraic operations）可以确定单子，从而建立了一个效应接口与其实现解耦的框架。一个效应签名 Σ 声明一组操作（例如对于状态而言的 get : () → S、put : S → ()）；程序可以自由调用这些操作，而无需承诺某种特定的解释。Plotkin 与 Pretnar [24] 随后引入了效应处理器（effect handlers），通过提供续延语义来解释操作：

    handle e with { op(v, κ) ↦ … }    (2)

处理器接收操作参数 v 与定界续延 κ，并可以零次、一次或多次调用它，从而在统一框架内实现异常、协程与非确定性 [25]。诸如 Koka [26, 27]、Eff [28] 和 OCaml 5 [29] 等语言已以各不相同的设计权衡采纳了代数效应。

### 2.2. 余效应

与效应相对偶，余效应系统 [17, 30] 丰富的是**上下文**而非类型，从而产生如下形式的判定：

    Γ|coeffect ⊢ t : T        (3)

这里，上下文被附注上余效应代数中的一个元素，描述该计算对其环境的要求，例如要访问的资源、要持有的能力，或要依赖的服务。效应建模的是程序对世界的影响，而余效应建模的是世界对程序的约束。

**余单子余效应。** 使用余单子（comonad）来结构化上下文相关计算的思想最早由 Uustalu 与 Vene [31] 提出，他们提出了对称（半）幺半余单子，作为 Moggi 效应单子框架的对偶，用以刻画诸如数据流与属性求值等概念。Petricek 等人 [17] 在此基础上提出余效应，作为上下文相关性的统一静态分析。一个余单子 (D, ε, δ) 刻画上下文相关计算：ε : D(A) → A 从上下文中提取当前值，δ : D(A) → D(D(A)) 为嵌套访问复制上下文。Environment 余单子 D(X) = E × X 建模对固定环境 E 的依赖；Stream 余单子 D(X) = ℕ → X 建模对时序数据的依赖。

**分级余效应。** 为了更细粒度的追踪，分级余效应系统使用一个预序半环 𝒮 = (S, ≤, +, ×, 0, 1) 作为余效应代数 [32]，这一做法后来由 Gaboardi 等人 [18] 与分级效应统一起来。S 的元素为每个变量绑定附注其用量：0 表示未使用，1 表示线性使用，n 表示有界使用，∞ 表示无限制使用。半环运算按序（×）与并行（+）地组合余效应，从而在统一的代数框架 [36] 内实现精确的资源追踪、敏感性分析 [33] 与信息流控制 [34, 35]。

### 2.3. 与动态可组合性的关系

效应与余效应系统沿着两个互补的方向组织对计算的推理：效应描述计算如何修改其环境，余效应描述计算如何依赖其环境。这两个方向正对应于第 1 节所识别的动态可组合性的两个维度：

- **时间可组合性**要求组件对共享环境的修改在卸载时可逆。相关的效应是那些**有状态的**效应，它们持久地变换该环境；逆转这种变换要求它允许存在逆。
- **空间可组合性**要求组件间的依赖被声明并以响应式方式管理。这种依赖正是余效应所刻画的东西，而管理它们就相当于把每个依赖对环境的供给进行解析。

然而，经典的效应与余效应系统是**静态**的工具：效应在词法上固定的作用域内被追踪，并由编译期处理器消解；余效应标注则针对执行前就确定下来的上下文进行验证。相比之下，动态组合要求这些保证对运行时到来与离去的组件成立，并且是针对持续演化的上下文而言。任何固定的词法作用域都无法界定一个部署后才加载的插件；任何编译期上下文都无法预知由运行时配置涌现出来的依赖。

这促使我们转变视角：与其给静态类型系统增加更多标注，不如把效应与余效应的概念结构**具体化（reify）**，使运行时能够直接操作它们，从而动态地建立起这些系统在静态下所提供的保证。第 3 节展开这一构造。

---

## 3. 可逆效应与响应式余效应

本节把第 2 节引入的效应与余效应概念提升为运行时机制，构建一个动态组合理论。核心思想是把携带效应与余效应的**类型上下文**转化为**上下文类型**（context types）——即可在运行时操作的类型，把上下文具体化为一个一等实体。对于效应类型，我们把它建模为配备逆变换的上下文变换，从而实现动态时间可组合性。对于余效应上下文，我们把它建模为携带依赖信息的类型，从而实现动态空间可组合性。两种机制与不同控制流的交互产生了一个组件生命周期模型，而同时携带效应与余效应的统一上下文本身就构成一个编程范式。

### 3.1. 可逆效应

时间可组合性是指在运行时加载与卸载组件的能力，使得卸载时共享环境被恢复到组合之前的状态。这要求组件对环境所做的每一次修改都既可追踪又可逆转。因此，我们把效应建模为类型 Γ → Γ × (Γ → Γ) 的函数：作用于当前上下文，它产生修改后的上下文以及一个显式的逆。逆使效应可逆转，而把它返回给运行时则使效应可追踪。我们称这样的效应为**可逆的**（revertible）：通过在执行过程中记录并组合这些逆，完整的环境恢复就成为一种结构性保证。

#### 3.1.1. 效应上下文

给定任意非纯函数 f_impure : X → Y，我们把它变换为纯形式 f : Γ × X → Γ × Y，其中 Γ 是上下文，所有可能的副作用都可以表示为对 Γ 的变换。对于任意固定的输入 x : X，诱导出的映射 γ ↦ pr₁(f(γ, x)) 独立于返回值地刻画了 f 的副作用。作为第一近似，假设每个效应的逆先验可得；那么 Γ 上的可允许效应在复合运算 ∘ 下构成一个子群 𝔉Γ ≤ Sym(Γ)，其公理对应于系统的结构性需求：

- **封闭性**保证效应的顺序复合仍然是可允许的；
- **单位元**（idΓ，Γ 上的恒等函数）充当复合的单位元；
- **逆元**要求每个效应 f ∈ 𝔉Γ 都允许一个逆 f⁻¹ ∈ 𝔉Γ，保证任何效应的复合都可以被撤销。

逆元需求由一个作用域限制所证成：Γ 只建模系统的**内部**状态，即系统对其保有完全控制的状态。系统对其自身上下文所做的每一次修改都是可逆的，因为先前的值总能被恢复。针对系统边界之外实体（网络请求、文件 I/O）的操作不修改 Γ；从 𝔉Γ 的角度看，它们相当于 idΓ。外部交互由领域特定的策略（例如补偿事务 [37]）处理，超出了本模型的范围。

为了在上下文内部记录效应，我们引入如下核心定义：

**定义 1.** 给定上下文 Γ，定义其**效应上下文**（effect context）为：

    ∂Γ ≔ Γ × 𝔉Γ    (4)

它可以理解为一个二元组 (γ, φ)，其中：

- γ ∈ Γ 表示当前的上下文状态；
- φ ∈ 𝔉Γ 是把上下文恢复到其初始状态的变换。

特别地，初始状态可以表示为 (γ₀, idΓ)。

鉴于上述 φ 的存在，在 ∂Γ 上执行的所有效应都可以被自动追踪并完整恢复。下面给出具体构造。

**定义 2.** 定义从 𝔉Γ 到 𝔉∂Γ 的变换 trackΓ：

    trackΓ : (Γ → Γ) → ∂Γ → ∂Γ
    trackΓ = f ↦ (γ, φ) ↦ (f(γ), φ ∘ f⁻¹)

该变换把上下文 Γ 上的任意函数 f ∈ 𝔉Γ 转化为效应上下文 ∂Γ 上的函数。当在状态 (γ, φ) 下执行 trackΓ(f) 时，f 的逆与 φ 相复合，从而在上下文中有效地追踪 f 的效应。每个 trackΓ(f) 本身都是 ∂Γ 上的双射，其逆为 (γ′, φ′) ↦ (f⁻¹(γ′), φ′ ∘ f)；相应地，我们把 ∂Γ 上的可允许效应定义为像集 𝔉∂Γ ≔ trackΓ(𝔉Γ)，根据定理 4，它是 Sym(∂Γ) 的子群，且与 𝔉Γ 同构。

**定理 3.** 下图交换。即 ∀f ∈ 𝔉Γ，pr₁ ∘ trackΓ(f) = f ∘ pr₁。

```
        f
    Γ ──────► Γ
    │         │
pr₁ │         │ pr₁
    ▼         ▼
   ∂Γ ──────► ∂Γ
       track
```

**定理 4.** track 保持 ∘ 运算。即 ∀f, g ∈ 𝔉Γ：

    trackΓ(f ∘ g) = trackΓ(f) ∘ trackΓ(g)    (5)

该定理保证：对于 𝔉Γ 中的每个函数，我们都能构造出一个等价的 track 版本，其效应在 ∂Γ 上被追踪。

**定义 5.** 定义 ∂Γ 上的变换 recoverΓ：

    recoverΓ : ∂Γ → ∂Γ
    recoverΓ = (γ, φ) ↦ (φ(γ), idΓ)

该变换把恢复函数 φ 作用于当前状态 γ，并把 φ 重置为恒等。这有效地恢复了 γ 上的所有效应。下图说明在一系列效应 track(f₁), ⋯, track(fₙ) 被应用于 ∂Γ 之后，recover 如何把上下文恢复到初始状态：

```
 f₁        fₙ
──►  ...  ──►
track     track     recover
∂Γ ──► ∂Γ  ...  ∂Γ ──► ∂Γ
```

#### 3.1.2. 可逆效应函数

上一节的 track/recover 模型假设逆是先验给定的，并原子地恢复所有累积的效应。然而在实践中，每个效应的逆并非先验可知：它必须由调用方在效应被应用的时刻提供。此外，recover 是"全有或全无"的：它无法选择性地撤销某一个效应而保留其他效应。为了解决这两个问题，我们在输入侧与输出侧同时增强该模型：

1. 在**输入侧**，我们不仅变换 Γ，还返回相应的逆函数，从而产生手动追踪能力：Γ → Γ × (Γ → Γ)，即 Γ → ∂Γ；
2. 在**输出侧**，我们不仅变换 ∂Γ，还返回相应的逆函数，从而产生部分恢复能力：∂Γ → ∂Γ × (∂Γ → ∂Γ)，即 ∂Γ → ∂²Γ。

这种增强保持了输入与输出之间的结构一致性，因此我们仍然可以定义相应的理论，维持 track 的数学性质。我们把 𝔉Γ 增强为 𝔈Γ 与 𝔈*Γ：

**定义 6.** 定义效应函数 𝔈Γ 与严格效应函数 𝔈*Γ 为：

    𝔈Γ ≔ Γ → Γ × (Γ → Γ)
    𝔈*Γ ≔ (e : Γ → Γ × (Γ → Γ))
           × ((γ : Γ) → (let (δ, g) = e(γ) in g(δ) = γ))    (6)

其中 e(γ) 产生一个二元组 (δ, g)，表示：

- δ : Γ 是新的上下文；
- g : Γ → Γ 是当前效应的逆函数。

上述定义中的约束 g(δ) = γ 可图示为如下交换图，确保返回值的第二分量确实是该变换本身的逆：

```
       e
Γ ─────────► ∂Γ
│             │
│pr₁          │pr₂
▼             ▼
Γ ◄───────── Γ
    f⁻¹
```

正如 track 把 𝔉Γ 提升到 𝔉∂Γ，我们定义 effect 把 𝔈Γ 提升到 𝔈∂Γ：

**定义 7.** 定义效应函数变换 effectΓ 为：

    effectΓ : 𝔈Γ → ∂Γ → ∂²Γ
    effectΓ = e ↦ (γ, φ) ↦ let (δ, g) = e(γ) in
              ((δ, φ ∘ g), trackΓ(g))

下图说明 effect 如何把 𝔈Γ 上的操作"翻译"到 𝔈∂Γ 上。我们将证明这种翻译确实保持图的交换性。

**定理 8.** effect 在 𝔈* 上封闭。即 ∀e ∈ 𝔈*Γ，effectΓ(e) ∈ 𝔈*∂Γ。

由于效应函数 𝔈Γ 不再是上下文上的自同态，它们无法被直接复合。因此我们定义一个新运算来表达效应复合：

**定义 9.** 给定函数 f, g ∈ 𝔈Γ，定义它们的**效应复合** f ⋄ g 为：

    f ⋄ g : Γ → ∂Γ
    f ⋄ g = γ ↦
            let (δ, s) = g(γ) in
            let (ε, t) = f(δ) in
            (ε, s ∘ t)

**定理 10.** ⋄ 运算在 𝔈* 上封闭。即 ∀f, g ∈ 𝔈*Γ，f ⋄ g ∈ 𝔈*Γ。

现在我们可以证明与 track 相类似的 effect 性质。

**定理 11.** effect 保持 ⋄ 运算。即 ∀f, g ∈ 𝔈Γ：

    effectΓ(f) ⋄ effectΓ(g) = effectΓ(f ⋄ g)    (7)

这些构造共同构成了**可逆效应**：𝔈*Γ 中的每个效应函数都显式地提供自身的逆，effect 在效应上下文 ∂Γ 上追踪这些逆，而 ⋄ 运算在保持可逆性的同时组合它们。时间可组合性正是由这一构造实现的：加载组件相当于应用一系列效应函数（在 φ 中累积逆），卸载组件相当于应用 φ 把上下文恢复到组合之前的状态。

卸载一个组件是否会干扰另一个组件，取决于 ⋄ 下的可交换性：若 f ⋄ g = g ⋄ f，则恢复 f 会完整保留 g 的贡献，这两个效应可以安全地分属相互独立的组件。当效应不可交换时，系统必须强制施加排序约束——要么在单个组件内部（通过效应迭代器的 LIFO 纪律，第 3.3 节），要么跨组件（通过施加依赖驱动的激活顺序的响应式余效应，第 3.2 节）。

### 3.2. 响应式余效应

空间可组合性是指组件声明彼此依赖、并由系统在运行时解析、提供与撤销这些依赖的能力。这要求在共享上下文变化时重新评估依赖的满足性，从而使组件恰好在其依赖变得可用时激活、在其依赖被撤销时去激活。因此，我们把组件的依赖建模为一个**规范**（specification），并针对该规范把上下文的每一次变化分类为"激活""去激活"或"中性"。针对规范的分类正是检测满足性变化的手段；对该分类做出响应正是驱动激活与恢复的手段。我们称这样的余效应为**响应式的**（reactive）：通过对上下文变化进行分类并据此驱动激活与恢复，正确的依赖排序成为一种结构性保证。

#### 3.2.1. 余效应上下文

传统的控制反转（IoC）容器 [38, 39] 通常把依赖建模为简单的键值映射。本节把 IoC 形式化为一个余效应上下文，它与可逆效应协同作用，为动态组合提供数学基础。

**定义 12.** 给定类型族 𝒱 : K → Type，定义**余效应上下文**（coeffect context）为依赖偏函数类型：

    Σ ≔ (k : K) ⇀ 𝒱k    (8)

其中 σ : Σ 是一个有限偏函数，为每个 k ∈ dom(σ) ⊆ K 赋一个类型为 𝒱k 的值。我们记：

- σ(k) 表示应用（当 k ∈ dom(σ) 时有定义）；
- σ[k ↦ v] 表示扩展（当 k ∉ dom(σ) 时有定义）；
- σ∖k 表示限制（当 k ∈ dom(σ) 时有定义）；
- k ∈ dom(σ) 表示成员关系。

类型族 𝒱 的使用保证了每个依赖键 k 都与一个特定的值类型 𝒱k 相关联，为依赖访问提供了静态类型安全。扩展与限制的前置条件对应于可逆效应中的幂等性要求：依赖不能被提供两次（扩展要求 k ∉ dom(σ)），也不能在缺失时被撤销（限制要求 k ∈ dom(σ)）。基于这一上下文结构，我们定义两个核心操作：

**定义 13.** Σ 上的 get 与 set 操作定义为：

    get : (k : K) → Σ → 𝒱k
    get = k ↦ σ ↦ σ(k)
    set : (k : K) × 𝒱k → Σ → Σ × (Σ → Σ)
    set = (k, v) ↦ σ ↦ (σ[k ↦ v], λσ′.σ′ ∖ k)

其中 get(k) 要求 k ∈ dom(σ)、set(k, v) 要求 k ∉ dom(σ) 作为前置条件。

值得注意的是，set(k, v) 的类型为 𝔈Σ，恰是余效应上下文上的一个效应函数。因此我们可以直接应用第 3.1 节的效应机制：effectΣ 提供了依赖注册的自动追踪与恢复。这正是响应式余效应与可逆效应之间的协同：余效应操作是效应，而效应是可逆的。

#### 3.2.2. 规范与通知

前面的定义描述了单个依赖如何被注册与访问。然而，访问一个缺失的依赖是一次运行时失败。因此，组件应当仅在其声明的所有依赖都已就位时才激活，而不是乐观地访问它们、在某个依赖缺失时失败。这引出两个问题：组件声明的依赖是否被**联合满足**，以及当该状态变化时系统应如何响应。余效应上下文 Σ 携带一个天然的观测结构，使这两个问题都可解：对于任意余效应规范 d ⊆ K，定义满足性谓词：

    σ ⊧ d ≔ ∀k ∈ d. k ∈ dom(σ)    (9)

该谓词是可判定的（因为 dom(σ) 有限）。由于对 σ 的所有变更都经过效应函数（其逆恢复先前的作用域），满足性的变化在每个效应边界处都可检测。这是响应性的代数基础：效应系统保证每一次余效应变化都被观测到。

**定义 14.** 一个**余效应规范**（coeffect specification）是：

    𝔇Σ ≔ Set(K)    (10)

表示组件从环境中声明的依赖集合。

使该规范具有响应性的，是它如何对状态转移进行分类。任何把 σ 变换为 σ′ 的效应，都可以依据 d 的满足性状态是否被改变而被规范 d ∈ 𝔇Σ 分类：

**定义 15.** 给定余效应规范 d ⊆ K 与状态 σ, σ′ ∈ Σ，定义：

    notify_d(σ, σ′) ≔
      activating    若 σ ⊭ d ∧ σ′ ⊧ d
      deactivating  若 σ ⊧ d ∧ σ′ ⊭ d
      neutral       其他情况    (11)

这是良定义的，因为 σ ⊧ d 可判定，且所有状态转移都由效应函数介导。响应式不变式是：一次"激活"转移触发组件效应的执行（伴随完整的效应追踪），而一次"去激活"转移触发累积效应的恢复。这些转移的精确操作语义取决于它们与控制流的交互，并在第 3.3 节展开。

空间可组合性由 set 与 notify 之间的交互而来。若组件 A 提供键 k（通过 set(k, v)），组件 B 声明 k ∈ dB，则 B 只能在 A 完全激活并提供 k 之后激活（因为 σ ⊧ dB 要求 k ∈ dom(σ)）。反过来，卸载 A 会把 k 从 dom(σ) 中移除，这将破坏 B 的满足性；因此系统确保 B 在 A 的恢复开始之前已完全去激活。于是，依赖排序（依赖方在其依赖之后激活；依赖在其依赖方之后卸载）从通知机制中自然涌现，而无需单独规定。

#### 3.2.3. 隔离与拦截

基本的余效应上下文 Σ 建模的是一张扁平的依赖表。然而在实践中，系统可能需要为不同组件把不同的值绑定到同一个逻辑依赖上。本节用两种机制扩展余效应上下文：**余效应隔离**（同一键在不同上下文中解析出不同结果）与**余效应拦截**（依赖访问上的横切行为）。

**余效应隔离。** 通过引入隔离域（isolation realm），余效应隔离允许同一依赖在不同上下文中绑定到不同值。这在多租户系统、测试环境与组件沙箱中有广泛应用。

**定义 16.** 定义带隔离的余效应上下文为：

    Σiso ≔ Map(K, R) × ((r : R) ⇀ 𝒱r)    (12)

它可以表示为一个二元组 (ρ, σ)，其中：

- ρ : Map(K, R) 是**隔离域表**，把逻辑键映射到域标识符；
- σ : (r : R) ⇀ 𝒱r 是**依赖表**，一个从域标识符到类型化值的偏依赖函数。

这种两层映射结构把逻辑层与存储层解耦，使依赖访问具有上下文感知能力。当访问键 k 时，系统先解析 ρ(k) 得到域标识符 r，再访问 σ(r) 获取实际值。

**定义 17.** Σiso 上的 get、set 与 isolate 操作是：

    get : (k : K) → Σiso → 𝒱ρ(k)
    get = k ↦ (ρ, σ) ↦ σ(ρ(k))
    set : (k : K) × 𝒱ρ(k) → Σiso → Σiso × (Σiso → Σiso)
    set = (k, v) ↦ (ρ, σ) ↦ ((ρ, σ[ρ(k) ↦ v]), λ(ρ′, σ′).(ρ′, σ′ ∖ ρ′(k)))
    isolate : K × R → Σiso → Σiso × (Σiso → Σiso)
    isolate = (k, r) ↦ (ρ, σ) ↦ ((ρ[k ↦ r], σ), λ(ρ′, σ′).(ρ′ ∖ k, σ′))

余效应隔离机制本质上实现了一个运行时的临时多态（ad-hoc polymorphism）系统。通过隔离域标识符，同一个依赖键可以在不同上下文中解析出完全不同的值，而且这种多态可以在运行时动态调整。与传统依赖注入相比，余效应隔离提供了更细粒度的控制，能够为特定组件定制隔离；所有操作都保持为效应函数（𝔈Σiso），因而继承可逆性。

**余效应拦截。** 第二种机制——余效应拦截——把横切元数据附加到依赖访问上，在不修改依赖值的情况下添加行为。这种元数据可以是上下文携带的，也可以是组件声明的，因此我们同时扩展余效应上下文与余效应规范：

**定义 18.** 定义带拦截的余效应上下文与规范为：

    Σinter ≔ ((k : K) → ℳk) × ((k : K) ⇀ (ℳk → 𝒱k))
    𝔇inter ≔ (k : K) ⇀ ℳk    (13)

上下文 Σinter 是一个二元组 (ι, σ)：ι 是安装在上下文本身上的上下文携带元数据，默认为空（εk）；σ 把每个键 k 映射到一个从元数据 ℳk 到值 𝒱k 的提供函数。规范 d ∈ 𝔇inter 携带组件声明的元数据，为每个键赋其元数据 d(k)，并以 dom(d) 作为依赖集合。每个键为其元数据配备一个幺半群 (ℳk, ⊕k, εk)：合并 ⊕k 是结合的，单位元为 εk（空元数据）。

**定义 19.** Σinter 上的 get、set 与 intercept 操作是：

    get : (k : K) × ℳk → Σinter → 𝒱k
    get = (k, μ) ↦ (ι, σ) ↦ σ(k)(μ ⊕k ι(k))
    set : (k : K) × (ℳk → 𝒱k) → Σinter → Σinter × (Σinter → Σinter)
    set = (k, ψ) ↦ (ι, σ) ↦ ((ι, σ[k ↦ ψ]), λ(ι′, σ′).(ι′, σ′ ∖ k))
    intercept : (k : K) × ℳk → Σinter → Σinter × (Σinter → Σinter)
    intercept = (k, ν) ↦ (ι, σ) ↦ ((ι[k ↦ ι(k) ⊕k ν], σ), λ(ι′, σ′).(ι′[k ↦ ι(k)], σ′))

当带有规范 d 的组件访问键 k 时，系统求值 σ(k)(d(k) ⊕k ι(k))：组件声明的元数据与上下文携带的元数据 ι 相合并，并把提供函数应用于结果。这种合并遵循每个键自身的语义（例如标量字段被覆盖，而集合值字段取并集），且是右偏的，因此 ι(k) 具有优先权，可以覆盖组件的声明，让外围上下文在不修改该组件的前提下约束组件如何使用某个余效应（例如第 5.2 节）。

### 3.3. 组件生命周期

前面几节发展的效应与余效应模型各自独立地定义了代数结构与规范。为了赋予它们操作意义，我们把二者结合为**组件**（component）这一概念——即它们共同支配的运行时实体。这里 Γ 同时充当效应上下文与余效应上下文；具体的统一在第 3.4 节给出。

**定义 20.** 上下文 Γ 上的一个**组件**定义为：

    ℭΓ ≔ 𝔇Γ × 𝔈Γ    (14)

表示一个二元组 (d, e)，并配备一个可变的生命周期状态，其中：

- d ∈ 𝔇Γ：余效应规范，声明从环境所需的依赖；
- e ∈ 𝔈Γ：效应函数，定义组件处于活跃状态时所贡献的效应。

组件的两侧共同决定其目标状态：从效应侧看，组件必须处于"存活"状态（其效应已被应用于上下文，且相应的逆尚未被调用）；从余效应侧看，所有声明的依赖必须被满足（σ ⊧ d）。当两个条件都成立时目标状态为 ACTIVE（活跃）；否则为 INACTIVE（不活跃）。每当目标状态发生变化——无论由哪一侧引起——就发起一次转移：RELOAD（重载）执行组件的效应函数 e，在上下文上累积副作用；UNLOAD（卸载）应用累积的逆来恢复上下文。在其最简形式中，生命周期是图 1 所示的双状态模型。在实践中，转移会与各种控制流结构交互，需要针对幂等恢复、迭代、一致性与异步进行扩展。

```
      RELOAD
INACTIVE ──────► ACTIVE
     ◄──────
      UNLOAD
图 1 | 基础组件生命周期
```

#### 3.3.1. 幂等恢复

组件的 UNLOAD 应用累积的逆来恢复上下文；为了使其安全，每个逆至多只能运行一次。在 recover 的层次上，这一点由构造保证：定义 5 把 φ 重置为 idΓ，因此第二次 recover 是空操作。这一义务只对 effect 交还给调用方的部分销毁器（定义 7 的 ∂Γ → ∂²Γ 分量）成立，它从累积的 φ 中切除单个效应的逆：调用它两次会把该逆应用两次并破坏上下文。因此我们使每个返回的销毁器幂等。

我们把每个销毁器的"已武装"状态作为其自身的私有状态保存，使得被保护的逆保持为它所作用的状态 γ 的纯函数，而不依赖隐藏的可变状态。构建守卫是**生成式**的（generative）[40]：每次应用为新销毁器生成一个全新句柄，因此没有两个销毁器共享句柄，且句柄始终内在于守卫。实现上，这体现为在每个 dispose 闭包中新捕获的 armed 变量。

**定义 21.** 幂等守卫 idem : (Γ → Γ) → (Γ → Γ)（∂Γ 上同理）使一个逆至多在其首次调用时触发。包装是生成式的：每次应用生成一个全新句柄 h，它对所产生的销毁器私有，且不出现在 idem 的类型中。状态记录已被消耗的句柄；全新的 h 不在其中，因而处于存活状态，触发时将其标记为已消耗：

    idem(g) = let h fresh in γ ↦ { g(γ)[h ↦ ()]  若 h ∉ dom(γ)
                                  { γ            若 h ∈ dom(γ)    (15)

由于 h 是在守卫被构建时选定的，而非由调用方提供，且已消耗集合只会增长，销毁器至多触发一次，同时仍是一个纯的前置条件效应函数；∂Γ、∂²Γ 与 𝔉Γ 保持不变，已消耗集合也不属于可逆状态的一部分。

于是，effectΓ（定义 7）的幂等变体只差一处替换：返回的逆 trackΓ(g) 被 idem（它自行生成句柄）包裹，而前向累积 φ ∘ g（由已经幂等的 recover 恢复）保持不变。

    effectΓ^idem : 𝔈Γ → ∂Γ → ∂²Γ
    effectΓ^idem = e ↦ (γ, φ) ↦ let (δ, g) = e(γ) in
                   ((δ, φ ∘ g), idem(trackΓ(g)))

由于只有返回的逆被包裹，累积 φ 与第 3.1.2 节的同态结果不受影响。

#### 3.3.2. 迭代

组件的 RELOAD 转移可能顺序执行多个效应，而 UNLOAD 必须恢复它们。我们使用**效应迭代器**（effect iterators）建模多步执行，其中每一步返回修改后的上下文、一个逆以及一个续延：

**定义 22.** 定义效应迭代器 𝔈iterΓ 与严格效应迭代器 𝔈iter*Γ 为如下递归类型：

    𝔈iterΓ ≔ μℑ. Γ → Γ × (Γ → Γ) × Maybe(ℑ)
    𝔈iter*Γ ≔ μℑ. (e : Γ → Γ × (Γ → Γ) × Maybe(ℑ))
                × ((γ : Γ) → (let (δ, g, o) = e(γ) in g(δ) = γ))    (16)

其中 e(γ) 产生一个三元组 (δ, g, o)，表示：

- δ 是新的上下文；
- g 是当前效应的逆函数；
- o 表示续延：

- Nothing 表示迭代终止；
- Just(i) 提供下一步迭代。

效应迭代器变换 effectiterΓ 通过递归调用把 effectΓ 扩展到迭代器结构上：

**定义 23.** 定义效应迭代器变换 effectiterΓ 为：

    effectiterΓ : 𝔈iterΓ → ∂Γ → ∂²Γ
    effectiterΓ = i ↦ (γ, φ) ↦
      let (δ, g, o) = i(γ) in
      match o
        | Nothing ⇒ ((δ, φ ∘ g), trackΓ(g))
        | Just(i) ⇒ let (s, r) = effectiterΓ(i)(δ, φ ∘ g) in
                     (s, trackΓ(g) ∘ r)

在每一步递归中，逆 g 按应用顺序复合到 φ 上，因此累积的恢复函数 φ ∘ g₁ ∘ ⋯ ∘ gk 在应用时自然按 LIFO 顺序恢复效应。在任意两个连续步骤之间，若目标状态已改变，系统可以中断转移，应用迄今累积的逆来恢复上下文。这一性质内在于迭代器模型：每一步的 Maybe(𝔈iter) 续延构成了一个自然的中断边界，而无需额外机制，如图 2 所示。在这个意义上，效应迭代器是一个被具体化的定界续延——正是主流语言通过 yield 运算符 [41] 暴露的结构，因此该模型可以直接映射到它们已经提供的生成器上。

```
      RELOAD
INACTIVE ──────► ACTIVE
     ◄──────
      UNLOAD
图 2 | 带步骤边界中断的迭代式转移
```

单步效应（𝔈Γ）是迭代器立即返回 Nothing 的退化情形，因此转移是原子的、没有中断边界。对于多步转移，步骤的粒度决定响应性：更细的步骤能更快地响应目标状态变化，代价是更频繁的条件检查。

作为进一步的扩展，把迭代器返回类型中的逆分量 Γ → Γ 替换为另一个迭代器，就得到双向效应迭代器 𝔈bidiΓ = μℑ. Γ → Γ × ℑ × Maybe(ℑ)，其三元组 (δ, p, n) 同时提供前向续延 n 与逆向步骤 p。这允许 UNLOAD 也能增量进行，并可被中断回到 RELOAD。然而，很少有编程语言提供原生的双向迭代语义，限制了这一扩展的实际适用性。

#### 3.3.3. 一致性

当依赖在运行时被替换时，目标状态可能快速连续地经历 ACTIVE → INACTIVE → ACTIVE。若为第一次 ACTIVE 发起的 RELOAD 在第二次 ACTIVE 到来时仍在进行（或尚未被中断），系统就面临一个依赖值已改变的 ACTIVE → ACTIVE 场景。简单地继续原来的 RELOAD 会把组件绑定到过期的依赖上，违反一致性。

为了检测这种过期，我们引入一个**纪元（epoch）机制**，对目标状态进行版本化。为余效应规范 d 定义纪元函数 εd : Σ → E：

    εd(σ) ≔ ⟨σ(k) | k ∈ d⟩    (17)

两个纪元相等当且仅当所有依赖值都相同：εd(σ) = εd(σ′) ⇔ ∀k ∈ d. σ(k) = σ′(k)。INACTIVE 的纪元是一个区分性的常量 ⊥E，不等于任何活跃纪元。

每当一次转移开始时，目标状态当前的纪元 εtarget 被记录为 εinert。在每个迭代器步骤边界处，系统比较 εtarget 与 εinert：只有二者匹配时下一步才继续；否则转移被中止，并应用累积的逆。

纪元机制有效地把生命周期从一维的二元 INACTIVE/ACTIVE 模型扩展为多维的星形结构。ACTIVE 的不同依赖配置形成相互独立的分支，全部连接到 INACTIVE 状态，如图 3 所示。

```
        INACTIVE ⊥
     ┌────┼────┬────┐
   ACTIVE ACTIVE ACTIVE ...
  ⟨a:2,b:2⟩ ⟨a:1,b:3⟩ ⟨a:2,b:3⟩ ...
图 3 | 基于纪元的生命周期：每种依赖配置构成一个独立分支
```

#### 3.3.4. 异步

同步模型假设每次转移相对于外部状态变化是瞬时完成的。当转移涉及异步计算时，这一假设被打破。我们抽象地建模非瞬时性：一次转移产生一个类型为 Future(A) 的值，其中 Future 是一个不透明的类型构造子，其定义性质是：在提交与解析之间，外部状态可能发生变化。

在该模型下，RELOAD 与 UNLOAD 占据真实时间，期间目标状态可能改变。这以两种方式威胁资源安全：立即回滚一个进行中的 RELOAD 会违反尚未执行完的效应的 LIFO 顺序；快速振荡可能两次调用同一个逆。为了恢复安全性，我们把 RELOAD 与 UNLOAD 从瞬时转移提升为**惯性状态**（inertial states）：一旦进入，转移就运行至完成，系统才响应目标状态的任何变化。所得状态机如图 4 所示。

```
      RELOAD
INACTIVE ──────► ACTIVE
     ◄──────
      UNLOAD
图 4 | 异步转移的惯性状态机
```

惯性状态的语义是：

- 若组件处于 RELOAD 且目标变为 INACTIVE，则 RELOAD 运行至完成，然后 UNLOAD 开始。若目标在 RELOAD 完成前又回到 ACTIVE，则组件正常进入 ACTIVE。
- 对称地，若组件处于 UNLOAD 且目标变为 ACTIVE，则 UNLOAD 先完成，然后 RELOAD 开始。若目标回到 INACTIVE，则组件进入 INACTIVE。
- 一个挂起的逆向转移被推迟，直到当前惯性状态解析完毕，届时依据当时的目标决定执行它或丢弃它。

迭代器步骤边界与惯性语义相组合：在每一步，系统检查纪元是否仍然匹配。若不匹配，转移即中止（应用累积的逆），被推迟的转移随即开始；这是在每个惯性转移内部运作的同步中断机制。

### 3.4. 上下文范式

第 3.3 节的组件定义使用单一上下文 Γ 同时携带效应与余效应。本节为该统一给出具体构造，并论证所得的上下文类型本身就构成一个编程范式。

#### 3.4.1. 统一上下文

对于上下文 Γ，效应上下文 ∂Γ（第 3.1 节）提供了一个更高层的抽象，记录上一层的上下文以及该层累积的副作用。使这一结构递归化，并与余效应上下文 Σ 相结合，得到如下类型：

**定义 24.** 上下文类型 Γ∞ 定义为：

    Γ∞ ≔ μΓ. Γ × (Γ → Γ) × Σ    (18)

其中三个投影分别是：

- Γ：当前的上下文状态（递归的）；
- Γ → Γ：用于效应恢复的累积逆；
- Σ：携带依赖信息的余效应上下文。

在该定义下，effect 成为 Γ∞ 上的自同态，把 ∂-塔统一为一个自相似的类型。余效应上下文 Σ 被结构化地整合进来：依赖操作（set、get）作用于 Σ 分量，而第二分量中的逆函数追踪它们的逆转。由于 Σ 所基于的类型族 𝒱 不受约束，系统需要跨组件共享的任何全局状态都可以编码为一个具有适当值类型的依赖——Σ 涵盖所有共享可变状态，而不仅仅是组件间的依赖。组件与其环境之间的每一次交互都经过这一单一实体。

**层次化组合。** Γ∞ 的递归结构支持层次化控制：父上下文聚合多个子层的效应，形成一个树形控制结构，在维持模块化的同时实现跨层的统一管理。效应变换实现了字面意义上的"插件（plug-in）"隐喻：

- 加载组件对应于执行其效应（插入，plugging in）；
- 卸载组件对应于恢复其效应（拔出，unplugging，且不影响其他正在运行的组件）；
- 处于层次结构不同层的组件可以独立地加载与卸载；父上下文聚合并管理其所有子组件的效应，实现任意深度的嵌套组合。

**指称与实现。** 把一个操作类型化为 Γ∞ 上的效应，固定了它的**指称**（denotation）——即一个后继状态与一个逆的配对——但不固定它的**实现**（realization），后者决定该逆如何被施行。

**定义 25.** 效应函数 f : 𝔈Γ∞ 允许两种实现：

- **就地实现**变更上下文并返回一个非平凡的逆；后继别名输入，恢复运行逆来逆转变更。
- **派生实现**保持输入不变，在 Γ∞ 的递归结构中返回一个全新上下文，并以恒等为逆；恢复即丢弃派生上下文。

在纯函数式场景下二者合一；命令式宿主可以为每个操作任选其一。第 4.1.2 节将给出两种实现的代表性实现。

#### 3.4.2. 定位上下文范式

编程范式在如何处理副作用上存在根本差异。两个既定的极点定义了这一谱系：

**显式状态线程化（函数式）。** 为保持引用透明性，纯函数式语言把副作用建模为对状态的显式变换。State 单子 S → (A, S) [22] 把环境线程化地贯穿每一次计算。这一方法产生强大的组合保证：效应在类型中可见，且适合等式推理。然而，它带来显著的人体工学成本：调用链中的每个函数都必须接受并返回状态参数，即便它只是原样传递状态。随着效应维度数量的增长（日志、配置、I/O），单子叠加或效应处理器的样板代码会急剧膨胀。

**隐式变更（命令式/面向对象）。** 主流命令式语言允许组件修改共享状态、访问依赖，而无需在调用点显式声明。在效应侧，一个代表性例子是 React 的 useEffect 钩子：它在组件的内部 fiber 上注册一个持久副作用，但既没有把效应目标、也没有把注册机制作为显式参数——识别依赖于隐藏运行时状态中的调用顺序位置。在余效应侧，Java 的服务定位器模式（例如 Spring 的 ApplicationContext.getBean(...)）在运行时从全局注册表中检索依赖，需要在每个调用点做空值检查与类型转换；依赖关系是隐式的，散布在整个代码库中。更一般地，理解 f() 如何修改或依赖系统，需要传递性地阅读其实现。重构变得脆弱，因为移动或移除一个调用可能悄悄破坏远处的隐含不变式。

**上下文范式**结合了函数式方法的可追踪性与命令式方法的人体工学。效应与余效应都通过一个显式的上下文参数介导。因此，每个操作都可归因于它被调用的那个特定上下文，进而归因于该上下文所属的那个组件。

除了结合两个极点的优点，上下文范式还让开发者**逐个**处理每个效应与依赖，并把它们自动组合成系统的行为。对于可逆效应，开发者提供每个原子操作的逆，任何复合操作的逆由复合运算自然得出（第 3.1 节），因此加载与拆除由构造即可逆。对于响应式余效应，组件只声明它需要的依赖，运行时自动解析并重新接线（第 3.2 节），在提供者被增删或替换时保持依赖的一致接线。在两个方向上，原本依赖开发者自律的正确性都成为范式的结构性属性。

---

## 4. 实现与案例研究

本节介绍 **Cordis**，它把第 3 节的形式化模型实现为实用的编程抽象。Cordis 是一个时空可组合性的**元框架**：与面向特定领域的应用框架（例如 Web 路由、ORM、UI 渲染）不同，它不规定任何具体场景；其唯一职责是提供通用的动态组合语义。实现分为三层：(1) **核心库**（第 4.1 节）直接实现效应与余效应系统；(2) **组件加载器**（第 4.2 节）在核心之上扩展配置对账与热模块替换；(3) 诸如 **Koishi** 之类的应用框架（第 4.3 节）在前两层之上构建领域特定功能。

### 4.1. 核心库

表 1 总结了理论构造与其运行时对应物之间的对应关系。特别地，本节通篇使用下文引入的运行时名称，把理论符号保留给形式化对应关系。我们还用 @@name 表示框架内部的符号键，因此 ctx[@@store] 中的方括号表示对上下文上一个不透明槽位的符号键访问，而非对字符串键映射的索引。

**fiber**（纤程）是组件的运行时实例：一个携带组件经历第 3.3 节生命周期的有状态对象。它把组件的静态规范与生命周期算法所读取的实时转移状态捆绑在一起。静态规范包括其余效应规范 fiber.inject（d）与效应函数 fiber.apply（e），以及两个相关上下文：fiber.parent（fiber 被实例化于其上的上下文）与 fiber.ctx（由它派生的、fiber 在其中运行的全新子上下文，定义 25）。转移状态包括累积逆 fiber.dispose（recover）、目标状态版本 fiber.epoch（εd(σ)，其中 ⊥ 表示 INACTIVE）以及在途转移句柄 fiber.inertia。表 1 将它们与其理论对应物并列列出。

| 理论（第 3 节） | 实现 |
|---|---|
| Γ∞ | ctx，一等上下文 |
| 𝔈Γ, 𝔈iterΓ | 返回/产出逆的效应回调 |
| effectΓ(e) | ctx.effect(callback) |
| Σ, Σiso, Σinter | ctx[@@store], ctx[@@isolate], ctx[@@intercept] |
| get(k), set(k, v) | ctx.get(key), ctx.set(key, value) |
| isolate(k, r) | ctx.isolate(key, realm) |
| intercept(k, ν) | ctx.intercept(key, metadata) |
| ℭΓ | fiber，组件的运行时实例 |
| d : 𝔇Γ | fiber.inject |
| e : 𝔈Γ | fiber.apply |
| εd(σ) | fiber.epoch |
| recover | fiber.dispose，累积逆 |

**表 1 | 理论与实现的对应关系**

本节其余部分自底向上地构建核心库。第 4.1.1 节实现可逆效应——上下文被变更的唯一原语；第 4.1.2 节在其上实现响应式余效应；第 4.1.3 节把二者组合为组件生命周期；第 4.1.4 节暴露基于它们构建的上下文级操作。

#### 4.1.1. 效应追踪

本节实现可逆效应（第 3.1 节）。Cordis 中的每一次上下文变更都流经单一原语 ctx.effect：余效应的提供、组件的实例化以及其他每一个变更上下文的操作都归结为一次 ctx.effect 调用，因此任何经由上下文执行的操作在组件卸载时都被自动追踪与恢复。在操作上，ctx.effect 是 effectiterΓ（定义 23）的实现：它接受一个类型为 𝔈iterΓ 的回调，把它提升到 𝔈iter∂Γ，产生一个 dispose 闭包，调用该闭包即恢复该效应。Cordis 通过这一操作同时接受 𝔈Γ 与 𝔈iterΓ（临时多态）；我们以迭代器形式为代表，因为普通的效应函数是只产出一个逆的退化迭代器。

算法 1 展示 ctx.effect 的构造。我们用 f ∘ g 表示先运行 g 再运行 f 的销毁器，用 id 表示空操作；把每个新逆前置，即得到 LIFO 恢复。

```
算法 1  效应追踪
 1  async function execute(callback, guard)
 2    iter ← callback()
 3    inverse ← id
 4    while guard()
 5      (value, done) ← await iter.next()
 6      if value then inverse ← value ∘ inverse
 7      if done then break
 8    return inverse
 9  function effect(ctx, callback)
10    armed ← true
11    task ← execute(callback, () ↦ armed)
12    async function dispose()
13      if not armed then return
14      armed ← false
15      recover ← await task
16      recover()
17    ctx.dispose ← dispose ∘ ctx.dispose
18    return dispose
```

引擎 execute 把回调作为效应迭代器（𝔈iterΓ，定义 22）驱动，并把每一步产出的逆折叠为单一复合逆。在每一步之前它查询调用方提供的守卫；一旦守卫跳闸，迭代停止，仅保留迄今累积的逆。这正是第 3.3.2 节的步骤边界中断：Maybe(𝔈iter) 续延由迭代器的 done 标志与守卫共同实现。

ctx.effect 是 execute 之上的一层薄包装，额外做了两件事。第一，幂等自销毁（实现 idem，定义 21）：守卫报告 armed 标志，返回的 dispose 把 armed 翻转为 false，这同时中止任何在途迭代并使恢复至多触发一次。第二，父组合：dispose 被前置到外围上下文累积逆 ctx.dispose 之前，因此子效应的逆本身是父上下文上的一个效应，实现了 ∂²Γ 的递归结构。组件层（第 4.1.3 节）复用同一个 execute，但使用测试纪元稳定性而非 armed 的守卫。

#### 4.1.2. 余效应操作

本节实现响应式余效应（第 3.2 节）。所有余效应操作作用于每个上下文所携带的三个符号键槽位：

- @@store：值存储 σ : (r : R) ⇀ 𝒱r，从域符号到类型化值；
- @@isolate：域表 ρ : Map(K, R)，从余效应键到域符号；
- @@intercept：拦截表 ι : (k : K) → ℳk，为每个键赋其元数据。

前两者构成两层解析 k → ρ(k) → σ(ρ(k))：ctx.get(key)（算法 2）从 @@isolate 读取域符号 ρ(k)，再从 @@store 读取绑定值 σ(ρ(k))。ρ 的间接性让隔离可以把一个键重定向到一个独立的绑定；而 @@intercept 只在绑定被访问时被查询，调整的是绑定的使用方式而非其解析结果。我们分两部分实现这些操作：(1) **提供与通知**，安装或撤销绑定并把变化传播给依赖方；(2) **隔离与拦截**，重塑键的解析方式。

**提供与通知。** 由于 set(k, v) 的类型为 𝔈Σ（第 3.1 节），余效应的提供是一次 ctx.effect 调用，并继承其自动追踪与恢复。算法 2 实现 ctx.set(key, value)，即具体的 set(k, v)：回调把一个值绑定到域符号 ρ(k) 下的存储中，返回的 dispose 函数移除它。安装与移除都会调用 notify，把变化传播给依赖组件。

```
算法 2  余效应操作
1  function get(ctx, key)
2    realm ← ctx[@@isolate][key]      ▷ ρ(k)
3    return ctx[@@store][realm]       ▷ σ(ρ(k))
4  function set(ctx, key, value)
5    function callback()
6      realm ← ctx[@@isolate][key]    ▷ ρ(k)
7      ctx[@@store][realm] ← value    ▷ σ[ρ(k) ↦ v]
8      notify(ctx, [key])
9      return function()
10       delete ctx[@@store][realm]    ▷ σ∖ρ(k)
11       notify(ctx, [key])
12   return ctx.effect(callback)
```

算法 3 通过以下方式把每次绑定变化传播给依赖方：对每个存活的 fiber，测试某个发生变化的键是否出现在其 fiber.inject 中并解析到同一域；若是，则调用 refresh（第 4.1.3 节）针对新状态重新评估该 fiber。这正是定义 15 的响应式分类：翻转 σ ⊧ d 的变化激活或去激活 fiber，而 refresh 的幂等性使中性变化无害。这种重新评估与多样化控制流的交互在第 4.1.3 节展开。

```
算法 3  响应式通知
1  function notify(ctx, keys)
2    for fiber in all_fibers do
3      for key in keys do
4        if key ∈ fiber.inject and fiber.ctx[@@isolate][key] = ctx[@@isolate][key] then
5          refresh(fiber)
6          break
```

**隔离与拦截。** 这两个操作在结构上做的是同一件事：各自派生一个子上下文，为 key 调整一张继承来的表，而父上下文保持不变，因此恢复是隐式的：丢弃子上下文即已足够，无需运行显式逆。ctx.isolate(key, realm) 用 realm（默认是全新生成的符号）覆盖域映射 ρ（实现 isolate，定义 17），因此为同一键赋不同符号的两个上下文会解析出相互独立的绑定。ctx.intercept(key, metadata) 把元数据合并进拦截表 ι（实现 intercept，定义 19）：遵循该定义，新元数据与上下文为该键已有的元数据相合并，并优先于它。

#### 4.1.3. 组件生命周期

组件由 ctx.use 实例化为一个 fiber。本节赋予 fiber（第 4.1 节引入）第 3.3.4 节惯性状态机的操作意义。两个字段驱动下述算法：fiber.parent，即 fiber.ctx 的父上下文，构成组件层次结构（Γ∞ 的递归结构，第 3.4.1 节）；fiber.inertia，即在途异步转移的句柄（空闲时为 null）。

算法 4 展示组件实例化。一个组件把余效应规范 component.inject（d）与效应函数 component.apply 配对；实例化把组件的 config 绑定进 fiber.apply（第 9 行），即生命周期随后运行的、已应用配置的效应函数（e）。回调函数（第 2 行）是在父 fiber 中被追踪的效应：执行时，它通过调用 refresh（算法 5）启动子组件的生命周期；被恢复时，它把子组件的纪元强制置为 ⊥ 并触发卸载。这保证卸载父组件会级联到所有子组件，是父效应上下文上 ⋄ 复合的直接结果。

```
算法 4  组件实例化
1  function use(ctx, component, config)
2    function callback()
3      refresh(fiber)
4      return function()
5        fiber.epoch ← ⊥
6        unload(fiber)
7    fiber ← Fiber(parent: ctx, inject: component.inject)
8    fiber.ctx ← ctx[fiber ↦ fiber]
9    fiber.apply ← () ↦ component.apply(fiber.ctx, config)
10   ctx.effect(callback)
11   return fiber
```

算法 5 实现第 3.3.4 节的惯性状态机，其中 RELOAD 与 UNLOAD 是惯性的：一旦进入，转移就运行至完成，系统才响应目标状态变化。refresh 函数从余效应存储重新计算 fiber 的纪元，若 fiber 尚不处于转移中，则发起 reload 或 unload 任务²。reload 函数记录当前纪元并执行组件的效应函数 apply。完成后，它检查纪元是否仍匹配：若匹配，fiber 稳定于 ACTIVE；若不匹配（无论新纪元是 ⊥ 还是不同的活跃配置），它链式进入 unload。对称地，unload 按 LIFO 顺序恢复所有被追踪的效应，然后稳定于 INACTIVE 或链式进入 reload。这种相互递归实现了惯性性质：一旦转移开始，它就在任何新转移能够开始之前完成。

```
算法 5  组件生命周期
1   function refresh(fiber)
2     epoch ← εd(σ)
3     if epoch = fiber.epoch then return
4     fiber.epoch ← epoch
5     if fiber.inertia then return
6     if epoch ≠ ⊥ then
7       fiber.inertia ← create_task(reload(fiber))
8     else
9       fiber.inertia ← create_task(unload(fiber))
10  async function reload(fiber)
11    epoch0 ← fiber.epoch
12    recover ← await execute(fiber.apply, () ↦ fiber.epoch = epoch0)
13    fiber.dispose ← recover ∘ fiber.dispose
14    if fiber.epoch = epoch0 then
15      fiber.inertia ← null
16    else
17      fiber.inertia ← create_task(unload(fiber))
18  async function unload(fiber)
19    await fiber.dispose()
20    fiber.dispose ← id
21    if fiber.epoch = ⊥ then
22      fiber.inertia ← null
23    else
24      fiber.inertia ← create_task(reload(fiber))
```

²create_task 调度一个异步函数并发运行，并返回其句柄（存于 fiber.inertia）。我们为语言无关性而显式写出它：对于急切调度（如 TypeScript 的 Promise），调用是隐式的，返回的 Promise 即句柄；而对于惰性调度（如 Python 协程、Rust future），宿主必须生成（spawn）任务才能使其推进。

纪元通过以下方式计算：针对当前余效应存储解析每个声明的键，并把解析出的值组成元组（第 3.3.3 节）。由于 notify（第 4.1.2 节）在每次余效应变化时都重新计算纪元，fiber 恰好在其解析值变化时重载。

该算法在两个互补层次上运作。在**转移层次**，reload 与 unload 在完成时检查纪元，实现跨转移的惯性链式衔接。在每个转移内部的**步骤层次**，效应执行（算法 1）在每个迭代器步骤边界检查纪元，实现在单次转移内的部分回滚。这两种机制分别对应第 3.3.3 节的转移间与转移内纪元检查。

#### 4.1.4. 上下文访问

第 4.1.2 节的余效应操作构成一个反射式 API：余效应用 ctx.set(key, value) 写入、用 ctx.get(key) 读取，二者都以名称作键。Cordis 在此反射式 API 之上叠加了第二种、更原生的扩展与消费上下文的方式：**属性访问**。组件可以把一个余效应作为属性 ctx[key] 来访问，仿佛它是上下文的原生结构，而非通过方法调用。在 TypeScript 中，Cordis 用一个 Proxy 实现这一点，其 get 陷阱介导每一次属性访问。算法 6 展示上下文如何在第 4.1.2 节的原语 get 之上把这样一次访问解析为余效应。

```
算法 6  代理介导的上下文访问
1  function resolve(ctx, key)
2    fiber ← ctx.fiber
3    repeat
4      if key ∈ fiber.inject then return get(ctx, key)
5      if fiber = root then throw UNDECLARED_ACCESS
6      fiber ← fiber.parent.fiber
```

算法 6 从访问上下文出发沿 fiber 链向上遍历：在第一个于其 inject 中声明 key 的 fiber 处，访问被授权，值由 get（算法 2）获取；若遍历到根仍无此声明，则访问被拒绝。这正是代理与裸 ctx.get 的差异所在：ctx.get(key) 是一次全量查找，返回绑定值或空，从不失败；而代理拒绝任何对未声明余效应的访问，在使用点强制执行余效应规范 d。这里不会出现"已声明但缺失"的值，因为 fiber 只有在其所有声明的余效应都被满足后才变为 ACTIVE（第 4.1.3 节）。

这种拒绝是在访问点执行的运行时检查。由于组件的余效应规范 d 是静态声明的，同样的违规原则上可以在编译期检测到——在执行前把每个 ctx[key] 对已声明的 d 进行解析；第 5.3 节讨论宿主语言的类型级依赖声明与编译期元编程如何施行这一介导。

### 4.2. 组件加载器

核心库为组件开发者提供了动态组合的命令式原语，如 ctx.effect、ctx.use 与 ctx.set。对于应用编排者——他们把现成组件装配成运行中的系统，并在其生命周期内调整组合——则存在一个独立关切。组件加载器通过引入**声明式配置层**来处理这一关切：编排者把期望的组合指定为一个持久化数据结构，加载器把对这一规范的变更翻译为相应的命令式 fiber 操作。

#### 4.2.1. 声明式配置层

Cordis 系统的运行时是 fiber 的组合，它们按照所栖身的上下文层次结构（第 3.4.1 节）构成一棵树。通过把每个 fiber 序列化为一个条目（entry），并把条目组织成同样的树结构，声明式层把应用的结构捕获为一个持久化规范。

**定义 26.** 一个条目声明单个 fiber，记录：

- **id** —— 稳定标识符，当其组（group）的子列表变化时用作对账键；
- **url** —— 要实例化的组件模块的 URL；
- **isolate** —— 应用于该条目上下文的隔离标注；
- **intercept** —— 应用于该条目上下文的拦截标注；
- **config** —— 绑定进组件以形成其效应函数 apply 的配置；
- **disabled** —— 该条目是否被管理性地关闭。

在运行时，一个条目管理它所声明的 fiber，并响应这些字段的变化，把声明式规范绑定到其命令式实现上。

这些条目构成一棵**配置树**，是系统加载内容的权威记录。一个条目可以是映射到单个 fiber 的叶节点，或者其组件可以转而加载更多组件，使该条目成为一个分支节点。Cordis 为这种分组与嵌套加载提供组件：@cordisjs/group 以子条目列表作为其配置并把它们作为一个子组加载，而 @cordisjs/include 加载一个外部配置文件（YAML 或 JSON）并把其条目作为嵌套子树嫁接进来。

当一个条目的记录变化时，加载器**增量式**对账：不是把 fiber 整个拆除再重建，而是检查哪些字段变化了，并对每个字段施加干扰最小的操作。

- **id、url** —— 重建条目，因为其身份或组件已改变；
- **isolate** —— 重写条目的域映射，迁移该条目提供的任何余效应，并通知解析发生变化的依赖方；
- **intercept** —— 原地更新，因为拦截元数据在读取时被查询，无需重载；
- **config** —— 交给组件，由组件决定如何应用新负载，通常是对照前一个负载做 diff，仅在实质变化时重载。特别地，@cordisjs/group 条目的 config 是其子条目列表，因此它把更新作为子 id 上的键控 diff 来应用，创建、移除或更新每个子条目；由于更新一个存活的子条目会重新进入同样的逐字段分发，组对账与条目更新一起沿树递归。
- **disabled** —— 置位时卸载 fiber，清零时重载它。

Cordis 还允许组件在运行时更新自身的配置或禁用它自己。无论哪种情况，加载器都把变化写回配置层，使持久化规范始终忠实于实时系统。

#### 4.2.2. 热模块替换

热模块替换（HMR）在模块层次应用可逆效应模式：当源文件变化时（通常在开发期间），系统在不重启进程的情况下就地替换受影响的模块。由于 fiber 已经为其组件的所有效应与余效应划定了边界，一个本身即组件的模块可以仅通过 fiber 操作被替换：销毁旧 fiber 恢复该组件安装的一切，而一个从重载模块实例化的新 fiber 重新安装它们。因此，与 Webpack [42] 或 Vite [43] 的 HMR 不同，HMR 无需开发者标注的接受边界。

@cordisjs/hmr 组件提供 HMR 引擎，它分三个阶段运作。

**阶段 1：模块分类。** 引擎接收两个输入：stashed 集合（自上次重载以来内容发生变化的文件 URL）与 externals 集合（无法热替换、反而触发完整重启的模块）。用 get_imports(url) 表示 url 直接导入的模块，它分类变化的依赖子图，把每个模块标记为 accepted（接受）或 declined（拒绝）：

```
算法 7  模块分类
1   function classify(stashed, externals)
2     accepted ← stashed
3     declined ← externals
4     pending ← ∅
5     for url in stashed do
6       pending ← pending ∪ (get_imports(url) ∖ (accepted ∪ declined))
7     repeat
8       progress ← false
9       for url in pending do
10        if get_imports(url) ∩ accepted ≠ ∅ then
11          accepted ← accepted ∪ {url}
12          pending ← pending ∖ {url}
13          progress ← true
14        else if get_imports(url) ⊆ declined then
15          declined ← declined ∪ {url}
16          pending ← pending ∖ {url}
17          progress ← true
18        else
19          pending ← pending ∪ (get_imports(url) ∖ (accepted ∪ declined))
20    until not progress
21    declined ← declined ∪ pending
22    return (accepted, declined)
```

以 stashed 文件的导入为种子，不动点在一个模块的某个导入被接受时接受它，在其所有导入都被拒绝时拒绝它；任何未决的、陷入导入环的模块默认被拒绝。

**阶段 2：过期条目检测。** 引擎随后用 accepted 与 declined 把组件条目过滤为过期条目——即依赖树触及某个已变化模块的条目。它用 get_dependencies 遍历每个条目的树，后者收集一个模块的传递导入，同时把 declined 视为边界：

```
算法 8  过期条目检测
1   function get_dependencies(root, declined)
2     deps ← ∅
3     function traverse(url)
4       if url ∈ deps or url ∈ declined then return
5       deps ← deps ∪ {url}
6       for child in get_imports(url) do traverse(child)
7     traverse(root)
8     return deps
9   function detect(entries, accepted, declined)
10    stale_entries ← ∅
11    for entry in entries do
12      tree ← get_dependencies(entry.url, declined)
13      if tree ∩ accepted ≠ ∅ then
14        accepted ← accepted ∪ tree
15        stale_entries ← stale_entries ∪ {entry}
16    return stale_entries
```

一个条目恰好在其树与 accepted 相交时为过期；该树随后被并入 accepted，因此沿它的每个过期模块都在下一阶段被作废。

**阶段 3：事务性重载。** 最后，引擎重载过期条目。它作废 accepted 模块的缓存³，备份每个被移除的模块以便回滚，然后按 url 重新导入每个过期条目的组件模块，并换入一个全新 fiber：

```
算法 9  事务性模块重载
1   function reload(ctx, accepted, stale_entries)
2     backup ← invalidate_caches(accepted)
3     try
4       for entry in stale_entries do
5         entry.fiber.dispose()
6         entry.fiber ← ctx.use(import(entry.url), entry.config)
7     catch error
8       restore_caches(backup)
9       for entry in stale_entries do
10        entry.fiber.dispose()
11        entry.fiber ← ctx.use(backup[entry.url], entry.config)
12      throw error
```

³在 Node.js 上，这意味着同时清除 ES 模块与 CommonJS 模块系统的缓存，因为通过 ES 加载器导入的模块可能同时出现在两者中。

事务性保证确保系统从不进入半重载状态：若有任何模块导入失败（例如由于语法错误），缓存被恢复，每个过期条目都从 backup[entry.url]（缓存刚被恢复的先前组件）重建，撤销已经完成的交换。

### 4.3. 案例研究：Koishi

Koishi 是一个构建于 Cordis 之上的开源聊天机器人应用框架⁴。经过四年的开发，它已积累了 4000 多个社区贡献的插件⁵，涵盖即时通信（IM）适配器、数据库驱动、管理控制台与终端用户功能。其规模与多样性使其成为 Cordis 在生产环境中动态可组合性的代表性验证。

**元框架的表达力与通用性。** Koishi 作为一个服务端机器人运行，其每一项能力都是在第 4.1 节的上下文原语之上实现的一个插件；Koishi 本身只贡献聊天机器人领域的词汇。同样的模型在一个完全不同的运行时中再次出现：Koishi 的 Web 控制台是第二个、独立的 Cordis 应用，其插件组合的是浏览器与其用户界面的原语，而非服务端的原语。上述迥异的场景确立了第 3 节模型的两个性质。(1) 它是**富有表达力**的：其原语足以承载一个完整的生产系统，宿主框架仅提供领域词汇。(2) 它是**通用**的：它固定了效应与余效应如何组合，而把它们的含义留给每个应用，因而既不预设特定领域，也不预设特定运行时。

**无认知负担的时间可组合性。** 第 1.2.1 节所调查的插件系统无法在不重启扩展宿主的情况下卸载单个扩展的效应。Koishi 例行地执行这一操作：编排者从控制台禁用某个插件，其效应即被就地撤销；在开发期间，HMR 引擎在保存时重新应用编辑过的插件，同时保留系统其他部分的缓存状态与实时连接。Cordis 使这种移除不仅成为可能，而且对插件作者而言毫不费力。由于经由上下文执行的效应被追踪、其逆被自动复合（第 3.1 节），即使缺乏经验的作者也能获得插件上下文介导效应的有序清理，而无需编写卸载路径。这实现了第 1.2.1 节所指出缺失的关注局部性：原本依赖每位作者勤勉的正确性，改由抽象一次性消解。

**开放生态系统中的空间可组合性。** 与第 1.2.1 节中插件间依赖基本缺失的插件系统相比，Koishi 的生态系统展现出真正的依赖拓扑：IM 适配器提供对各个消息平台的访问，数据库驱动提供持久化存储，功能插件把这些声明为余效应并访问它们。在运行时重新配置提供者——例如切换存储后端或重连适配器——只重新激活解析依赖发生变化的那部分依赖方（第 3.2 节）；依赖不可用的插件保持不活跃直到依赖出现，而不会报错。案例研究所证实的是，这种组合在独立编写的代码之间依然成立：一个插件与其依赖通常由不同作者编写，他们除了连接彼此的余效应之外无需任何协调，因此响应式余效应使装配在由独立贡献者构成的开放生态系统中保持一致。

**有效性的威胁。** 这里的证据取自单一宿主语言中的单一生态系统，因此无法把范式的优点与其 TypeScript 实现、或 Koishi 特定领域的优点分离开来；而且它是观察性的，而非针对替代架构的受控对比。因此案例研究所确立的是"存在性与采用"的结果，而非定量结果；衡量该抽象的开销及其相对基线对开发者生产力的影响，仍是未来的工作。

⁴Koishi 目前使用 Cordis v3。本文介绍的是 Cordis v4，它细化了效应与余效应语义并重新设计了加载器；核心组合模型在两个版本间是共享的。
⁵Koishi 使用术语"插件"（plugin）指称本文形式化为"组件"（component）的概念。

---

## 5. 讨论

前面几节给出的形式化模型与实现引入了一种面向动态可组合性的编程范式。本节考察该范式如何扩展到更广泛的工程关切，并讨论设计张力与开放问题。

### 5.1. 服务多路复用

诸如 OSGi [44] 之类的动态组件平台围绕**服务**组织组合：服务是能力单元，由提供者在某个接口下发布、由消费者绑定。Cordis 的余效应模型呼应了这一概念，其中服务对应于某个键背后的接口。提供某服务的组件是其**提供者**，注入某服务的组件是其**消费者**。单个服务可以由多个提供者实现，而这种多重性可以以两种形式实现。(1) **独占绑定**：多个实现共享一个接口，但任一时点至多绑定一个；编排者选择绑定哪个实现，在它们之间切换需要卸载一个提供者、加载另一个，从而瞬时扰动每个消费者的依赖。(2) **服务代理**（service broker）：一个作为接口入口点的中心服务被后端提供者与消费者共同注入，因此多个提供者共存，代理把每个请求分发到它们之间。与独占绑定相比，代理吸收了这种扰动：更新一个后端提供者时代理原地不动，因此消费者看不到其依赖的任何变化，也就不会触发重载。

服务代理支撑三种能力：负载均衡、滚动更新与跨进程调用。

**负载均衡。** 当多个提供者共存时，代理根据可配置的策略（例如轮询、最小负载、延迟加权）或消费者显式指定的目标在它们之间分发请求。由于提供者是普通组件，它们可以被增删以伸缩容量；每个提供者通过一个可逆效应向代理注册，因此卸载它会逆转该注册，并自动把它从代理的路由集中移除。

**滚动更新。** 在运行时升级服务实现归结为一次受控的提供者转移 [45, 46]。为了施行该转移，新提供者作为一个额外 fiber 被加载并向代理注册；一旦它变为 ACTIVE，流量就从旧提供者逐渐切换到新提供者（例如通过调整选择权重），旧提供者在不承载在途请求后被卸载。这种提供者转移把传统上是基础设施级的操作（例如容器编排、蓝绿部署）转化为应用级的组合模式。

**跨进程调用。** 服务代理也可以跨进程边界应用 [47]。每个进程承载自己的 Cordis 上下文与本地提供者；一个协调组件把它们连接起来，把每个进程视为一个远程提供者。跨进程服务访问由保持接口的 RPC 机制介导，使分布对消费者透明。一个需要注意之处是，跨进程调用会产生延迟并可能中途失败，因此同步地暴露它会阻塞调用方。因此，一个打算跨进程暴露的接口必须针对异步契约设计。

### 5.2. 访问控制与沙箱化

给定一个由独立组件装配的应用，保护该应用需要两种互补机制：(1) 约束组件可以访问哪些依赖，以及 (2) 把不可信代码与宿主环境隔离开来。Cordis 通过依赖声明与拦截支持第一种；第二种则需要外部的隔离边界。

**基于能力的访问控制。** 依赖访问机制（第 4.1.4 节）本身已经构成一种对代理介导属性的访问控制形式：组件只能访问它已声明的依赖；未声明的访问会引发错误。这在结构上类似于基于能力的安全 [48–50]，其中权限由持有引用而非环境权威（ambient authority）赋予。inject 声明充当能力请求，上下文代理充当能力介导者。由于这些请求是静态声明的，组件所需的代理介导能力的完整集合在它运行之前就已可知，让编排者在加载时审查并批准它们，而非在访问发生时才发现。

这种介导通过拦截机制推广为细粒度策略。访问控制元数据可以由上下文携带或由组件声明（定义 18），提供者在依赖被调用时查询它，以决定某请求是否被允许。例如，一个文件系统依赖可以携带声明组件可读写哪些路径的元数据，提供者把每次调用对该元数据进行核对。由于这种拦截位于上下文之上，而非任一方的代码之中，编排者可以调整它来约束任何组件对某依赖的访问，而无需修改提供者——例如授予某个社区组件只读的数据库访问权，而核心组件保留完整访问权。此外，由于拦截只影响依赖如何被调用，而非它是否被满足，它可以在运行时被安装、重新配置或移除，而不触发任何重载或扰动依赖图。

**沙箱化不可信组件。** 当组件的代码不可信时，语言级的访问控制是不够的，因为一个能访问宿主运行时的恶意组件可以直接触及底层对象，使这些检查形同虚设。真正的隔离需要一个超出语言级手段可及范围的执行边界，例如软件故障隔离 [51]、隔离的语言运行时、沙箱化进程或虚拟化容器 [52]。无论采用何种机制，不可信组件都在其自己的隔离上下文中运行，并通过一个桥接器访问宿主提供的依赖——这是第 5.1 节跨进程调用的推广：同样的透明性论证使这种桥接访问与本地注入无法区分。在宿主侧，桥接器是一个普通 fiber，其能力可以被上述访问控制所衰减。

### 5.3. 语言无关性与语言选择

尽管 Cordis 以 TypeScript 实现，上下文范式是语言无关的：时空可组合性仅由其两个可组合性维度定义，因而可以在任何满足两个维度上特定需求的语言中实现。我们依次分析每个维度的需求。

**时间可组合性。** 最基本地，时间可组合性要求**闭包**：一个可逆效应把一个动作与一个逆配对，而该逆必须作为值被捕获，连同它所恢复的状态，以便在拆除时重放。此外，组件的代码与加载它的副作用必须能够在运行时被引入与撤销。

语言如何满足第二个需求取决于其执行模型。在托管运行时中，这表现为一个可编程的模块注册表，已加载的模块在被解除引用后可以从注册表中逐出并被垃圾回收；例如 Node.js 就暴露了这样的注册表⁶。原生代码不暴露模块注册表，因此引入与撤销表现为显式的动态链接与解除链接（例如 Unix 上的 dlopen/dlclose、Windows 上的 LoadLibrary/FreeLibrary）[53]，即把目标代码加载进运行中的进程、之后再分离它。WebAssembly 依据其嵌入器走其中一条路：在托管嵌入器（如 JavaScript 宿主）下，模块实例由宿主的收集器回收；在原生嵌入器（如 Wasmtime）丢弃它时被释放。跨这些机制，可逆效应模型把加载视为上下文上的一个效应，其逆撤销该模块引入的符号、类型或处理器的注册。

**空间可组合性。** 空间可组合性要求一种机制，让组件声明其依赖，并让运行时提供与注入这些依赖。这归结为一个依赖注入（DI）问题 [39]，它在不同语言间表现为两个层次：依赖如何被类型化，以及依赖访问如何被介导。

在**类型层次**，语言应当提供一种方式让开发者表达良类型的依赖访问。消费者通过从上下文读取其键来获取余效应，因此上下文类型（第 3.2.1 节）必须记录每个键的余效应。类型类（Haskell）[54] 与 trait（Rust）[55] 通过让提供者从其自己的模块以 instance 或 impl [56] 扩展上下文类型来实现这一点。TypeScript 的模块扩充（module augmentation）[57] 同样让提供者模块把声明合并进上下文类型。

在**运行时层次**，依赖访问必须被动态介导：某个键背后的余效应可能随提供者的加载与卸载而变化，并可能在不同上下文间解析出不同结果。因此，语言需要一种透明地拦截访问的方式，使消费者代码保持不变，例如通过 JavaScript 的 Proxy 对象 [58] 或 Python 的描述符协议（__get__）[59]。缺少这种原语时，运行时反射 [60, 61] 可以动态介导访问，代价是类型安全与开发者体验。

跨这两个层次，元编程设施一起提供类型化与介导。注解 [62] 与装饰器把元数据附加到声明上，由处理器展开为介导访问的访问器；编译期元编程（例如 Rust 过程宏、Scala 宏 [63]、Zig comptime）为每个依赖产出一个带类型的声明以及这样的访问器，从而省去通用拦截原语。

⁶CommonJS 通过 require.cache 暴露模块缓存；ES 模块不提供公开的逐出 API，尽管模块仍可通过引擎内部接口管理。

### 5.4. 相互依赖与组件粒度

在响应式余效应模型中，依赖环只是让涉及的组件永久处于不活跃状态：给定组件 A 与 B，若 A 需要 B 提供的键、B 需要 A 提供的键，则二者的满足性谓词都永远不可能为真。与并发系统中的死锁不同，这种状态是静态可预测的（仅凭依赖声明），且不产生运行时错误；它是静默的非激活，而非失败。

在实践中，大多数表面上的相互依赖都可以分解为更细粒度的组件来消除环。考虑两个组件：一个服务器（提供网络接口）与一个访问控制器（执行授权策略）。这两个组件双向交互：访问控制器介导到达服务器的请求，服务器暴露修改访问控制策略的端点。单体式设计会让每个组件依赖另一个。然而，两个交互方向在逻辑上是独立的关切。把它们分解得到四个组件：server-core、access-control-core、request-mediation（依赖两个 core，对进入的请求应用访问控制）以及 policy-management（依赖两个 core，通过服务器暴露策略修改）。通过这种方法，环被消除，因为两个 core 互不依赖；只有集成组件依赖二者。

这种分解原则上总是可行，因为每个双向交互都可以分解为独立的单向绑定，但它增加了组件数量：在一般情况下，给定 n 个相互交互的组件，集成组件的数量可能随 n 二次增长，因为每对交互组件可能为每个交互方向都需要一个独立组件。这不影响正确性或运行时性能（组件是轻量的），而更细的粒度可能是有益的：用户获得仅加载其所需特定集成绑定的能力，实际上提高了系统的可组合性。然而，它可能影响开发者体验：更多组件需要更多配置、更多命名，以及理解依赖图时更多的认知负担。

缓解这种粒度成本是工程关切而非理论关切。实用策略包括**包捆绑**（把相关细粒度组件分组为单个可安装单元）、**基于约定的接线**（自动连接名称或类型匹配某模式的组件）与**脚手架工具**（从声明式规范生成样板集成组件）。这些策略在减少创作负担到接近单体式情形的同时，保留了无环模型的形式化保证。

### 5.5. 依赖的类型化与版本化

在形式化模型中，依赖链接纯粹由**键的同一性**建立：提供键 k 的组件满足任何在其依赖集合中声明 k 的组件。类型族 𝒱k 保证了单个编译单元内的类型级一致，但当组件被独立开发与构建时——这是组件生态系统中常见的场景——这一保证被打破。这种破坏导致两个不同的问题。

**接口漂移。** 提供者可能在版本之间修改与 k 相关联的接口（增删字段、改变方法签名、改变行为契约），而针对早期接口编译的消费者仍继续声明同一个键 k。依赖在余效应层次被满足（k ∈ dom(σ)），但运行时值不再符合消费者的期望，导致类型错误、方法未找到的失败或静默的行为分歧 [64]。

**键冲突。** 两个独立开发的提供者可能使用同一个键名 k 表示完全不相关的接口。由于仅凭键的同一性就建立链接，期望某个提供者接口的消费者会在没有任何兼容性检查的情况下接受另一个提供者的值。与接口漂移（其中提供者与消费者至少共享共同血统）不同，键冲突中期望类型与实际类型之间毫无关系，使由此产生的失败不可预测且难以诊断。

这两个问题指向同一个缺口：余效应模型只提供**名义链接**（按键名），而没有版本化或结构化的链接（按接口兼容性）[65]。我们讨论弥合该缺口的三种方法，从最耦合于基础设施到最语言无关。

**键命名空间化。** 把键空间从 K 扩展到 K × P（其中 P 标识定义接口的包），即可由构造消除键冲突：独立开发的、本地名称相同的接口占据不同的键。这是最直接的解决方案，但也是最耦合的：它把包命名空间嵌入形式化模型本身，使系统依赖外部包注册表来确立键的同一性。

**对等依赖（peer dependencies）。** 更轻的耦合是通过宿主语言的包管理器声明版本约束 [66]。这是 Cordis 目前采用的方法。组件依赖在语义上是对等依赖：组件不把其依赖打包在内部，而是期望运行时上下文提供它们。支持对等依赖的包管理器（如 npm）可以强制版本兼容：若提供某键的包的版本超出消费者声明的对等范围，不兼容在安装时即被捕获，而非表现为运行时失败。然而，这种方法有两个局限：(1) 它依赖提供者忠实地遵循语义化版本，而这是一种不可强制执行的约定；(2) 包管理器通常把每个依赖解析到单一版本，这妨碍了在单个应用内从同一包的多个版本加载组件。

**结构化兼容。** 一种完全语言无关的方法会把成员检查 k ∈ dom(σ) 替换为一个兼容性谓词，验证提供者的实际接口在结构上涵盖消费者的期望。这类似于结构子类型 [67]：若提供的接口是所需接口的子类型，则提供者满足消费者。挑战在于语言无关地定义该谓词：结构化兼容对记录类型（宽度子类型）是直接的，但对行为契约（例如前置/后置条件 [68]、效应规范 [21]）变得复杂，而一旦参数多态引入有界量化 [69] 就不可判定。

这三种方法处理问题的不同侧面。设计一个统一的依赖模型，把这些方法结合起来，同时保留余效应模型的动态组合保证，仍是一个开放问题。

---

## 6. 相关工作

动态可组合性与多个既有研究领域相交。我们综述最相关的几条工作线，并把我们的贡献与它们各自区分开来。

### 6.1. 效应与余效应系统

第 2 节回顾了作为我们工作理论支柱的效应与余效应。有三条线把它们扩展到与 Cordis 相关的方向：把效应重铸为上下文必须提供的能力，赋予效应可逆而非解释性的语义，以及在单一分级纪律下统一效应与余效应。

**作为能力的代数效应。** 代数效应（第 2.1 节）使效应操作对类型系统可见。与我们工作最接近的扩展是 Brachthäuser 等人的 Effekt 语言，它把效应类型重新解释为能力 [70, 71]：一个效应类型表达的是计算从其上下文**需要**什么，而非它可能产生什么副作用。同样的重解释已进入主流实践：Effect-TS 库 [72] 把效应的上下文需求记录在专用类型参数中，使计算的类型声明其外围上下文必须提供哪些服务；与 Effekt 一样，它通过对静态类型的解释而非逆转来消解效应。这一视角与我们一样，把上下文视为能力的介导者。Cordis 与 Effekt 在两方面不同。在**目的**上，代数效应使效应可见以实现模块化解释，让一个操作具有多种处理器语义；而 Cordis 使其可见以实现追踪与逆转，为每次上下文变换配对一个逆。在**场景**上，Effekt 在类型层静态地约束效应，默认采用基于作用域的推理，其中能力是二等公民、被限制在其词法作用域内，并通过装箱（boxing）恢复一等使用——装箱通过在类型中追踪被捕获的能力来解除该限制；Cordis 则在运行时约束效应，旨在实现组件移除时的完整资源恢复。

**可逆效应语义。** 一条并行的线赋予效应可逆而非解释性的语义。Heunen 等人 [73] 通过把 Hughes 的箭头改编为 dagger 箭头与逆箭头，在可逆场景下建模副作用，捕获序列化与可变存储等其操作允许逆的效应。这是与我们可逆效应最接近的形式化阐述：二者都为每个效应配对一个逆，而非通过处理器消解它。二者在可逆性的**所在之处**不同。Heunen 等人工作在指称的、范畴化的场景下，其中可逆性是全局性质，由构造保证——因为每个计算都是可逆的，逆从范畴结构中恢复出来。Cordis 则在运行时追踪逆，不要求整个计算可逆，而只要求每个原子效应允许一个逆，任何复合的逆由复合运算导出（第 3.1 节）。

**作为统一效应与余效应的分级类型。** Orchard 等人 [74] 提出分级模态类型，作为涵盖效应推理（通过分级单子）与余效应推理（通过分级余单子）的伞状概念，并在 Granule 语言中实现，证明单一类型系统可以同时追踪计算做了什么与需要什么；更近的工作把余效应扩展到类 Java 的命令式语言 [75, 76] 与按值调用（call-by-push-value）[77]。所有这些都在类型层运作：效应与余效应是编译期对词法固定作用域检查的静态标注。我们的贡献与该分析正交：我们把同样的两个概念提升为运行时机制，使 Cordis 能够处理动态组合。时间撤销与空间依赖随着已加载组件集合的演化而被重新解析，而非在固定程序文本上一次性解决。

### 6.2. 编程范式

第 3.4.2 节把上下文范式确立为一种通过显式上下文介导效应与余效应的纪律。有两个既有范式值得显式比较：一个与我们共享术语，另一个与我们共享对横切关切的处理。

**上下文导向编程（COP）。** COP [78, 79] 为语言配备**层**（layers）——根据执行上下文在运行时激活与去激活的部分方法与类定义，使行为适应变化而基代码无需命名其上下文依赖 [80]。COP 与 Cordis 在把上下文视为一等、运行时可变实体，以及动态激活与去激活行为上是一致的，但这种相似只是名义上的。在 COP 中，"上下文"指代环境性的执行情境（例如位置、用户、模式），激活在动态作用域范围（extent）内改变方法分派；层既追踪不到它引发的副作用、也不逆转它们，且激活不受依赖满足性支配。在 Cordis 中，上下文是介导效应与余效应的 Γ∞ 实体：激活运行组件的可逆效应、由响应式余效应的满足性驱动（第 3.2 节），去激活则完整逆转它们。COP 改变的是运行什么行为；Cordis 组合与逆转的是组件安装了什么效应与依赖。它们的差异是一种权衡。COP 把激活折叠进宿主语言的方法分派，以语言特化性为代价获得动态作用域的层范围；而 Cordis 作为语言无关的覆盖层，在共享上下文上响应式地解析激活。因此，Cordis 只能把 COP 的全局、值驱动的那部分表达为余效应：实现间的上下文相关选择，而非动态作用域的激活。

**面向切面编程（AOP）。** AOP [81, 82] 把横切关切模块化为一个**切面**（aspect）：一个量化基程序中所选连接点（join point）的切入点（pointcut），以及在每个连接点织入的建议（advice）。Cordis 处理同样的、否则会散布于组件之间的情境行为问题，但它的切面类比物是一个**余效应**：许多组件声明依赖的一个共享介导点，因此横切行为可以在那里被重塑而无需编辑其中任何一个。两种范式随后在两个轴上不同。(1) **声明 vs. 无察觉**：AOP 切入点是无察觉且量化的，匹配任意连接点，其代码并不知道自己被建议；而 Cordis 把横切限制在每个组件声明的余效应上，因此其触及范围正是那个被声明的表面。这带来确定性与可追踪性：应用编排者可以在配置层检查与治理某个组件被什么横切，而无需阅读或分析其源代码；而 AOP 关切只能通过量化它的那些切面来解读。(2) **生命周期整合**：Cordis 中的横切变化由组件的效应承载，在组件卸载时被逆转，并响应式地传播给其依赖方，因此它是动态组合模型内的一次移动；动态 AOP 系统 [83, 84] 也可以在运行时织入与解除织入，但那是作为独立操作，既不绑定于组件生命周期，也不在被建议代码之间触发重新解析。

### 6.3. 时间可组合性

时间可组合性关乎在运行中的程序里替换或移除组件，同时恢复它所安装的效应。先前的方法按它们如何处理离场组件的状态与效应来划分：把状态前移到后继版本，通过开发者编写的清理来恢复效应，或在预先固定的作用域内自动逆转效应。

**有状态前向迁移。** 一个庞大的系统家族通过把组件状态跨版本前移，在不中断服务的情况下替换运行程序中的组件。它们都遵循同样的时序纪律：组件只有在到达一个安全的、无交互的点时才能被交换。Kramer 与 Magee 把这一准则确立为**静止**（quiescence）[45]，Vandewoude 等人后来把它放宽为扰动更小的**宁静**（tranquility）[46]；我们的滚动更新模式（第 5.1 节）通过在卸载提供者前排空在途请求来执行它。动态软件更新（DSU）随后通过手写的变换函数前移状态：Hicks 等人的通用 C 语言 DSU [85]、Stoyle 等人通过 con-freeness 分析实现的类型安全更新点 [86]，以及 Hayden 等人的 Kitsune [87]，都把旧版本数据映射到新版本表示，就地继承堆对象、打开的文件与连接，同时重新初始化未被迁移的部分。同样的纪律扩展到持久化状态：Overeem 等人 [88] 通过手写的升级操作在保持系统可用的同时，把一个运行中事件存储的数据在模式版本之间转换。Erlang/OTP [14] 在进程层采取同样的立场，通过 code_change/3 迁移状态，并通过重启受监督进程而非逆转其效应来从故障中恢复；JavaScript 的热模块替换（如 webpack [42]、Vite [43]）在模块层做同样的事，通过 module.hot 或 import.meta.hot API 跨重载前移状态。与 Cordis 的模块替换（第 4.2 节）相比，这些方法更优雅地迁移内存状态：Cordis 逆转旧组件被追踪的效应并从干净的起点重新应用新组件的效应，因此组件自身的内存状态除非置于更长生命周期的依赖中，否则无法在重载后存活；在可逆效应之上叠加 DSU 式的前向迁移是未来工作。尽管如此，Cordis 的方法在两方面更通用：它不需要 DSU 与 HMR 所需的手写迁移函数，且它支持完整卸载组件并恢复其资源，而非仅就地更新。

**开发者编写的恢复。** 第二个家族通过开发者手写的清理或补偿逻辑恢复组件的效应。插件生命周期约定（如 OSGi [44]、Eclipse 的扩展点、IntelliJ 与 VSCode）把清理委托给开发者编写的卸载回调；Command 模式 [89] 把操作连同用于撤销/重做栈的 undo 方法封装在一起；saga 模型 [37] 把长事务结构化为每一步都配对一个补偿动作；代数效应处理器可以附加在拆除时运行的终结器 [90]；事件溯源 [91] 通过追加补偿事件而非执行真正逆来撤销状态。在所有这些中，逆都是一项**不被强制执行的义务**，与操作解耦，因此被遗忘的逆会静默泄漏资源（如第 1.2.1 节经验性地记录的那样）。React 的 useEffect 钩子 [92] 最接近结构性地为效应配对其逆：它返回一个清理函数，运行时在每次重新执行前与卸载时调用它。它的短板在于**可组合性**：钩子只能在组件或另一个钩子的顶层调用，绝不能在条件、循环或嵌套函数内调用，且其效应体既不接受异步函数也不接受迭代器。因此，效应无法由其他效应组装，也无法与控制流交织，从而没有可以导出复合逆的基础。Cordis 效应没有这样的限制：它们是自由组合、可异步运行的普通操作，且只要求每个原子效应有手写的逆，任何复合的逆由复合运算导出，因此组装既有效应完全无需编写逆。这种"每个效应与其逆结构性配对"的做法使完整恢复成为系统的**不变式**，而非开发者自律的问题。

**静态作用域逆转。** 第三个家族由构造自动逆转效应，但把逆转限制在预先固定的作用域内。软件事务内存 [93, 94]（源自硬件事务内存 [95]）记录一个读/写日志，使一组内存操作要么提交要么中止，把内存回滚到事务前状态。可逆计算——从 Landauer 与 Bennett 的热力学分析 [96, 97] 到 Janus [98] 等可逆语言——走得更远，使整个计算的每一步都全局可逆。线性类型 [99]、RAII [4] 与 Rust 的所有权系统 [55] 把资源的释放绑定到一个词法区域。它们各自都静态地固定逆转的作用域与触及范围；Cordis 则相反，不预先固定这种作用域：它在一个组件的生命周期内逆转任意的上下文操作，并把词法资源管理视为互补的——适用于单个组件内的局部资源。

### 6.4. 空间可组合性

空间可组合性关乎组件对他人的依赖如何被声明与绑定。先前的机制按绑定如何响应变化来划分：初始化时一次性接线依赖，响应整个组件的可用性，或以单个值的粒度传播变化。

**初始化时依赖接线。** 两个既有机制在初始化时把组件接线在一起。依赖注入框架 [39]（如 Spring [100]、Guice、Angular、Inversify）在初始化时把依赖注入组件，UI 框架上下文（如 Vue.js 的 provide/inject 与 React 的 Context API）沿组件树传递依赖。有些支持动态作用域（如 Spring 的 prototype/request 作用域、Angular 的层次化注入器），但都不响应式地重新解析：当提供者在运行时被替换或撤销时，既有依赖方既不去激活也不重新初始化，且没有一个提供我们组件状态机那种生命周期管理。Cordis 的响应式余效应（第 3.2 节）提供了这一点：通知机制在满足性谓词变化时触发生命周期转移。

**可用性响应的组件模型。** 与我们响应式余效应最接近的先例响应服务的可用性。OSGi 的声明式服务（Declarative Services）与 iPOJO [101, 102] 让组件声明提供的与所需的服务，运行时随服务的出现与消失自动激活与去激活它们；iPOJO 的 Gravity 项目 [102] 明确面向对变化服务可用性的自治运行时适应，其 provide/require 模型直接预示了 Cordis 的 ctx.provide/ctx.get 模式。R-OSGi [47] 通过 RPC 把同样的抽象透明地扩展到分布式场景，把网络故障映射为服务撤销事件——第 5.1 节把这一模式作为 Cordis 模型的扩展来讨论。所有这些系统都通过去激活回调来恢复，而这在两方面受限。第一，回调是手写的，因此资源安全依赖开发者自律，被遗忘的回调会静默泄漏。第二，回调是同步的：若拆除需要与正在离场的依赖做异步交换，这些框架不提供等待它的协议，只能对一个可能已经过期的引用做阻塞等待。Cordis 的响应式余效应弥合了这两个缺口：去激活逆转依赖方累积的效应，而其惯性 UNLOAD 状态（第 3.3.4 节）在响应进一步变化之前把异步拆除运行至完成。

**值级响应性。** 函数式响应式编程（FRP）[103] 及其现代化身（如 SolidJS 中的信号 [104, 105]、Vue 的响应式系统、Angular Signals）以值级粒度传播变化：当信号变化时，派生计算被同步地或在调度器 [106] 下重新求值。Cordis 的响应式余效应以组件级粒度运作，增加了值级传播所不建模的异步生命周期语义。二者是互补而非竞争的：一个 Cordis 余效应本身可以携带响应式值，组件只针对它实际消费的部分更新，从而把组件级响应性细化为跨越两个层次的更细粒度响应式余效应。

---

## 7. 结论

我们通过把效应与余效应这两个类型论概念提升为运行时机制，给出了动态可组合性的形式化基础。**可逆效应**通过为上下文变换配备显式逆、并证明效应追踪保持组合运算来处理时间可组合性，从而保证组件移除时状态的完整恢复。**响应式余效应**通过形式化带类型依赖上下文（含基于满足性的通知、余效应隔离与余效应拦截）来处理空间可组合性。一个带有面向多样化控制流的正交扩展的组件生命周期模型，把两种机制的交互操作化。它们共同产生一个统一的上下文类型，把效应上下文与余效应上下文整合为一个自洽的编程范式。

我们把该范式实现为 Cordis 元框架，其核心库提供效应追踪与余效应解析，以及一个带有配置对账与热模块替换的声明式组件加载器。Koishi 案例研究在一个拥有 4000 多个社区插件的生产系统中验证了 Cordis 的设计。

在人工策展的插件生态系统之外，未来验证的一个引人注目的方向是**自我进化的智能体运行框架**（第 1.2.2 节），其中 AI 智能体持续生成并替换其自身的运行框架组件，且几乎无人监督。在这样的场景中应用 Cordis，将验证快速组件替换下完整恢复的时间保证，以及频繁拓扑变化下依赖协调的空间保证。这种验证将证明该范式作为智能体运行框架与其他自治系统中可恢复、可协调、持续自我进化的基础的适用性。

---

## 参考文献

（参考文献按原文保留，不作翻译。）

[1] D. L. Parnas, "On the criteria to be used in decomposing systems into modules," Communications of the ACM, vol. 15, no. 12, pp. 1053–1058, 1972, doi: 10.1145/361598.361623.

[2] D. Birsan, "On Plug-ins and Extensible Architectures," ACM Queue, vol. 3, no. 2, pp. 40–46, 2005, doi: 10.1145/1053331.1053345.

[3] B. Burns, B. Grant, D. Oppenheimer, E. Brewer, and J. Wilkes, "Borg, Omega, and Kubernetes," Communications of the ACM, vol. 59, no. 5, pp. 50–57, 2016, doi: 10.1145/2890784.

[4] B. Stroustrup, The Design and Evolution of C++. Addison-Wesley, 1994.

[5] S. Marlow, S. Peyton Jones, A. Moran, and J. Reppy, "Asynchronous Exceptions in Haskell," in Proceedings of the ACM SIGPLAN 2001 Conference on Programming Language Design and Implementation, in PLDI '01. New York, NY, USA: Association for Computing Machinery, 2001, pp. 274–285. doi: 10.1145/378795.378858.

[6] C. Szyperski, Component Software: Beyond Object-Oriented Programming, 2nd ed. Addison-Wesley, 2002.

[7] R. Lopopolo, "Harness Engineering: Leveraging Codex in an Agent-First World." [Online]. Available: https://openai.com/index/harness-engineering/

[8] Anthropic, "Harness Design for Long-Running Application Development." [Online]. Available: https://www.anthropic.com/engineering/harness-design-long-running-apps

[9] L. Wang et al., "A Survey on Large Language Model Based Autonomous Agents," Frontiers of Computer Science, vol. 18, no. 6, p. 186345, 2024, doi: 10.1007/s11704-024-40231-1.

[10] Y. Qin et al., "Tool Learning with Foundation Models," ACM Computing Surveys, 2025, doi: 10.1145/3704435.

[11] C. Packer, V. Fang, S. G. Patil, K. Lin, S. Wooders, and J. E. Gonzalez, "MemGPT: Towards LLMs as Operating Systems," CoRR, 2023.

[12] T. Guo et al., "Large Language Model Based Multi-Agents: A Survey of Progress and Challenges," in Proceedings of the Thirty-Third International Joint Conference on Artificial Intelligence, in IJCAI 2024. 2024, pp. 8048–8057. doi: 10.24963/ijcai.2024/890.

[13] T. Cai, X. Wang, T. Ma, X. Chen, and D. Zhou, "Large Language Models as Tool Makers," in Proceedings of the Twelfth International Conference on Learning Representations, in ICLR 2024. 2024.

[14] J. Armstrong, "Making Reliable Distributed Systems in the Presence of Software Errors," Doctoral dissertation, 2003.

[15] E. Moggi, "Notions of computation and monads," Information and Computation, vol. 93, no. 1, pp. 55–92, 1991, doi: 10.1016/0890-5401(91)90052-4.

[16] G. Plotkin and J. Power, "Adequacy for Algebraic Effects," in Foundations of Software Science and Computation Structures, F. Honsell and M. Miculan, Eds., Berlin, Heidelberg: Springer Berlin Heidelberg, 2001, pp. 1–24.

[17] T. Petricek, D. Orchard, and A. Mycroft, "Coeffects: unified static analysis of context-dependence," in Proceedings of the 40th International Conference on Automata, Languages, and Programming - Volume Part II, in ICALP'13. Riga, Latvia: Springer-Verlag, 2013, pp. 385–397. doi: 10.1007/978-3-642-39212-2_35.

[18] M. Gaboardi, S.-y. Katsumata, D. Orchard, F. Breuvart, and T. Uustalu, "Combining effects and coeffects via grading," in Proceedings of the 21st ACM SIGPLAN International Conference on Functional Programming, in ICFP 2016. Nara, Japan: Association for Computing Machinery, 2016, pp. 476–489. doi: 10.1145/2951913.2951939.

[19] A. Church, "A Formulation of the Simple Theory of Types," The Journal of Symbolic Logic, vol. 5, no. 2, pp. 56–68, 1940, doi: 10.2307/2266170.

[20] B. C. Pierce, Types and Programming Languages. MIT Press, 2002.

[21] J. M. Lucassen and D. K. Gifford, "Polymorphic Effect Systems," in Proceedings of the 15th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages, in POPL '88. San Diego, California, USA: Association for Computing Machinery, 1988, pp. 47–57. doi: 10.1145/73560.73564.

[22] P. Wadler, "Monads for functional programming," in Program Design Calculi, M. Broy, Ed., Berlin, Heidelberg: Springer Berlin Heidelberg, 1993, pp. 233–264.

[23] G. Plotkin and J. Power, "Notions of Computation Determine Monads," in Foundations of Software Science and Computation Structures, Berlin, Heidelberg: Springer Berlin Heidelberg, 2002, pp. 342–356. doi: 10.1007/3-540-45931-6_24.

[24] G. Plotkin and M. Pretnar, "Handlers of Algebraic Effects," in Programming Languages and Systems (ESOP), Berlin, Heidelberg: Springer Berlin Heidelberg, 2009, pp. 80–94. doi: 10.1007/978-3-642-00590-9_7.

[25] M. Pretnar, "An Introduction to Algebraic Effects and Handlers. Invited tutorial paper," Electron. Notes Theor. Comput. Sci., vol. 319, no. C, pp. 19–35, Dec. 2015, doi: 10.1016/j.entcs.2015.12.003.

[26] D. Leijen, "Koka: Programming with Row Polymorphic Effect Types," Electronic Proceedings in Theoretical Computer Science, vol. 153, pp. 100–126, Jun. 2014, doi: 10.4204/eptcs.153.8.

[27] D. Leijen, "Type directed compilation of row-typed algebraic effects," in Proceedings of the 44th ACM SIGPLAN Symposium on Principles of Programming Languages, in POPL '17. Paris, France: Association for Computing Machinery, 2017, pp. 486–499. doi: 10.1145/3009837.3009872.

[28] A. Bauer and M. Pretnar, "Programming with algebraic effects and handlers," Journal of Logical and Algebraic Methods in Programming, vol. 84, no. 1, pp. 108–123, Jan. 2015, doi: 10.1016/j.jlamp.2014.02.001.

[29] K. Sivaramakrishnan et al., "Retrofitting parallelism onto OCaml," Proc. ACM Program. Lang., vol. 4, no. ICFP, Aug. 2020, doi: 10.1145/3408995.

[30] T. Petricek, D. Orchard, and A. Mycroft, "Coeffects: a calculus of context-dependent computation," in Proceedings of the 19th ACM SIGPLAN International Conference on Functional Programming, in ICFP '14. Gothenburg, Sweden: Association for Computing Machinery, 2014, pp. 123–135. doi: 10.1145/2628136.2628160.

[31] T. Uustalu and V. Vene, "Comonadic Notions of Computation," Electronic Notes in Theoretical Computer Science, vol. 203, no. 5, pp. 263–284, 2008, doi: 10.1016/j.entcs.2008.05.029.

[32] A. Brunel, M. Gaboardi, D. Mazza, and S. Zdancewic, "A Core Quantitative Coeffect Calculus," in Proceedings of the 23rd European Symposium on Programming Languages and Systems - Volume 8410, Berlin, Heidelberg: Springer-Verlag, 2014, pp. 351–370. doi: 10.1007/978-3-642-54833-8_19.

[33] J. Reed and B. C. Pierce, "Distance makes the types grow stronger: a calculus for differential privacy," SIGPLAN Not., vol. 45, no. 9, pp. 157–168, Sep. 2010, doi: 10.1145/1932681.1863568.

[34] M. Abadi, A. Banerjee, N. Heintze, and J. G. Riecke, "A core calculus of dependency," in Proceedings of the 26th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages, in POPL '99. San Antonio, Texas, USA: Association for Computing Machinery, 1999, pp. 147–160. doi: 10.1145/292540.292555.

[35] D. E. Denning, "A lattice model of secure information flow," Commun. ACM, vol. 19, no. 5, pp. 236–243, May 1976, doi: 10.1145/360051.360056.

[36] U. Dal Lago and F. Gavazzo, "A relational theory of effects and coeffects," Proc. ACM Program. Lang., vol. 6, no. POPL, Jan. 2022, doi: 10.1145/3498692.

[37] H. Garcia-Molina and K. Salem, "Sagas," in Proceedings of the 1987 ACM SIGMOD International Conference on Management of Data, in SIGMOD '87. 1987, pp. 249–259. doi: 10.1145/38713.38742.

[38] R. Johnson and B. Foote, "Designing Reusable Classes," Journal of Object-Oriented Programming, vol. 1, pp. 22–35, 1988.

[39] M. Fowler, "Inversion of Control Containers and the Dependency Injection pattern." [Online]. Available: https://martinfowler.com/articles/injection.html

[40] A. M. Pitts and I. D. B. Stark, "Observable Properties of Higher Order Functions that Dynamically Create Local Names, or What's New?," in Mathematical Foundations of Computer Science 1993 (MFCS 1993), in Lecture Notes in Computer Science, vol. 711. Springer, 1993, pp. 122–141. doi: 10.1007/3-540-57182-5_8.

[41] R. P. James and A. Sabry, "Yield: Mainstream Delimited Continuations," in First International Workshop on the Theory and Practice of Delimited Continuations (TPDC 2011), 2011, pp. 20–32.

[42] webpack, "Hot Module Replacement." [Online]. Available: https://webpack.js.org/api/hot-module-replacement/

[43] Vite, "HMR API." [Online]. Available: https://vite.dev/guide/api-hmr

[44] OSGi Alliance, OSGi Core Release 8. OSGi Alliance, 2020.

[45] J. Kramer and J. Magee, "The Evolving Philosophers Problem: Dynamic Change Management," IEEE Transactions on Software Engineering, vol. 16, no. 11, pp. 1293–1306, 1990, doi: 10.1109/32.60317.

[46] Y. Vandewoude, P. Ebraert, Y. Berbers, and T. D'Hondt, "Tranquility: A Low Disruptive Alternative to Quiescence for Ensuring Safe Dynamic Updates," IEEE Transactions on Software Engineering, vol. 33, no. 12, pp. 856–868, 2007, doi: 10.1109/tse.2007.70733.

[47] J. S. Rellermeyer, G. Alonso, and T. Roscoe, "R-OSGi: Distributed Applications Through Software Modularization," in Proceedings of the ACM/IFIP/USENIX 8th International Middleware Conference, in Middleware '07. 2007, pp. 1–20. doi: 10.1007/978-3-540-76778-7_1.

[48] J. B. Dennis and E. C. Van Horn, "Programming Semantics for Multiprogrammed Computations," Communications of the ACM, vol. 9, no. 3, pp. 143–155, 1966.

[49] M. S. Miller, K.-P. Yee, and J. Shapiro, "Capability Myths Demolished," in Technical Report SRL2003-02, 2003.

[50] R. N. M. Watson, J. Anderson, B. Laurie, and K. Kennaway, "Capsicum: Practical Capabilities for UNIX," in Proceedings of the 19th USENIX Security Symposium, 2010, pp. 29–46.

[51] R. Wahbe, S. Lucco, T. E. Anderson, and S. L. Graham, "Efficient Software-Based Fault Isolation," in Proceedings of the 14th ACM Symposium on Operating Systems Principles, in SOSP '93. 1993, pp. 203–216. doi: 10.1145/168619.168635.

[52] A. Barth, A. P. Felt, P. Saxena, and A. Boodman, "Protecting Browsers from Extension Vulnerabilities," in Proceedings of the 17th Annual Network and Distributed System Security Symposium, in NDSS '10. 2010.

[53] W. W. Ho and R. A. Olsson, "An Approach to Genuine Dynamic Linking," Software: Practice and Experience, vol. 21, no. 4, pp. 375–390, 1991, doi: 10.1002/SPE.4380210404.

[54] P. Wadler and S. Blott, "How to Make Ad-hoc Polymorphism Less Ad Hoc," in Proceedings of the 16th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages, in POPL '89. 1989, pp. 60–76. doi: 10.1145/75277.75283.

[55] N. D. Matsakis and F. S. K. II, "The Rust Language and Type System," in ACM SIGPLAN ML Family Workshop, Gothenburg, Sweden, Sep. 2014.

[56] D. Dreyer, R. Harper, M. M. T. Chakravarty, and G. Keller, "Modular Type Classes," in Proceedings of the 34th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages, in POPL '07. 2007, pp. 63–70. doi: 10.1145/1190216.1190229.

[57] Microsoft, "Declaration Merging." [Online]. Available: https://www.typescriptlang.org/docs/handbook/declaration-merging.html

[58] T. Van Cutsem and M. S. Miller, "Proxies: Design Principles for Robust Object-oriented Intercession APIs," in Proceedings of the 6th Symposium on Dynamic Languages, in DLS '10. 2010, pp. 59–72. doi: 10.1145/1869631.1869638.

[59] R. Hettinger, "Descriptor HowTo Guide." [Online]. Available: https://docs.python.org/3/howto/descriptor.html

[60] P. Maes, "Concepts and Experiments in Computational Reflection," in Conference on Object-Oriented Programming Systems, Languages, and Applications (OOPSLA), 1987, pp. 147–155. doi: 10.1145/38765.38821.

[61] G. Bracha and D. M. Ungar, "Mirrors: design principles for meta-level facilities of object-oriented programming languages," in Proceedings of the 19th Annual ACM SIGPLAN Conference on Object-Oriented Programming, Systems, Languages, and Applications (OOPSLA), 2004, pp. 331–344. doi: 10.1145/1028976.1029004.

[62] R. Rouvoy and P. Merle, "Leveraging component-based software engineering with Fraclet," Annals of Telecommunications, vol. 64, no. 1–2, pp. 65–79, 2009, doi: 10.1007/s12243-008-0072-z.

[63] E. Burmako, "Scala Macros: Let Our Powers Combine!," in Proceedings of the 4th Workshop on Scala, in SCALA@ECOOP '13. 2013, pp. 3:1–3:10. doi: 10.1145/2489837.2489840.

[64] S. Raemaekers, A. van Deursen, and J. Visser, "Semantic Versioning and Impact of Breaking Changes in the Maven Repository," Journal of Systems and Software, vol. 129, pp. 140–158, 2017, doi: 10.1016/j.jss.2016.04.008.

[65] P. Lam, J. Dietrich, and D. J. Pearce, "Putting the Semantics into Semantic Versioning," in Proceedings of the 2020 ACM SIGPLAN International Symposium on New Ideas, New Paradigms, and Reflections on Programming and Software, in Onward! '20. 2020, pp. 157–179. doi: 10.1145/3426428.3426922.

[66] P. Abate, R. Di Cosmo, R. Treinen, and S. Zacchiroli, "Dependency Solving: A Separate Concern in Component Evolution Management," Journal of Systems and Software, vol. 85, no. 10, pp. 2228–2240, 2012, doi: 10.1016/j.jss.2012.02.018.

[67] L. Cardelli, "Structural Subtyping and the Notion of Power Type," in Proceedings of the 15th ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages, in POPL '88. 1988, pp. 70–79. doi: 10.1145/73560.73566.

[68] B. Meyer, "Applying "Design by Contract",", Computer, vol. 25, no. 10, pp. 40–51, 1992, doi: 10.1109/2.161279.

[69] B. C. Pierce, "Bounded Quantification is Undecidable," Information and Computation, vol. 112, no. 1, pp. 131–165, 1994, doi: 10.1006/inco.1994.1055.

[70] J. I. Brachthäuser, P. Schuster, and K. Ostermann, "Effects as capabilities: effect handlers and lightweight effect polymorphism," Proc. ACM Program. Lang., vol. 4, no. OOPSLA, 2020, doi: 10.1145/3428194.

[71] J. I. Brachthäuser, P. Schuster, E. Lee, and A. Boruch-Gruszecki, "Effects, capabilities, and boxes: from scope-based reasoning to type-based reasoning and back," Proc. ACM Program. Lang., vol. 6, no. OOPSLA1, 2022, doi: 10.1145/3527320.

[72] Effect, "Effect: A TypeScript library for building robust applications." [Online]. Available: https://effect.website/

[73] C. Heunen, R. Kaarsgaard, and M. Karvonen, "Reversible Effects as Inverse Arrows," in Proceedings of the Thirty-Fourth Conference on the Mathematical Foundations of Programming Semantics (MFPS XXXIV), in Electronic Notes in Theoretical Computer Science, vol. 341. 2018, pp. 179–199. doi: 10.1016/j.entcs.2018.11.009.

[74] D. Orchard, V.-B. Liepelt, and H. Eades III, "Quantitative program reasoning with graded modal types," Proc. ACM Program. Lang., vol. 3, no. ICFP, 2019, doi: 10.1145/3341714.

[75] R. Bianchini, F. Dagnino, P. Giannini, E. Zucca, and M. Servetto, "Coeffects for sharing and mutation," Proc. ACM Program. Lang., vol. 6, no. OOPSLA2, Oct. 2022, doi: 10.1145/3563319.

[76] R. Bianchini, F. Dagnino, P. Giannini, and E. Zucca, "A Java-like calculus with heterogeneous coeffects," Theoretical Computer Science, vol. 971, p. 114063, 2023, doi: https://doi.org/10.1016/j.tcs.2023.114063.

[77] C. Torczon, E. Suárez Acevedo, S. Agrawal, J. Velez-Ginorio, and S. Weirich, "Effects and Coeffects in Call-by-Push-Value," Proc. ACM Program. Lang., vol. 8, no. OOPSLA2, Oct. 2024, doi: 10.1145/3689750.

[78] R. Hirschfeld, P. Costanza, and O. Nierstrasz, "Context-oriented Programming," Journal of Object Technology, vol. 7, no. 3, pp. 125–151, 2008, doi: 10.5381/jot.2008.7.3.a4.

[79] P. Costanza and R. Hirschfeld, "Language constructs for context-oriented programming: an overview of ContextL," in Proceedings of the 2005 Symposium on Dynamic Languages (DLS '05), ACM, 2005, pp. 1–10. doi: 10.1145/1146841.1146842.

[80] G. Salvaneschi, C. Ghezzi, and M. Pradella, "Context-oriented programming: A software engineering perspective," Journal of Systems and Software, vol. 85, no. 8, pp. 1801–1817, 2012, doi: 10.1016/j.jss.2012.03.024.

[81] G. Kiczales et al., "Aspect-Oriented Programming," in ECOOP'97 — Object-Oriented Programming, 11th European Conference, in Lecture Notes in Computer Science, vol. 1241. Springer, 1997, pp. 220–242. doi: 10.1007/BFb0053381.

[82] G. Kiczales, E. Hilsdale, J. Hugunin, M. Kersten, J. Palm, and W. G. Griswold, "An Overview of AspectJ," in ECOOP 2001 — Object-Oriented Programming, 15th European Conference, in Lecture Notes in Computer Science, vol. 2072. Springer, 2001, pp. 327–353. doi: 10.1007/3-540-45337-7_18.

[83] A. Popovici, T. Gross, and G. Alonso, "Dynamic Weaving for Aspect-Oriented Programming," in Proceedings of the 1st International Conference on Aspect-Oriented Software Development (AOSD 2002), ACM, 2002, pp. 141–147. doi: 10.1145/508386.508404.

[84] J. Bonér, "What Are the Key Issues for Commercial AOP Use: How Does AspectWerkz Address Them?," in Proceedings of the 3rd International Conference on Aspect-Oriented Software Development (AOSD 2004), ACM, 2004, pp. 5–6. doi: 10.1145/976270.976273.

[85] M. Hicks, J. T. Moore, and S. Nettles, "Dynamic Software Updating," in Proceedings of the ACM SIGPLAN 2001 Conference on Programming Language Design and Implementation, in PLDI '01. 2001, pp. 13–23. doi: 10.1145/378795.378798.

[86] G. Stoyle, M. Hicks, G. Bierman, P. Sewell, and I. Neamtiu, "Mutatis Mutandis: Safe and Predictable Dynamic Software Updating," in Proceedings of the 32nd ACM SIGPLAN-SIGACT Symposium on Principles of Programming Languages, in POPL '05. 2005, pp. 183–194. doi: 10.1145/1040305.1040321.

[87] C. M. Hayden, K. Saur, E. K. Smith, and M. Hicks, "Kitsune: Efficient, General-Purpose Dynamic Software Updating for C," ACM Trans. Program. Lang. Syst., vol. 36, no. 4, 2014, doi: 10.1145/2629460.

[88] M. Overeem, M. Spoor, and S. Jansen, "The Dark Side of Event Sourcing: Managing Data Conversion," in IEEE 24th International Conference on Software Analysis, Evolution and Reengineering, in SANER '17. 2017, pp. 193–204. doi: 10.1109/SANER.2017.7884621.

[89] E. Gamma, R. Helm, R. Johnson, and J. Vlissides, Design Patterns: Elements of Reusable Object-Oriented Software. Boston, MA: Addison-Wesley, 1994.

[90] D. Leijen, "Algebraic Effect Handlers with Resources and Deep Finalization," technical report MSR-TR-2018-10, Apr. 2018.

[91] M. Fowler, "Event Sourcing." 2005.

[92] J. Lee, J. Ahn, and K. Yi, "React-tRace: A Semantics for Understanding React Hooks," Proc. ACM Program. Lang., vol. 9, no. OOPSLA2, pp. 471–498, 2025, doi: 10.1145/3763067.

[93] N. Shavit and D. Touitou, "Software Transactional Memory," in Proceedings of the Fourteenth Annual ACM Symposium on Principles of Distributed Computing, in PODC '95. 1995, pp. 204–213. doi: 10.1145/224964.224987.

[94] T. Harris, S. Marlow, S. Peyton Jones, and M. Herlihy, "Composable Memory Transactions," in Proceedings of the Tenth ACM SIGPLAN Symposium on Principles and Practice of Parallel Programming, in PPoPP '05. 2005, pp. 48–60. doi: 10.1145/1065944.1065952.

[95] M. Herlihy and J. E. B. Moss, "Transactional Memory: Architectural Support for Lock-Free Data Structures," in Proceedings of the 20th Annual International Symposium on Computer Architecture, in ISCA '93. 1993, pp. 289–300. doi: 10.1145/165123.165164.

[96] R. Landauer, "Irreversibility and Heat Generation in the Computing Process," IBM Journal of Research and Development, vol. 5, no. 3, pp. 183–191, 1961, doi: 10.1147/rd.53.0183.

[97] C. H. Bennett, "Logical Reversibility of Computation," IBM Journal of Research and Development, vol. 17, no. 6, pp. 525–532, 1973, doi: 10.1147/rd.176.0525.

[98] T. Yokoyama and R. Glück, "A Reversible Programming Language and its Invertible Self-Interpreter," in Proceedings of the 2007 ACM SIGPLAN Workshop on Partial Evaluation and Semantics-Based Program Manipulation, in PEPM '07. 2007, pp. 144–153. doi: 10.1145/1244381.1244404.

[99] P. Wadler, "Linear Types Can Change the World!," 1993.

[100] C. Walls, Spring in Action, 6th ed. Manning Publications, 2022.

[101] C. Escoffier, R. S. Hall, and P. Lalanda, "iPOJO: an Extensible Service-Oriented Component Framework," in IEEE International Conference on Services Computing, 2007, pp. 474–481. doi: 10.1109/SCC.2007.74.

[102] H. Cervantes and R. S. Hall, "Autonomous Adaptation to Dynamic Availability Using a Service-Oriented Component Model," in Proceedings of the 26th International Conference on Software Engineering, in ICSE '04. 2004, pp. 614–623. doi: 10.1109/ICSE.2004.1317483.

[103] C. Elliott and P. Hudak, "Functional Reactive Animation," in Proceedings of the Second ACM SIGPLAN International Conference on Functional Programming, in ICFP '97. 1997, pp. 263–273. doi: 10.1145/258948.258973.

[104] G. H. Cooper and S. Krishnamurthi, "Embedding Dynamic Dataflow in a Call-by-Value Language," in Programming Languages and Systems (ESOP 2006), in Lecture Notes in Computer Science, vol. 3924. Springer, 2006, pp. 294–308. doi: 10.1007/11693024_20.

[105] I. Maier and M. Odersky, "Deprecating the Observer Pattern with Scala.React," technical report EPFL-REPORT-176887, 2012. [Online]. Available: https://infoscience.epfl.ch/record/176887

[106] E. Bainomugisha, A. L. Carreton, T. Van Cutsem, W. De Meuter, and others, "A Survey on Reactive Programming," ACM Comput. Surv., vol. 45, no. 4, 2013, doi: 10.1145/2501654.2501666.
